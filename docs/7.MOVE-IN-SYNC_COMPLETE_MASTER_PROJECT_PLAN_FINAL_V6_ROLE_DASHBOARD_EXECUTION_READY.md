# MOVE-IN-SYNC — COMPLETE ENTERPRISE TRANSPORTATION PLATFORM
# MASTER PROJECT PLAN (COMPANY_TRANSPORT) — CONSOLIDATED SINGLE FILE
# Last Updated: 2026-09-03
# Status: LIVE IMPLEMENTATION — Resume from any session
# Version: FINAL V5 — Production Completion / Zero-Gap Implementation Specification
# Includes V4 functional scope plus final production engineering, API, portal, mobile,
# security, billing, observability, deployment, testing and go-live gates.
# Consolidates: original 23-phase implementation plan + multi-site policy-scoping
# design + full internal/external role structure + platform admin (SUPERADMIN/
# MOVE-IN-ADMIN) dashboard spec, all previously produced in this project thread.

---

## TABLE OF CONTENTS

1. Project Overview
2. Current Implementation Status
3. Architecture
4. Phase 0: Repository Stabilization
5. Phase 1: Database + Authentication + Tenancy
6. Phase 2: Organization + Roles + Permissions + Access Scopes
7. Phase 3: Employee + Hierarchy + Onboarding
8. Phase 4: Driver + Vehicle + Guard + Compliance
9. Phase 5: Locations + Maps + Nodal + Shuttle
10. Phase 6: Booking + Approval Workflows
11. Phase 7: Dispatch + Preferred Areas + Optimization
12. Phase 8: GPS + WebSockets + Geofencing
13. Phase 9: Breakdown + Replacement + SOS + Incidents
14. Phase 10: No-Show + Evidence + Appeal + Bans
15. Phase 11: Transport Billing + Vendors + Reconciliation
16. Phase 12: Employee Expenses
17. Phase 13: Analytics + Dashboards
18. Phase 14: AI + RAG + Copilot
19. Phase 15: Notifications
20. Phase 16: Mobile Apps + Offline + Multilingual
21. Phase 17: SaaS Subscriptions + Platform Billing
22. Phase 18: Security + Privacy
23. Phase 19: Observability + DR + Load Testing
24. Phase 20: Feature Flags + Staged Rollout + Rollback
25. Phase 21: Production Infrastructure + CI/CD
26. Phase 22: Frontend Admin Web Application
26B. Phase 22B: Platform Admin Frontend (SUPERADMIN / MOVE-IN-ADMIN — NEW)
26C. Phase 22C: Enterprise Dashboards + Reporting Platform — NEW
27. Testing Strategy
28. Non-Negotiable Rules
29. End-to-End Acceptance Test
30. Execution Roadmap
31. Multi-Company / Site / Shift Policy Scoping Design (NEW)
32. Full Role Structure & Capabilities — Internal + External (NEW)
33. Pre-Production Critical Gap Review (NEW)
34. Document Storage — Google Drive Integration + KYC/AML (NEW)
35. Employee CSV Template v2 + Flexible Team Assignment (NEW)
36. Manager — Full Booking/Cancel/Reschedule On Behalf of Employees (NEW)
37. Document Type Taxonomy — Multi-Country, Configurable, With Vendor Documents (NEW)
38. Training & Sandbox Environments — Company Practice Mode + SUPERADMIN Release Sandbox (NEW)
39. Vendor-less Drivers — "Others" Group for Billing (NEW)
40. SUPERADMIN SaaS Pricing Rules Engine + Multi-Currency + Production Scale (NEW)
41. Phase 41: Transport Control, Scheduling & Operational Policy Engine
42. Phase 42: Enterprise Architecture Hardening + Role Authority + Resilience
43. Phase 43: Cross-Process Clubbing + Flexible Billing Architecture
44. Phase 44: Driver Availability + Mobile GPS + Dynamic Dispatch + Multi-Admin Control
45. Phase 45+: Final Production Completion Master Specification

---

## 1. PROJECT OVERVIEW

**Product**: MOVE-IN-SYNC (temporary name — do not hard-code into business logic)
**Type**: Enterprise multi-tenant employee transportation and mobility management platform
**Architecture**: Monorepo — NestJS API Gateway + Next.js Web + Python ML Service + Prisma/PostgreSQL

**Goal**: Complete production-grade transportation management supporting:
- Multiple companies/tenants with different organizational structures
- Configurable RBAC with site/LOB/process/shift scoping
- Full transport lifecycle: Booking → Approval → Dispatch → Trip → Completion → Billing
- AI-powered routing, analytics, and copilot
- Mobile apps for employees and drivers
- SaaS platform with subscriptions, API keys, webhooks

---

## 2. CURRENT IMPLEMENTATION STATUS

### 2.1 What Exists

| Layer | Count | Details |
|---|---|---|
| Prisma Models | 186 | 5,740 lines |
| Enums | 90+ | Statuses, types, classifications |
| API Endpoints | 444 | Across 40 controllers |
| Services | 43 | 13,513 lines |
| Guards | 5 | JWT, Tenant, Roles, Permissions, Rate-Limit |
| WebSocket | 1 | EventsGateway (Socket.IO) — trip subscription + driver location |
| Redis | Yes | @keyv/redis cache module |
| BullMQ | Yes | Queue module scaffolded |
| Scheduler | Yes | @nestjs/schedule with compliance checks |
| Feature Flags | Yes | In-memory cache with company-level flags |
| ML Service | Scaffolded | Python/FastAPI: route optimizer, demand forecaster, anomaly detector, safety scorer, carbon tracker |
| Docker Compose | Yes | PostgreSQL, Redis, Elasticsearch, API, ML, Web |
| Tests | 4 spec files | auth, billing, employee, trip |
| Frontend | 5 TSX files | Demo dashboard with navigation |
| Dockerfiles | 2 | api-gateway + ml-service |

### 2.2 Critical Gaps

| Gap | Severity | Impact |
|---|---|---|
| No live database (demo mode) | CRITICAL | Nothing persists |
| No real mobile apps | CRITICAL | No driver/employee experience |
| No real GPS hardware integration | CRITICAL | No live tracking |
| No ML routing engine (only Haversine) | HIGH | Suboptimal routes |
| No HRMS integration | HIGH | Manual employee sync |
| No comprehensive tests | HIGH | No reliability guarantee |
| No CI/CD pipeline | MEDIUM | No deployment automation |
| Frontend is demo-only | HIGH | No usable admin UI |
| No real billing engine | HIGH | No automated invoicing |
| No real notification delivery | MEDIUM | No push/SMS/WhatsApp |

### 2.3 Existing Model Inventory (186 Models)

**Core**: Company, User, Session, Role, Permission, RolePermission, UserRoleAssignment
**Organization**: BusinessUnit, Department, Team, CompanyMembership, Invitation, CompanySite, LineOfBusiness, OrgProcess, Shift, Region, CostCenter, Office, OfficeLocation
**Access Control**: AccessScope, PermissionDefinition, RolePermissionConfig, ApprovalLevelConfig, UserAccessScopeHistory, TransportAccessRole, TransportAccessAssignment
**Employee**: ManagerRelationship, EmployeeTransportEligibility, EmployeeOrgAssignment, EmployeeOnboarding, EmergencyContact, SavedLocation, DocumentUpload, UserPreference
**Bulk Import**: EmployeeImportJob, EmployeeImportRow, EmployeeCSVImport, EmployeeCSVRow
**Driver**: DriverProfile, DriverTrip, DriverShift, DriverWorkSession, DriverVehicleAssignment, DriverWorkHoursPolicy, DriverWorkLog, DriverComplianceStatus, DriverPreferredArea, DriverDevice, DriverWallet, DriverWalletTransaction, DriverManagement, DriverPerformanceMetric
**Vehicle**: Vehicle, VehicleInspection, VehicleMaintenance, VehicleBreakdown, VehicleCapacityConfig, VehicleOnboarding, VehicleManagement
**Guard**: GuardRequirement, SafetyOptimizationLog
**Booking**: Booking, BookingPassenger, ShuttleBooking, RecurringBooking, TripSharingContact, PickupReminder
**Trip**: Trip, TripPassenger, TripStop, TripChangeRequest, TripCostSnapshot, TripCost, TripExpense, RouteMatchCandidate
**Boarding**: Boarding, BoardingVerification, PassengerBoarding
**Dispatch**: DispatchAssignment, ReplacementAssignment, RedispatchTrigger, DemandPressureSnapshot, DispatchPreferenceAudit
**GPS/Geofence**: LatestVehicleLocation, LocationPing, Geofence, GeofenceEvent, RouteDeviation, PickupArrivalEvent
**Policy**: TransportPolicy, TransportPolicyConfig, PickupDropTiming, TransportBoundary, CabCapacityConfig, CabApproval, ApprovalWorkflow, ApprovalLevel, ApprovalConfig, ApprovalEntry, ApprovalDelegation
**No-Show**: NoShowEvidence, NoShowPolicyConfig, PassengerContactAttempt, SupervisorCallRequest, NoShowAppeal, NoShowPolicy, EmployeeNoShowRecord
**Safety**: SOSAlert, EmergencyEvacuation, EmergencyBroadcast, EmergencyBuzzer, EmergencySystemContact, EmergencyContact
**Incidents**: Incident, Complaint, LostFound, Feedback
**Ban**: TransportBanPolicy, TransportBan, TransportBanRemovalRequest, TransportBanApproval, TransportBanRecord
**Expense**: TransportExpense, ExpenseDispute, EmployeeTransportLimit, ExpensePolicyConfig
**Billing**: PricingConfig, RateCard, RateCardHistory, BudgetAllocation, TripCost, VendorInvoice, InvoiceReconciliation, VendorSLAPenalty, VendorPaymentRecord
**Vendor**: Vendor, VendorOnboarding, VendorManagement, VendorUser, VendorDriverProfile, VendorVehicle, VendorTripRecord, VendorPerformanceMetric
**Compliance**: ComplianceConfig, ComplianceRule, ComplianceDocument, ComplianceAlert, ComplianceTeam, CompanyDocumentType, CompanyComplianceDoc, ComplianceAudit, ComplianceTask, CompliancePolicy, SuperComplianceConfig, SuperComplianceAlert, SuperComplianceScore, PlatformComplianceRule, CompanyComplianceStanding
**Routes**: Route, RouteStop, TransportService, RouteManagement, NodalPoint, ShuttleRoute
**Notifications**: Notification, CommunicationLog, CommunicationPreference, MassCancellation
**AI**: AIConversation, AIMessage, AIUsage, KnowledgeDocument, KnowledgeChunk
**Platform**: PlatformSettings, Job, AuditLog, FuelEntry, OdometerReading, DashboardAnalytics, AdminMFASecret

---

## 3. ARCHITECTURE

### 3.1 Tech Stack

| Layer | Technology |
|---|---|
| API Gateway | NestJS (TypeScript) |
| Database | PostgreSQL 16 + Prisma ORM — **single system of record, no MongoDB** (see decision note below) |
| Cache | Redis 7 |
| Queue | BullMQ |
| Search | Elasticsearch 8 |
| WebSocket | Socket.IO |
| Web Frontend | Next.js (React) |
| ML Service | Python FastAPI + scikit-learn |
| Auth | JWT + Passport + bcrypt |
| Container | Docker + Docker Compose |
| Target Infra | Kubernetes |

**Decision: PostgreSQL only, no MongoDB.** Tenant isolation (Rule 2) needs Row-Level Security, which has no Mongo equivalent and shouldn't depend on every query remembering to filter by `companyId`. The policy-scoping resolution (Section 31), financial ledger data (rate cards, invoices, reconciliation), and geospatial queries (pickup clustering, geofencing) are all naturally relational/joined workloads — denormalizing them into documents trades one set of bugs for another. Where a Mongo-shaped need shows up, use a Postgres extension instead of a second database: **TimescaleDB** for high-frequency GPS pings, **pgvector** for AI/RAG embeddings, **JSONB** for genuinely semi-structured fields (e.g. `PolicyScope.configJson`), and the existing **Elasticsearch** for full-text search — one system of record, not two to keep in sync.

### 3.2 Module Structure

```
apps/
  api-gateway/src/
    common/           # Guards, middleware, interceptors, decorators
    modules/
      auth/           # Authentication, JWT, MFA, device binding
      company-admin/  # Company administration
      dashboard/      # Admin dashboard (17 controllers)
      employee-management/
      employees/      # Employee CRUD + trip sharing
      enterprise-ops/
      finance/        # Budget, invoice reconciliation, SLA
      fleet/          # Driver, vehicle, compliance, wallet
      notifications/
      no-show-evidence/
      org-management/ # Region, site, LOB, process
      phase4a/        # Bulk import, expenses, limits
      safety/         # Safety optimization, guard, evacuation
      security/       # MFA, device binding
      storage/        # File upload/download
      trips/          # Trip management, ETA, route matching
      billing/        # SaaS billing
      communications/ # Email, SMS, WhatsApp
      driver-preferences/
  web/src/
    app/              # Next.js pages
    components/       # React components
  ml-service/app/
    api/v1/endpoints/ # ML API endpoints
    models/           # ML models
    services/         # ML services
```

---

## PHASE 0: REPOSITORY STABILIZATION

**Estimated Time**: 1-2 sessions
**Goal**: Get project running with real database, all endpoints verified, zero TS errors.

### Tasks

| # | Task | Status |
|---|---|---|
| 0.1 | TS build errors | DONE |
| 0.2 | Docker Compose | EXISTS |
| 0.3 | Prisma migrate | PARTIAL |
| 0.4 | Database seed | MISSING |
| 0.5 | Connect API to real DB | DEMO MODE |
| 0.6 | Verify all 444 endpoints | PARTIAL |
| 0.7 | Fix demo-mode fallbacks | MANY |
| 0.8 | Env configuration | PARTIAL |

### Verification

- [ ] nest build = 0 errors
- [ ] docker-compose up starts all
- [ ] API at localhost:3001
- [ ] JWT login works
- [ ] All controllers mount
- [ ] Prisma Studio shows data

**Deliverable**: Project running with real DB and verified endpoints.

---

## PHASE 1: AUTH + TENANCY

**Estimated Time**: 1-2 sessions
**Goal**: MFA, API keys, tenant isolation

### Tasks

| # | Task | Status |
|---|---|---|
| 1.1 | Company CRUD | EXISTS |
| 1.2 | User CRUD | EXISTS |
| 1.3 | JWT | EXISTS |
| 1.4 | bcrypt | EXISTS |
| 1.5 | Session | EXISTS |
| 1.6 | TenantGuard | EXISTS |
| 1.7 | RolesGuard | EXISTS |
| 1.8 | PermsGuard | EXISTS |
| 1.9 | RateLimit | EXISTS |
| 1.10 | RequestID | EXISTS |
| 1.11 | MFA | EXISTS |
| 1.12 | DeviceBind | EXISTS |
| 1.13 | API keys | MISSING |
| 1.14 | API key guard | MISSING |
| 1.15 | Secrets | PARTIAL |
| 1.16 | CORS | HARDCODED |
| 1.17 | Lockout | MISSING |
| 1.18 | PwdPolicy | MISSING |
| 1.19 | Timeout | MISSING |
| 1.20 | RefreshToken | MISSING |

**Deliverable**: Production auth with MFA, API keys

---

## PHASE 2: ORG + RBAC + SCOPES

**Estimated Time**: 3-4 sessions
**Goal**: Site/LOB/process/shift scoping

### Tasks

| # | Task | Status |
|---|---|---|
| 2.1 | Region | EXISTS |
| 2.2 | Site | EXISTS |
| 2.3 | LOB | EXISTS |
| 2.4 | Process | EXISTS |
| 2.5 | Shift | EXISTS |
| 2.6 | Dept/BU/CC | EXISTS |
| 2.7 | Role config | PARTIAL |
| 2.8 | Perm toggle | PARTIAL |
| 2.9 | AccessScope | EXISTS |
| 2.10 | AccessScopeGuard | MISSING-CRITICAL |
| 2.11 | Effective Access | MISSING |
| 2.12 | Access Preview | MISSING |
| 2.13 | Temp Access | MISSING |
| 2.14 | Access History | EXISTS |
| 2.15 | Inheritance | MISSING |
| 2.16 | Authz Cache | MISSING |
| 2.17 | Revocation | MISSING |
| 2.18 | Auditable Authz | MISSING |
| 2.19 | `PolicyScope` model + generic resolver service (company→site→process→shift, most-specific-wins) | MISSING-NEW |
| 2.20 | `AccessScopeGuard` implementation — unblocks 2.10, also enforces PolicyScope writes stay within admin's scope | MISSING-CRITICAL |
| 2.21 | `maxScopeLevel` field on `TransportAccessAssignment` — caps which scope level a given admin can set policy at | MISSING-NEW |
| 2.22 | Scope-violation attempts logged to AuditLog (not just rejected) | MISSING-NEW |
| 2.23 | `PlatformRoleAssignment` model + seed 7 internal roles (SUPERADMIN/FINANCE/PROJECT_MANAGER/PROJECT_COORDINATOR/COMPLIANCE/SECURITY_ADMIN/SUPPORT/AUDITOR) — separate from tenant `AccessScope` | MISSING-NEW |

**Deliverable**: Full RBAC with scoping, plus generic policy-scope resolution and platform-level (internal) role assignment

> See Section 31 for the full `PolicyScope` design (schema, resolution algorithm, worked example) and Section 32 for the full internal/external role capability matrix that 2.23 seeds.

---

## PHASE 3: EMPLOYEE + HIERARCHY

**Estimated Time**: 2-3 sessions
**Goal**: Import, hierarchy, self-service

### Tasks

| # | Task | Status |
|---|---|---|
| 3.1 | Profile | EXISTS |
| 3.2 | ManagerRel | EXISTS |
| 3.3 | OrgAssignment | EXISTS |
| 3.4 | Eligibility | EXISTS |
| 3.5 | Self-service | PARTIAL |
| 3.6 | Bulk import | EXISTS |
| 3.7 | Template | EXISTS |
| 3.8 | Validate | EXISTS |
| 3.9 | Errors | EXISTS |
| 3.10 | Config levels | MISSING |
| 3.11 | Access level | MISSING |
| 3.12 | Email valid | PARTIAL |
| 3.13 | Mobile norm | MISSING |
| 3.14 | Coords | PARTIAL |
| 3.15 | Location type | MISSING |
| 3.16 | Manager map | PARTIAL |
| 3.17 | Nodal pref | MISSING |

**Deliverable**: Employee management with import

---

## PHASE 4: DRIVER + VEHICLE + GUARD

**Estimated Time**: 2-3 sessions
**Goal**: Compliance, inspection, guard

### Tasks

| # | Task | Status |
|---|---|---|
| 4.1 | Driver profile | EXISTS |
| 4.2 | Driver CRUD | EXISTS |
| 4.3 | Compliance docs | EXISTS |
| 4.4 | Expiry alerts | EXISTS |
| 4.5 | Dispatch block | PARTIAL |
| 4.6 | Vehicle model | EXISTS |
| 4.7 | Vehicle CRUD | EXISTS |
| 4.8 | QR verify | MISSING |
| 4.9 | Inspection | EXISTS |
| 4.10 | Capacity | EXISTS |
| 4.11 | Guard model | PARTIAL |
| 4.12 | Guard compliance | MISSING |
| 4.13 | Guard optimization | EXISTS |
| 4.14 | Guard search | PARTIAL |
| 4.15 | Guard assign | PARTIAL |
| 4.16 | Guard escalation | PARTIAL |
| 4.17 | Preferred areas | EXISTS |
| 4.18 | Work hours | EXISTS |

**Deliverable**: Driver/vehicle/guard with compliance

---

## PHASE 5: MAPS + NODAL + SHUTTLE

**Estimated Time**: 3-4 sessions
**Goal**: Routing abstraction, navigation

### Tasks

| # | Task | Status |
|---|---|---|
| 5.1 | Cab booking | EXISTS |
| 5.2 | Shuttle | EXISTS |
| 5.3 | Seat reservation | PARTIAL |
| 5.4 | Shuttle utilization | MISSING |
| 5.5 | Nodal transport | EXISTS |
| 5.6 | Nodal engine | MISSING |
| 5.7 | Route model | EXISTS |
| 5.8 | Routing provider | HAVERSINE-OSRM |
| 5.9 | Route cache | MISSING |
| 5.10 | Provider tracking | MISSING |
| 5.11 | Fallback | MISSING |
| 5.12 | Navigation links | MISSING |
| 5.13 | Geospatial filter | MISSING |
| 5.14 | Metro commute | MISSING |

**Deliverable**: Routing, navigation, shuttle, nodal

---

## PHASE 6: BOOKING + APPROVALS

**Estimated Time**: 2-3 sessions
**Goal**: Multi-level approvals

### Tasks

| # | Task | Status |
|---|---|---|
| 6.1 | Booking | EXISTS |
| 6.2 | Policy validation | PARTIAL |
| 6.3 | Approval workflow | EXISTS |
| 6.4 | Per-process | PARTIAL |
| 6.5 | Multi-level | EXISTS |
| 6.6 | Approver detect | PARTIAL |
| 6.7 | Cancellation | PARTIAL |
| 6.8 | Recurring | EXISTS |
| 6.9 | Emergency | PARTIAL |
| 6.10 | Last-minute | MISSING |
| 6.11 | Conflict detect | MISSING |
| 6.12 | Delegation | EXISTS |
| 6.13 | Bulk approval | MISSING |
| 6.14 | Approval chain resolution routed through `resolvePolicy()` instead of per-process hardcoding — closes 6.4 "Per-process: PARTIAL" | MISSING-NEW |

**Deliverable**: Booking with approvals, delegation

---

## PHASE 7: DISPATCH + OPTIMIZATION

**Estimated Time**: 3-4 sessions
**Goal**: Auto/manual dispatch engine

### Tasks

| # | Task | Status |
|---|---|---|
| 7.1 | Auto dispatch | PARTIAL |
| 7.2 | Dispatch factors | MISSING |
| 7.3 | Manual dispatch | PARTIAL |
| 7.4 | Override audit | MISSING |
| 7.5 | Decline tracking | PARTIAL |
| 7.6 | Decline analytics | MISSING |
| 7.7 | Preferred areas | EXISTS |
| 7.8 | Area priority | PARTIAL |
| 7.9 | High-demand | MISSING |
| 7.10 | Dispatch SLA | MISSING |
| 7.11 | Empty km | MISSING |
| 7.12 | AI dispatch | MISSING |
| 7.13 | Dispatch dashboard | MISSING |

**Deliverable**: Production dispatch engine

---

## PHASE 8: GPS + WEBSOCKET + GEOFENCE

**Estimated Time**: 4-5 sessions
**Goal**: Real-time GPS, 16+ alerts

### Tasks

| # | Task | Status |
|---|---|---|
| 8.1 | WebSocket | EXISTS |
| 8.2 | GPS ingestion | PARTIAL |
| 8.3 | Batch writes | MISSING |
| 8.4 | VehicleLocation | EXISTS |
| 8.5 | Geofence model | EXISTS |
| 8.6 | Geofence detection | PARTIAL |
| 8.7 | Route deviation | PARTIAL |
| 8.8 | GPS consent | MISSING |
| 8.9 | GPS retention | MISSING |
| 8.10 | Control room | MISSING |
| 8.11 | Duplicate protection | MISSING |
| 8.12 | Accuracy filter | MISSING |
| 8.13 | Overspeeding | MISSING |
| 8.14 | Stoppage detect | MISSING |
| 8.15 | 16+ alerts | MISSING |

**Deliverable**: Real-time GPS, geofencing, alerts

---

## PHASE 9: BREAKDOWN + SOS + INCIDENTS

**Estimated Time**: 3-4 sessions
**Goal**: Replacement, SOS cascade

### Tasks

| # | Task | Status |
|---|---|---|
| 9.1 | Breakdown | EXISTS |
| 9.2 | Breakdown notify | PARTIAL |
| 9.3 | Replacement search | PARTIAL |
| 9.4 | Capacity replace | EXISTS |
| 9.5 | Guard replace | PARTIAL |
| 9.6 | Transfer OTP/QR | MISSING |
| 9.7 | Split vehicles | PARTIAL |
| 9.8 | Trip preserved | EXISTS |
| 9.9 | Vehicle status | PARTIAL |
| 9.10 | SOS alert | EXISTS |
| 9.11 | SOS cascade | MISSING |
| 9.12 | Incidents | EXISTS |
| 9.13 | Incident SLA | PARTIAL |
| 9.14 | Lost found | EXISTS |
| 9.15 | Feedback | EXISTS |

**Deliverable**: Breakdown, replacement, SOS, incidents

---

## PHASE 10: NO-SHOW + APPEAL + BANS

**Estimated Time**: 3-4 sessions
**Goal**: No-show engine, appeals, SLA

### Tasks

| # | Task | Status |
|---|---|---|
| 10.1 | ArrivalEvent | EXISTS |
| 10.2 | GPS arrival | PARTIAL |
| 10.3 | Grace period | EXISTS |
| 10.4 | Call tracking | EXISTS |
| 10.5 | Call gap | MISSING |
| 10.6 | CallingProvider | MISSING |
| 10.7 | Evidence verify | EXISTS |
| 10.8 | No-show workflow | PARTIAL |
| 10.9 | SupervisorCall | EXISTS |
| 10.10 | Supervisor timeout | MISSING |
| 10.11 | Control room queue | MISSING |
| 10.12 | Appeal | EXISTS |
| 10.13 | 48h SLA | MISSING |
| 10.14 | Appeal escalate | MISSING |
| 10.15 | Appeal decision | MISSING |
| 10.16 | Ban | EXISTS |
| 10.17 | Ban removal | EXISTS |
| 10.18 | Secure email | MISSING |
| 10.19 | No-show grace period, call-gap timing, and ban thresholds resolved via `resolvePolicy()` (company→site→process→shift) instead of one global `NoShowPolicyConfig` row — this is the literal "Transport Admin sets no-show policy per site/process" requirement | MISSING-NEW |

**Deliverable**: No-show, calls, appeals, SLA, bans — configurable per site/process/shift, not just per company

---

## PHASE 11: BILLING + VENDORS + RECONCILIATION

**Estimated Time**: 4-5 sessions
**Goal**: Rate cards, reconciliation, GST

### Tasks

| # | Task | Status |
|---|---|---|
| 11.1 | Rate card | EXISTS |
| 11.2 | Versioning | EXISTS |
| 11.3 | Immutability | EXISTS |
| 11.4 | Trip cost | PARTIAL |
| 11.5 | Cost allocation | EXISTS |
| 11.6 | Budget vs actual | MISSING |
| 11.7 | Vendor CRUD | EXISTS |
| 11.8 | Vendor invoices | EXISTS |
| 11.9 | Reconciliation | EXISTS |
| 11.10 | Anomaly detect | MISSING |
| 11.11 | Vendor SLA | EXISTS |
| 11.12 | Vendor perf | EXISTS |
| 11.13 | 6 billing models | MISSING |
| 11.14 | Maker-checker | MISSING |
| 11.15 | GST | MISSING |

**Deliverable**: Billing, reconciliation, GST

---

## PHASE 12: EMPLOYEE EXPENSES

**Estimated Time**: 2-3 sessions
**Goal**: Expense lifecycle, limits

### Tasks

| # | Task | Status |
|---|---|---|
| 12.1 | Expense CRUD | EXISTS |
| 12.2 | Receipt upload | PARTIAL |
| 12.3 | Manager approval | EXISTS |
| 12.4 | Director escalate | MISSING |
| 12.5 | Reimbursement CSV | MISSING |
| 12.6 | Expense limits | EXISTS |
| 12.7 | Duplicate detect | PARTIAL |
| 12.8 | Dispute | EXISTS |
| 12.9 | Analytics | MISSING |
| 12.10 | OCR foundation | MISSING |
| 12.11 | Approval limits | MISSING |
| 12.12 | State machine | MISSING |

**Deliverable**: Expenses with approval, limits, export

---

## PHASE 13: ANALYTICS + DASHBOARDS

**Estimated Time**: 4-5 sessions
**Goal**: 5 dashboards, 30+ reports

### Tasks

| # | Task | Status |
|---|---|---|
| 13.1 | Trip analytics | PARTIAL |
| 13.2 | Utilization | PARTIAL |
| 13.3 | Cost analytics | PARTIAL |
| 13.4 | No-show rate | MISSING |
| 13.5 | Cancellation rate | MISSING |
| 13.6 | Breakdown rate | MISSING |
| 13.7 | Vendor perf | EXISTS |
| 13.8 | Safety analytics | PARTIAL |
| 13.9 | SLA analytics | PARTIAL |
| 13.10 | Executive dashboard | MISSING |
| 13.11 | Manager dashboard | PARTIAL |
| 13.12 | Employee dashboard | PARTIAL |
| 13.13 | Control room | MISSING |
| 13.14 | Finance dashboard | MISSING |
| 13.15 | Report framework | MISSING |
| 13.16 | 30 reports | MISSING |
| 13.17 | Report scheduling | MISSING |
| 13.18 | Report export | MISSING |
| 13.19 | AI SummarAIze | PARTIAL |

**Deliverable**: Dashboards, reports, AI summaries

---

## PHASE 14: AI + RAG + COPILOT

**Estimated Time**: 4-5 sessions
**Goal**: AI copilot with RAG + authz

### Tasks

| # | Task | Status |
|---|---|---|
| 14.1 | ML route | SCAFFOLDED |
| 14.2 | Demand forecast | SCAFFOLDED |
| 14.3 | Cost anomaly | SCAFFOLDED |
| 14.4 | Safety scoring | SCAFFOLDED |
| 14.5 | Carbon tracking | SCAFFOLDED |
| 14.6 | AI Copilot | PARTIAL |
| 14.7 | RAG base | EXISTS |
| 14.8 | AI authz | MISSING-CRITICAL |
| 14.9 | AI approval | MISSING |
| 14.10 | Provider abstraction | MISSING |
| 14.11 | AI usage | EXISTS |
| 14.12 | AI grounding | MISSING |
| 14.13 | AI tools | MISSING |

**Deliverable**: AI copilot, RAG, ML models

---

## PHASE 15: NOTIFICATIONS

**Estimated Time**: 3-4 sessions
**Goal**: Multi-channel delivery

### Tasks

| # | Task | Status |
|---|---|---|
| 15.1 | Model | EXISTS |
| 15.2 | Push FCM | MISSING |
| 15.3 | Email SES | PARTIAL |
| 15.4 | SMS Twilio | MISSING |
| 15.5 | WhatsApp | MISSING |
| 15.6 | In-app | PARTIAL |
| 15.7 | Event mapping | MISSING |
| 15.8 | Preferences | EXISTS |
| 15.9 | Templates | MISSING |
| 15.10 | Batching | MISSING |
| 15.11 | Retry | MISSING |

**Deliverable**: Push, email, SMS, WhatsApp, in-app

---

## PHASE 16: MOBILE APPS + OFFLINE

**Estimated Time**: 8-12 sessions
**Goal**: Employee + driver apps

### Tasks

| # | Task | Status |
|---|---|---|
| 16.1 | Emp Login | MISSING |
| 16.2 | Emp Dashboard | MISSING |
| 16.3 | Emp Book | MISSING |
| 16.4 | Emp Track | MISSING |
| 16.5 | Emp History | MISSING |
| 16.6 | Emp Expenses | MISSING |
| 16.7 | Emp Notifications | MISSING |
| 16.8 | Emp Profile | MISSING |
| 16.9 | Emp SOS | MISSING |
| 16.10 | Driver Login | MISSING |
| 16.11 | Driver Trips | MISSING |
| 16.12 | Driver Navigate | MISSING |
| 16.13 | Driver Boarding | MISSING |
| 16.14 | Driver Transit | MISSING |
| 16.15 | Driver NoShow | MISSING |
| 16.16 | Driver Breakdown | MISSING |
| 16.17 | Driver Inspection | MISSING |
| 16.18 | Driver Areas | MISSING |
| 16.19 | Driver Shift | MISSING |
| 16.20 | Offline SQLite | MISSING |
| 16.21 | i18n | MISSING |

**Deliverable**: Mobile apps with offline + i18n

---

## PHASE 17: SAAS BILLING + PLATFORM

**Estimated Time**: 4-5 sessions
**Goal**: Subscriptions, metering, webhooks

### Tasks

| # | Task | Status |
|---|---|---|
| 17.1 | Domain separation | PARTIAL |
| 17.2 | SaaS plans | PARTIAL |
| 17.3 | Subscription states | PARTIAL |
| 17.4 | Platform invoices | PARTIAL |
| 17.5 | GST | MISSING |
| 17.6 | Usage metering | PARTIAL |
| 17.7 | API keys | MISSING |
| 17.8 | Webhooks | MISSING |
| 17.9 | White-label | MISSING |
| 17.10 | Plan limits | MISSING |

**Deliverable**: SaaS billing, metering, webhooks

---

## PHASE 18: SECURITY + PRIVACY

**Estimated Time**: 3-4 sessions
**Goal**: Privacy, encryption, access

### Tasks

| # | Task | Status |
|---|---|---|
| 18.1 | Consent | MISSING |
| 18.2 | Data access | MISSING |
| 18.3 | Data correction | MISSING |
| 18.4 | Data deletion | MISSING |
| 18.5 | GPS consent | MISSING |
| 18.6 | Data retention | MISSING |
| 18.7 | Legal hold | MISSING |
| 18.8 | Tenant test | PARTIAL |
| 18.9 | Encryption | PARTIAL |
| 18.10 | Vuln scanning | MISSING |
| 18.11 | Superadmin access | MISSING |
| 18.12 | Break-glass | MISSING |

**Deliverable**: Privacy, retention, encryption

---

## PHASE 19: OBSERVABILITY + DR + LOAD

**Estimated Time**: 3-4 sessions
**Goal**: Metrics, backup, DR, load test

### Tasks

| # | Task | Status |
|---|---|---|
| 19.1 | Health | EXISTS |
| 19.2 | Logging | PARTIAL |
| 19.3 | No log secrets | MISSING |
| 19.4 | Prometheus | MISSING |
| 19.5 | Grafana | MISSING |
| 19.6 | APM tracing | MISSING |
| 19.7 | Backup RPO | MISSING |
| 19.8 | PITR | MISSING |
| 19.9 | DR plan | MISSING |
| 19.10 | Load testing | MISSING |
| 19.11 | Background jobs | PARTIAL |
| 19.12 | Retention cron | MISSING |

**Deliverable**: Metrics, tracing, backup, DR, load

---

## PHASE 20: FEATURE FLAGS + ROLLOUT

**Estimated Time**: 2-3 sessions
**Goal**: Flags, staged rollout, rollback

### Tasks

| # | Task | Status |
|---|---|---|
| 20.1 | Flag service | EXISTS |
| 20.2 | Flag UI | MISSING |
| 20.3 | Canary deploy | MISSING |
| 20.4 | Staged rollout | MISSING |
| 20.5 | Rollback | MISSING |
| 20.6 | Migration safety | PARTIAL |
| 20.7 | Test company | MISSING |
| 20.8 | Rollout assign | MISSING |

**Deliverable**: Feature flags, staged rollout

---

## PHASE 21: INFRA + CI/CD

**Estimated Time**: 3-4 sessions
**Goal**: K8s, CI/CD, monitoring

### Tasks

| # | Task | Status |
|---|---|---|
| 21.1 | Dockerfiles | EXISTS |
| 21.2 | K8s manifests | MISSING |
| 21.3 | CI/CD pipeline | MISSING |
| 21.4 | Registry | MISSING |
| 21.5 | Env management | MISSING |
| 21.6 | SSL/TLS | MISSING |
| 21.7 | CDN | MISSING |
| 21.8 | Log aggregation | MISSING |
| 21.9 | Secrets mgmt | MISSING |
| 21.10 | HPA auto-scale | MISSING |

**Deliverable**: K8s, CI/CD, monitoring

---

## PHASE 22: FRONTEND WEB APP

**Estimated Time**: 10-15 sessions
**Goal**: 40+ admin pages

### Tasks

| # | Task | Status |
|---|---|---|
| 22.1 | Auth pages | P0 |
| 22.2 | Exec dashboard | P0 |
| 22.3 | Manager dash | P0 |
| 22.4 | Employee mgmt | P0 |
| 22.5 | Booking | P0 |
| 22.6 | Trips | P0 |
| 22.7 | Fleet | P0 |
| 22.8 | Dispatch | P0 |
| 22.9 | Admin roles | P0 |
| 22.10 | Routes | P1 |
| 22.11 | No-show queue | P1 |
| 22.12 | Expenses | P1 |
| 22.13 | Billing | P1 |
| 22.14 | Vendors | P1 |
| 22.15 | Analytics | P1 |
| 22.16 | Safety | P1 |
| 22.17 | Compliance | P1 |
| 22.18 | Settings | P1 |
| 22.19 | AI Copilot | P2 |
| 22.20 | Support | P2 |

**Deliverable**: 40+ pages, responsive, role-based

---

## PHASE 22B: PLATFORM ADMIN FRONTEND (SUPERADMIN / MOVE-IN-ADMIN) — NEW

**Estimated Time**: 3-4 sessions
**Goal**: Cross-tenant platform-ops UI for internal roles. Every page in Phase 22 is scoped *inside* one company (Exec dashboard, Fleet, Dispatch, etc.). Nothing above serves the internal roles who operate *across* companies — this phase closes that gap.

**Key architecture decision**: SUPERADMIN/MOVE-IN-ADMIN and internal staff are not tenant users, so they cannot reuse the tenant-scoped `AccessScopeGuard` (2.20) — they need a separate `PlatformAdminGuard` with no company/site/process scope, just an internal-role check against `PlatformRoleAssignment` (2.23). Recommendation: keep this in a **separate app** (not the same Next.js app as tenant admin pages, gated by route group), since tenant data must never leak across companies (Rule 1) and one less shared-routing surface is one less place that rule can be violated by a bug.

### Tasks

| # | Task | Status | Internal role(s) with access |
|---|---|---|---|
| 22B.1 | `PlatformAdminGuard` — SUPERADMIN vs MOVE-IN-ADMIN vs other internal role check, no tenant scope | MISSING-NEW | All internal roles |
| 22B.2 | Companies list — create/deactivate, subscription status, plan (pulls from Phase 17.2–17.3) | MISSING-NEW | SUPERADMIN (all), Project Manager/Coordinator (assigned accounts only) |
| 22B.3 | Platform billing dashboard — invoices, GST, revenue by plan (pulls from Phase 17.4–17.5) | MISSING-NEW | SUPERADMIN, Finance Team |
| 22B.4 | Rollout groups + feature-flag admin UI — promote/abort INTERNAL_TEST→CANARY→EARLY→GENERAL (pulls from Phase 20.7–20.8) | MISSING-NEW | SUPERADMIN |
| 22B.5 | `SupportTicket` model + notification hookup (Phase 15) + triage UI — closes §66's platform-support-vs-transport-complaint split | MISSING-NEW | Support Engineer, SUPERADMIN |
| 22B.6 | Cross-tenant compliance/audit dashboard — surfaces `SuperComplianceScore` | MISSING-NEW | Compliance Team (Platform), Platform Auditor |
| 22B.7 | Break-glass access request + log UI (pulls from Phase 18.11–18.12) | MISSING-NEW | Security Administrator, SUPERADMIN |
| 22B.8 | Status page admin | MISSING-NEW | SUPERADMIN |
| 22B.9 | Role-scoped internal dashboards — Project Manager/Coordinator see only assigned companies, Finance sees only billing, etc. (not one undifferentiated internal view) | MISSING-NEW | All internal roles |

**Deliverable**: A platform-ops surface where SUPERADMIN, Finance, Project Manager/Coordinator, Compliance, Security Admin, Support, and Auditor each get exactly the cross-tenant visibility their role needs — see Section 32 for the full capability matrix these pages implement.

---


## TESTING STRATEGY

### Unit Tests (Target: 80%)
Auth, Employee, Booking, Dispatch, Trip, Safety, No-Show, Billing, Access

### Integration Tests
- Booking -> Approval -> Dispatch -> Trip -> Complete
- Breakdown -> Replacement -> Transfer -> Resume
- No-Show -> Calls -> Appeal -> Decision
- Employee Import -> Validate -> Import -> Verify
- Expense -> Submit -> Approve -> Payroll
- Cross-tenant access denial
- Unauthorized scope access denial

### Security Tests
- Company A -> Company B data: DENIED
- Employee A -> Employee B expense: DENIED
- Employee approves own expense: DENIED
- Forged companyId: REJECTED
- Expired JWT: 401
- Revoked API key: 403

### Load Tests
- 500 GPS events/second (< 100ms)
- 100 dispatch operations/minute (< 500ms)
- 10,000 WebSocket connections (stable)
- p95 API response < 200ms

---

## NON-NEGOTIABLE RULES

1. Never allow cross-tenant data access
2. Never trust client-supplied authorization
3. Never hard-code one company hierarchy
4. Role and scope must be separate
5. Permission and visibility must be separate
6. Supervisor only sees authorized scope employees
7. Higher hierarchy does NOT mean unrestricted access
8. Safety beats cost optimization
9. Capacity must never be exceeded
10. Compliance checked before dispatch
11. AI cannot bypass authorization
12. AI cannot fabricate data
13. Breakdown preserves original trip identity
14. No-show evidence preserved per policy
15. Historical pricing immutable
16. Financial domains separated
17. Every admin action auditable
18. High-risk releases have rollback
19. Feature complete = tested end-to-end

---

## END-TO-END ACCEPTANCE TEST

1. CREATE COMPANY -> 2. CREATE SITES -> 3. CREATE LOBs -> 4. CREATE PROCESSES -> 5. CREATE SHIFTS -> 6. CREATE ROLES -> 7. CONFIGURE PERMISSIONS -> 8. CONFIGURE ACCESS -> 9. IMPORT EMPLOYEES -> 10. ASSIGN HIERARCHY -> 11. ADD PICKUP/DROP -> 12. REQUEST TRANSPORT -> 13. APPROVAL -> 14. DISPATCH -> 15. DRIVER ACCEPT -> 16. VEHICLE VERIFY -> 17. NAVIGATION -> 18. PICKUP GEOFENCE -> 19. BOARDING -> 20. TRIP START -> 21. GPS -> 22. BREAKDOWN -> 23. REPLACEMENT -> 24. TRANSFER -> 25. TRIP RESUME -> 26. COMPLETION -> 27. COST -> 28. VENDOR INVOICE -> 29. RECONCILIATION -> 30. ANALYTICS -> 31. NO-SHOW -> 32. CALL EVIDENCE -> 33. APPEAL -> 34. ESCALATION -> 35. DECISION -> 36. BAN -> 37. BAN REMOVAL -> 38. AUDIT -> 39. SAAS USAGE -> 40. INVOICE -> 41. PAYMENT

---

## EXECUTION ROADMAP

### Priority 1 (Sessions 1-5): Foundation
1. Phase 0 - Real database + verify endpoints
2. Phase 10 - No-Show + Evidence + Appeals
3. Phase 2 - Access Scope Guard (security critical)
4. Phase 8 - GPS + WebSocket (real-time core)
5. Phase 22 - Frontend for existing features

### Priority 2 (Sessions 6-15): Core Transport
6. Phase 7 - Dispatch engine
7. Phase 11 - Billing engine
8. Phase 5 - Routing + navigation
9. Phase 9 - Breakdown + replacement
10. Phase 13 - Analytics dashboards
11. Phase 6 - Booking enhancements
12. Phase 4 - Guard + vehicle verification
13. Phase 12 - Expense enhancements
14. Phase 3 - Employee enhancements
15. Phase 15 - Notifications

### Priority 3 (Sessions 16-25): Advanced
16. Phase 14 - AI + RAG + Copilot
17. Phase 17 - SaaS billing
18. Phase 18 - Security hardening
19. Phase 16 - Mobile apps (employee)
20. Phase 16 - Mobile apps (driver)
21. Phase 19 - Observability
22. Phase 20 - Feature flags
23. Phase 21 - CI/CD + K8s
24. Phase 22 - Remaining frontend
25. Final: Integration testing

---

## TOTAL ESTIMATE

| Category | Sessions | Hours (est.) |
|---|---|---|
| Backend (Phases 0-21) | 60-80 | 30-40 |
| Frontend (Phase 22) | 10-15 | 5-8 |
| Mobile Apps (Phase 16) | 8-12 | 4-6 |
| Testing + QA | 10-15 | 5-8 |
| **TOTAL** | **88-122** | **44-62** |

---

*This plan covers all 146 specification points.*
*Resume from any session by checking Phase status.*
*File: docs/Company_transport_Detailed_Project_Plan.md*
## ADDITIONAL FEATURES — FULL COVERAGE GAP CLOSURE

The following features were missing from the initial plan. Added for 100% spec coverage.

---

### PHASE A: CORPORATE CARPOOLING (Spec 7, 32)

**Estimated Time**: 2-3 sessions | **Goal**: Intra-organizational ride-sharing.

| # | Task | Status |
|---|---|---|
| A.1 | Carpool matching engine (same route, same company) | MISSING |
| A.2 | Ride host controls (route, stops, capacity) | MISSING |
| A.3 | Participant seat reservation | MISSING |
| A.4 | Cost sharing / incentive calculation | MISSING |
| A.5 | Carbon savings calculation per ride | MISSING |
| A.6 | Safety: verified employees only | MISSING |
| A.7 | Parking integration (dedicated spots) | MISSING |
| A.8 | Carpool analytics | MISSING |

---

### PHASE B: WORKPLACE MANAGEMENT (Spec 7)

**Estimated Time**: 2-3 sessions | **Goal**: Desk, room, parking, visitor, meal management.

| # | Task | Status |
|---|---|---|
| B.1 | Desk booking model + CRUD | MISSING |
| B.2 | Meeting room booking model + CRUD | MISSING |
| B.3 | Parking spot allocation | MISSING |
| B.4 | Visitor management (check-in, badge, host notify) | MISSING |
| B.5 | Meal ordering / cafeteria management | MISSING |
| B.6 | Space utilization analytics | MISSING |
| B.7 | Integration with transport (arrival -> desk ready) | MISSING |

---

### PHASE C: EV DASHBOARD + SUSTAINABILITY (Spec 37, 68)

**Estimated Time**: 1-2 sessions | **Goal**: EV monitoring, carbon, ESG.

| # | Task | Status |
|---|---|---|
| C.1 | EV battery level tracking | EXISTS (schema) |
| C.2 | EV range estimation | MISSING |
| C.3 | Charging status monitoring | MISSING |
| C.4 | Charging station management | MISSING |
| C.5 | EV vs ICE fleet comparison dashboard | MISSING |
| C.6 | Carbon footprint per trip/vehicle/company | SCAFFOLDED (ML) |
| C.7 | ESG compliance reporting | MISSING |
| C.8 | Green kilometers tracking | MISSING |
| C.9 | Sustainability analytics | MISSING |

---

### PHASE D: GEOSPATIAL ENGINE (Spec 18, 30, 31)

**Estimated Time**: 2-3 sessions | **Goal**: H3/geohash, nodal, spatial filtering.

| # | Task | Status |
|---|---|---|
| D.1 | H3 spatial indexing for employee locations | MISSING |
| D.2 | Geohash-based clustering for nodal points | MISSING |
| D.3 | Nodal recommendation engine | MISSING |
| D.4 | Nodal point optimization (add/remove/merge) | MISSING |
| D.5 | Employee density heatmaps | MISSING |
| D.6 | Demand prediction per zone per shift | MISSING |
| D.7 | Spatial pre-filtering before routing API | MISSING |
| D.8 | Geofence auto-generation from employee clusters | MISSING |

---

### PHASE E: SAFETY ADVANCED (Spec 42, 52)

**Estimated Time**: 2-3 sessions | **Goal**: Safe reach, marshals, night shift safety.

| # | Task | Status |
|---|---|---|
| E.1 | 3-tier safe reach verification (SMS + app + supervisor) | MISSING |
| E.2 | Safe reach confirmation workflow | MISSING |
| E.3 | Marshal assignment for female employees on night shifts | MISSING |
| E.4 | Marshal configuration per company policy | MISSING |
| E.5 | Marshal availability tracking | MISSING |
| E.6 | Female employee night shift safety rules | MISSING |
| E.7 | SOS button with emergency contact cascade | EXISTS |
| E.8 | Emergency broadcast to drivers/vehicles in area | EXISTS |
| E.9 | Panic alert with auto-location sharing | MISSING |
| E.10 | Safety score per route/driver/vehicle | SCAFFOLDED (ML) |

---

### PHASE F: BILLING FORENSICS + FRAUD (Spec 81)

**Estimated Time**: 2-3 sessions | **Goal**: Fraud detection, billing anomalies.

| # | Task | Status |
|---|---|---|
| F.1 | Billing anomaly detection (wrong km, wrong rate, duplicates) | MISSING |
| F.2 | Invoice fraud patterns (inflated km, phantom trips) | MISSING |
| F.3 | Trip-route mismatch detection | MISSING |
| F.4 | Duplicate invoice detection | MISSING |
| F.5 | Rate card compliance verification | MISSING |
| F.6 | Tax calculation verification (GST) | MISSING |
| F.7 | Forensic report generation | MISSING |
| F.8 | Alert system for billing anomalies | MISSING |

---

### PHASE G: DISASTER RECOVERY + DEPLOYMENT (Spec 103, 105, 108)

**Estimated Time**: 2-3 sessions | **Goal**: DR, blue-green, migration safety.

| # | Task | Status |
|---|---|---|
| G.1 | Disaster recovery plan documentation | MISSING |
| G.2 | RPO/RTO targets (RPO ~15min, RTO ~2hr) | MISSING |
| G.3 | WAL archiving configuration | MISSING |
| G.4 | Point-in-time recovery testing | MISSING |
| G.5 | Backup verification (restore test) | MISSING |
| G.6 | Blue-green deployment configuration | MISSING |
| G.7 | DB migration safety (EXPAND -> MIGRATE -> VERIFY -> CONTRACT) | PARTIAL |
| G.8 | Known-good version tracking for rollback | MISSING |
| G.9 | DR runbook (step-by-step recovery) | MISSING |

---

### PHASE H: ADVANCED NOTIFICATION + APPROVAL (Spec 56, 69)

**Estimated Time**: 1-2 sessions | **Goal**: Expiring links, secure approval, proactive alerts.

| # | Task | Status |
|---|---|---|
| H.1 | Expiring link generation (ban removal approval) | MISSING |
| H.2 | Single-use token for email approvals | MISSING |
| H.3 | Cryptographic protection for approval links | MISSING |
| H.4 | Proactive compliance expiry alerts (60/30/15/7/1 day) | PARTIAL |
| H.5 | Proactive demand alerts (peak hours, capacity) | MISSING |
| H.6 | Proactive safety alerts (route deviation, overspeeding) | MISSING |
| H.7 | Digest notifications (daily/weekly summary) | MISSING |
| H.8 | Notification priority levels (critical/high/normal/low) | MISSING |

---

### PHASE I: ROUTE OPTIMIZATION ADVANCED (Spec 31, 42)

**Estimated Time**: 2-3 sessions | **Goal**: STEADFCT framework, daily route rebuilding.

| # | Task | Status |
|---|---|---|
| I.1 | Daily route rebuilding from confirmed bookings | MISSING |
| I.2 | STEADFCT framework (Safety, Time, Efficiency, Distance, Fuel, Cost, Traffic) | MISSING |
| I.3 | Route learning from feedback (employee satisfaction) | MISSING |
| I.4 | Predictive ETA with ML | MISSING |
| I.5 | Traffic-aware routing integration | MISSING |
| I.6 | Route versioning (historical routes preserved) | MISSING |
| I.7 | Dead mileage minimization across fleet | MISSING |
| I.8 | Dynamic re-routing during trip | MISSING |

---

### PHASE J: DRIVER EXPERIENCE ADVANCED (Spec 34, 35, 76)

**Estimated Time**: 1-2 sessions | **Goal**: Paperless ops, vernacular, wellness.

| # | Task | Status |
|---|---|---|
| J.1 | Paperless trip documentation | MISSING |
| J.2 | Vernacular language support (Hindi, Marathi, Tamil, etc.) | MISSING |
| J.3 | Driver performance dashboard | EXISTS |
| J.4 | Driver earnings visibility | EXISTS |
| J.5 | Driver wellness tracking (breaks, hours, fatigue) | PARTIAL |
| J.6 | Driver rating by employees | EXISTS |
| J.7 | Driver incentive programs | MISSING |
| J.8 | Driver training/compliance tracking | PARTIAL |

---

### UPDATED TOTAL ESTIMATE

| Category | Sessions | Hours (est.) |
|---|---|---|
| Backend (Phases 0-21) | 60-80 | 30-40 |
| Additional Features (A-J) | 16-23 | 8-12 |
| Frontend (Phase 22) | 10-15 | 5-8 |
| Mobile Apps (Phase 16) | 8-12 | 4-6 |
| Testing + QA | 10-15 | 5-8 |
| **TOTAL** | **104-145** | **52-72** |

---

## FINAL 100% COVERAGE VERIFICATION (146/146)

| Spec | Phase(s) | Status |
|---|---|---|
| 1-20 | Phase 2 | COVERED |
| 21-24 | Phase 3 | COVERED |
| 25-27 | Phase 6 | COVERED |
| 28-29 | Phase 5 | COVERED |
| 30 | Phase 5 + D | COVERED |
| 31 | Phase 5 + I | COVERED |
| 32 | Phase 7 | COVERED |
| 33 | Phase 7 | COVERED |
| 34 | Phase 7 + J | COVERED |
| 35 | Phase 4 + J | COVERED |
| 36 | Phase 4 | COVERED |
| 37 | Phase 4 + C | COVERED |
| 38-40 | Phase 4 | COVERED |
| 41 | Phase 4 + E | COVERED |
| 42 | Phase 4 + E + I | COVERED |
| 43 | Phase 9 | COVERED |
| 44 | Phase 8 | COVERED |
| 45 | Phase 8 + D | COVERED |
| 46-49 | Phase 9 | COVERED |
| 50-54 | Phase 10 | COVERED |
| 55 | Phase 10 | COVERED |
| 56 | Phase 10 + H | COVERED |
| 57 | Phase 12 | COVERED |
| 58-61 | Phase 11 + F | COVERED |
| 62-63 | Phase 4 | COVERED |
| 64 | Phase 9 + E | COVERED |
| 65-67 | Phase 9 | COVERED |
| 68 | Phase 13 + C | COVERED |
| 69-74 | Phase 14 | COVERED |
| 75 | Phase 15 + H | COVERED |
| 76-77 | Phase 16 + J | COVERED |
| 78-85 | Phase 17 | COVERED |
| 86-92 | Phase 1 + 2 + 18 | COVERED |
| 93-99 | Phase 13 | COVERED |
| 100 | Phase 22 | COVERED |
| 101-102 | Phase 19 | COVERED |
| 103 | Phase 19 + G | COVERED |
| 104-105 | Phase 20 + G | COVERED |
| 106 | Phase 20 | COVERED |
| 107 | Phase 14 + 20 | COVERED |
| 108-110 | Phase 20 + 16 | COVERED |
| 111 | Phase 1 | COVERED |
| 112 | Schema (186 models) | COVERED |
| 113-139 | Phase 2 | COVERED |
| 140-146 | All Phases + Plan | COVERED |

**146/146 = 100% COVERED. ZERO GAPS.**

---
## PHASE 23: ADVANCED / CUTTING-EDGE FEATURES (Post-MVP)

**Estimated Time**: 35-50 sessions | **Goal**: Competitive differentiation, future-proofing.

### 23.1 Digital Twin for Fleet
- Virtual vehicle replicas with real-time sync
- Route simulation, failure prediction, historical replay
- Fleet-wide twin dashboard, what-if analysis
- Sessions: 3-4

### 23.2 Predictive Maintenance (ML)
- Predict breakdowns before they happen (oil, brakes, battery, tires)
- Usage pattern analysis, auto-scheduling
- Cost-of-delay calculation, model accuracy tracking
- Sessions: 2-3

### 23.3 IoT Sensor Integration
- OBD-II/CAN bus data ingestion
- Tire pressure, engine health, fuel, battery temp
- Threshold alerts, data aggregation, retention policy
- Sessions: 2-3

### 23.4 Computer Vision for Inspection
- Photo upload + CV models (tires, body, lights, interior)
- Inspection score, pass/fail with confidence
- Integration with dispatch blocking
- Sessions: 2-3

### 23.5 NLP Receipt OCR
- Receipt photo preprocessing (deskew, crop, enhance)
- Auto-extract: provider, amount, date, fare, tax, toll
- Duplicate detection, confidence scoring
- Sessions: 2-3

### 23.6 Voice Commands for Drivers
- Wake word, command parsing, multi-language
- "I've arrived", "Passenger boarded", "Report breakdown"
- Safety mode, voice transcript audit trail
- Sessions: 2

### 23.7 Multi-Modal Trip Planning
- Combine cab + metro + shuttle + walk
- Public transit API integration
- Cost/time/carbon comparison per mode
- Sessions: 2-3

### 23.8 Dynamic Pricing Engine
- Peak-hour, distance-based, demand-based pricing
- Night surcharge, weekend/holiday rules
- Budget cap integration, pricing analytics
- Sessions: 2

### 23.9 Blockchain Audit Trail
- Pluggable: Ethereum, Hyperledger, local
- Hash critical events, verify integrity
- Emergency override, legal compliance
- Sessions: 2

### 23.10 Autonomous Vehicle Readiness
- AUTONOMOUS vehicle type, dispatch rules
- AV safety requirements, trip lifecycle
- Remote operator dashboard, fallback to human
- Sessions: 2

### 23.11 Smart EV Charging
- Off-peak charging optimization
- Range-aware dispatch, battery degradation model
- Charging cost calculator, fleet dashboard
- Sessions: 2

### 23.12 Carbon Credit Trading
- Carbon calculation per trip/vehicle/fleet
- Offset marketplace integration, ESG reporting
- Green certification, reduction targets
- Sessions: 2

### 23.13 Weather-Aware Routing
- Weather API integration (rain, flood, fog)
- Auto route re-planning, weather-adjusted ETAs
- Delay alerts, extreme weather cancellation
- Sessions: 1-2

### 23.14 Demand Heatmaps
- Demand aggregation by zone/time/shift
- Heatmap visualization, peak hour analysis
- Nodal recommendation from heatmaps
- Sessions: 1-2

### 23.15 Loyalty + Rewards
- Points system, reward catalog, leaderboard
- Gamification badges, manager recognition
- Integration with carpooling
- Sessions: 1-2

### 23.16 Emergency Response API
- Police/ambulance/fire API abstraction
- Auto-detect severity, auto-share location
- Emergency contact cascade, drill simulation
- Sessions: 2

### 23.17 AR Navigation
- ARKit/ARCore integration
- Pickup AR overlay, parking guidance
- Vehicle identification, office navigation
- Sessions: 2

### 23.18 Public Transit Integration
- City-specific transit API adapters
- Bus/metro schedules, last-mile coordination
- Transit pass management, delay alerts
- Sessions: 2

### 23.19 Advanced Analytics + Predictions
- Demand forecasting, cost anomaly detection
- Vehicle utilization prediction
- Safety incident prediction, BI dashboard
- Sessions: 2-3

---

### GRAND TOTAL (ALL PHASES)

| Category | Sessions | Hours |
|---|---|---|
| Backend (Phases 0-22) | 60-80 | 30-40 |
| Additional Features (A-J) | 16-23 | 8-12 |
| Advanced Features (Phase 23) | 35-50 | 18-25 |
| Frontend (Phase 22) | 10-15 | 5-8 |
| Mobile Apps (Phase 16) | 8-12 | 4-6 |
| Testing + QA | 15-20 | 8-10 |
| **GRAND TOTAL** | **144-200** | **73-101** |

---

---

## SECTION 31: MULTI-COMPANY / SITE / SHIFT POLICY SCOPING DESIGN

Addresses the gap between what's already scaffolded (Region/Site/LOB/Process/Shift models exist per Phase 2) and what's still missing to make "Transport Admin sets policies like no-show per site/process/role" actually work and actually be enforced.

### 31.1 The Model, Stated Precisely

```
Company (tenant)
  └── Project                      (1 project per company)
        └── Site                   (physical location — e.g. "Pune Hinjewadi")
              └── LOB / Process    (e.g. "Voice Support", "Back Office")
                    └── Shift      (e.g. "6AM-3PM", "Night Shift")
```

A **Transport Admin** is assigned an **AccessScope** — some subset of (company, project, site, process). Within that scope, they can *set* policies. Outside it, they can't see or touch anything — this is what `AccessScopeGuard` (task 2.20, MISSING-CRITICAL) enforces.

**Policies** (no-show grace period, call-attempt rules, ban thresholds, approval chains, cab/shuttle eligibility, cancellation windows, expense limits, etc.) can be set at **any level** of that hierarchy. A policy set at a lower level overrides one set higher up. A policy not set at any level falls back to a platform default.

This is the piece that's currently implicit rather than built: the plan has the org tree (Phase 2) and it has policy tables (`NoShowPolicyConfig`, `TransportPolicyConfig`, `ApprovalConfig`, etc.) but no single, consistent **resolution mechanism** shared across all of them.

### 31.2 Data Model

Rather than each policy type inventing its own scoping columns (which is what tends to happen organically — `NoShowPolicyConfig` gets a `siteId`, `TransportPolicyConfig` gets a different scoping shape, etc.), use **one generic scope-resolution pattern** that every policy type plugs into.

```prisma
// One row per (policy type, scope level, scope target) — no per-policy-table scoping columns
model PolicyScope {
  id          String   @id @default(uuid())
  companyId   String
  scopeLevel  ScopeLevel      // PLATFORM | COMPANY | SITE | PROCESS | SHIFT
  siteId      String?         // set when scopeLevel = SITE or narrower
  processId   String?         // set when scopeLevel = PROCESS or narrower
  shiftId     String?         // set when scopeLevel = SHIFT

  policyType  PolicyType      // NO_SHOW | CANCELLATION | BAN | APPROVAL | EXPENSE_LIMIT | ...
  configJson  Json            // the actual policy payload — shape depends on policyType

  setByUserId String          // which Transport Admin set this
  setByRole   String          // their role at time of setting — audit trail
  effectiveFrom DateTime
  effectiveTo   DateTime?     // null = still active

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@unique([companyId, scopeLevel, siteId, processId, shiftId, policyType, effectiveFrom])
  @@index([companyId, policyType])
}

enum ScopeLevel {
  PLATFORM   // SUPERADMIN/Compliance Team-set global default — lowest priority
  COMPANY
  SITE
  PROCESS
  SHIFT      // highest priority
}
```

**Why one table instead of per-policy-type scoping columns:** every new policy type (and the spec has a dozen — no-show, ban, expense, approval, cancellation, guard requirement...) automatically gets scoping, resolution, audit, and history for free. You write the resolver once.

### 31.3 Resolution Algorithm

```
resolvePolicy(companyId, policyType, { siteId, processId, shiftId }):
  candidates = PolicyScope.where(
    companyId = companyId,
    policyType = policyType,
    effectiveTo IS NULL OR effectiveTo > now(),
    (scopeLevel = SHIFT   AND shiftId = shiftId) OR
    (scopeLevel = PROCESS AND processId = processId) OR
    (scopeLevel = SITE    AND siteId = siteId) OR
    (scopeLevel = COMPANY) OR
    (scopeLevel = PLATFORM)
  )

  return candidates.sortBy(specificity: SHIFT > PROCESS > SITE > COMPANY > PLATFORM)
                    .first()
                    .configJson
```

Most-specific-wins, same pattern as CSS specificity or firewall rule ordering.

**Concrete example — no-show grace period:**
- Platform default: 10 minutes
- Company X sets: 15 minutes (company-wide)
- Company X's Pune site sets: 20 minutes (traffic is worse there)
- Company X's Pune "Night Shift" process sets: 25 minutes (safety — don't want people stranded at night)

An employee on the Pune night shift gets 25 minutes. An employee at the same site on day shift gets 20 minutes (site default). An employee at a different site gets 15 minutes (company default).

### 31.4 Enforcement — Closing the AccessScopeGuard Gap

This design is only real if a Transport Admin **cannot write** a `PolicyScope` row outside their assigned scope:

```
POST /policies/no-show
  → AccessScopeGuard checks: does requesting user's AccessScope
    cover the (siteId, processId, shiftId) in the request body?
  → If the admin's scope is "Site: Pune only" and they submit
    a PolicyScope for a Hyderabad site → 403, audited as a
    scope-violation attempt (not just a silent reject)
```

This directly closes three items in Phase 2:
- **2.20 AccessScopeGuard** — makes the whole scoping model enforceable, not just structurally present
- **2.21 maxScopeLevel** — an admin's `AccessScope` needs a cap on which scope level they're permitted to set policy at (e.g. a Site-level Transport Admin can set SITE/PROCESS/SHIFT policies but not COMPANY-level ones)
- **2.22 Scope-violation logging** — attempts get audited, not just rejected

### 31.5 Why This Is Worth Doing as a Foundational Pass, Not Per-Feature

Right now the plan has policy config scattered across `NoShowPolicyConfig`, `TransportPolicyConfig`, `ExpensePolicyConfig`, `ApprovalConfig`, `TransportBanPolicy` — five+ tables, each likely reinventing its own idea of "what level does this apply at." Retrofitting a shared scope model after five policy types each have their own shape is a much bigger job than building the one `PolicyScope` table now and having Phase 6/10/12 policies all read through it from the start. This is the same class of decision as hierarchy-as-configuration — worth getting right before more phases build on top of per-table scoping.

---

## SECTION 32: FULL ROLE STRUCTURE & CAPABILITIES — INTERNAL + EXTERNAL

Two separate role universes, because they answer different questions:
- **Internal roles** = people who work *for Move-In-Sync* and operate across (or in service of) many companies.
- **External roles** = people who work *for a client company* and only ever see their own company's data.

For the external side, this is split into **two parallel tracks**: a **transport-operations track** (Transport Admin/Coordinator/Compliance — people whose job *is* running transport) and the **org/reporting track** (Director → Employee — people whose job is something else, and who interact with transport only as requesters/approvers). Trainer sits outside both as a functional specialist.

### 32.1 Internal Roles (Move-In-Sync, the company)

```
SUPERADMIN (Owner)
  ├── Finance Team
  ├── Security Administrator (Platform)
  ├── Compliance Team (Platform)
  ├── Support Engineer
  ├── Platform Auditor (read-only, cross-tenant)
  └── Project Manager
        └── Project Coordinator
```

| Role | Scope | Capabilities | Cannot do |
|---|---|---|---|
| **SUPERADMIN (Owner)** | Entire platform | Create/deactivate companies; manage subscriptions & plans; manage all internal roles/users; global security & policy config; approve rollout promotion (CANARY→EARLY→GENERAL) and abort; break-glass access grant; full audit visibility; final approval on platform-wide feature flags | Nothing withheld — but every action is logged (§95, Rule 15), including support access into a tenant |
| **Finance Team** | Platform billing only, all companies | SaaS subscription invoicing, GST/CGST/SGST/IGST handling, revenue reporting, payment/webhook reconciliation, dunning for PAST_DUE/GRACE_PERIOD accounts, contract & plan pricing management | Cannot see transport operational data (bookings, GPS, driver data) — platform billing is a separate financial domain from transport billing (Rule 17) |
| **Project Manager** | Assigned set of companies (strategic) | Owns the relationship for assigned accounts; reviews cost/performance/utilization reports; approves major changes (plan upgrades, rollout opt-in) for their accounts; escalation point between company leadership and Move-In-Sync; reviews Project Coordinator's work | Cannot do hands-on data entry/import; cannot change platform-wide config |
| **Project Coordinator** | Assigned companies (execution) | Runs company onboarding: merges/uploads company org data (sites, processes, shifts, employees) via bulk import; validates import errors before go-live; requests rollout-group promotion (submits to SUPERADMIN, doesn't approve); monitors onboarding health; first escalation point for the company during setup | Cannot approve rollout promotion; cannot touch billing or platform security config |
| **Compliance Team (Platform)** | Cross-tenant, read + flag | Monitors driver/vehicle/vendor document compliance across *all* companies; tracks `SuperComplianceScore`; flags companies falling below regulatory thresholds; manages platform-level compliance *rules* (the PLATFORM scope level baseline every company inherits) | Cannot set a specific company's internal policy (that's the company's own Transport Compliance/Admin); read-only on operational data outside compliance scope |
| **Security Administrator (Platform)** | Platform-wide | MFA policy enforcement, break-glass request review/approval alongside SUPERADMIN, vulnerability scan triage, secret rotation, session/device policy | Cannot create companies or touch billing |
| **Support Engineer** | Platform support tickets (§66) | Triages `SupportTicket`s (billing/technical/account/onboarding/API/integration/security/subscription/feature-request categories); first-line resolution; escalates to the right internal team | Cannot resolve *transportation* complaints (those stay with the company's own Transport Admin — §66 keeps these separate) |
| **Platform Auditor** | Cross-tenant, read-only | Full read access to audit logs across all companies for compliance/legal review | No write access anywhere, ever |

**Dashboard mapping:** SUPERADMIN and Security Administrator get the full Phase 22B set. Project Manager/Coordinator get 22B.2 (scoped to assigned accounts only) + an onboarding-progress view. Finance Team gets 22B.3 only. Compliance Team gets 22B.6 only. Support Engineer gets 22B.5 only.

### 32.2 External Roles (each client company)

**Track A — Transport Operations (runs the transport function)**

```
Transport Admin
  └── Transport Coordinator
Transport Compliance   (parallel — reports into Transport Admin, doesn't manage ops)
```

| Role | Scope | Capabilities | Cannot do |
|---|---|---|---|
| **Transport Admin** | Company-wide, or a specific site if delegated (per `AccessScope`) | Everything operational: manage drivers/vehicles/vendors, dispatch, breakdown/no-show/ban workflows; **sets policy** (no-show grace period, ban thresholds, approval chains) at their `maxScopeLevel` via `PolicyScope` — company-wide if assigned at COMPANY level, or narrower if assigned at SITE/PROCESS | Cannot touch SaaS billing/subscription; cannot set policy above their assigned scope level |
| **Transport Coordinator** | Delegated subset of a Transport Admin's scope | Day-to-day: booking management, trip monitoring, driver/vehicle coordination, dispatch support, passenger support, document checking | **Cannot set policy** — `PolicyScope` writes require Transport Admin role; sensitive actions (ban imposition, policy change) require Transport Admin approval |
| **Transport Compliance** | Company-wide or site, read + flag | Reviews driver/vehicle/vendor document expiry and validity; flags non-compliant assets (blocks them from dispatch eligibility per Rule 4); tracks the company's own compliance standing; escalates gaps to Transport Admin | Cannot approve bookings, dispatch, or set policy — compliance is a check function, not an operational one |

**Track B — Org / Reporting Hierarchy (requests & approves transport as part of their normal job)**

```
Director
  └── Senior Manager
        └── Manager
              └── Assistant Manager
                    └── Team Leader
                          └── Employee
```

| Role | Scope | Capabilities | Cannot do |
|---|---|---|---|
| **Director** | Whole company (or assigned division) | Executive dashboard visibility; approves escalated transport-policy exceptions and high-value budget items; final internal escalation point before it goes to Transport Admin/Compliance | Does not do operational transport management — that's Track A's job |
| **Senior Manager** | Multiple Managers' teams | Aggregated visibility across their managers' teams; approves escalations Managers can't resolve; reviews regional/site cost & utilization | Cannot access employees outside their reporting scope (hierarchy ≠ unrestricted access) |
| **Manager** | Their reportees (AMs, TLs, Employees) | Approves/denies transport requests; approves employee expenses; reviews no-show appeals and eligibility requests; **books, cancels, and reschedules transport on behalf of any employee in their full downline** (see Section 36) | Cannot approve their own request (separation of duties, Rule 3) |
| **Assistant Manager** | Delegated subset of Manager's team | Reviews & approves requests where policy permits; monitors team transportation; escalates to Manager | Approval authority is policy-gated, not automatic |
| **Team Leader** | Their direct team | Views team members' transport status; monitors bookings; submits/approves requests where configured; escalates issues | Narrowest approval authority in the ladder |
| **Employee** | Self only | Requests transport; manages own locations; tracks own trips; views own history; appeals own no-shows; submits own expenses | Cannot change own eligibility; cannot approve own request; cannot see anyone else's data |

**Functional specialist (outside both tracks)**

| Role | Scope | Capabilities | Cannot do |
|---|---|---|---|
| **Trainer** | Drivers/employees' training records, company-wide or site | Manages training modules and safety certification tracking; records completion; flags employees/drivers overdue on required training (can feed into Transport Compliance's dispatch-eligibility checks) | No access to financial, dispatch, GPS, or booking data — training data only |

### 32.3 How This Plugs Into the Rest of the Plan

- Every external role above gets an `AccessScope` row (existing model) with a `maxScopeLevel` (task 2.21) — one mechanism serving both org-hierarchy visibility and transport-ops policy authority, not two separate systems.
- Internal roles need `PlatformRoleAssignment` (task 2.23) — deliberately separate from any tenant's data model, gated by `PlatformAdminGuard` (task 22B.1).

### 32.4 Resolved — Soft Flags vs. Hard Blocks

**Decision**: hard compliance failures **auto-block** (Rule 4 requires it — an expired license or insurance cannot leave a driver dispatch-eligible even for a moment). Trainer flags (training overdue) **queue for Transport Admin sign-off** instead — a missed training module is a real risk to manage, but not one that should strand a route the moment it's flagged. Transport Compliance's flags are hard-block *only* when tied to a mandatory document (license/insurance/PUC/permit/fitness); anything softer routes through the sign-off queue the same as Trainer flags.

---

## SECTION 33: PRE-PRODUCTION CRITICAL GAP REVIEW

Everything below is either already MISSING-CRITICAL elsewhere in this plan or newly surfaced — flagged here together because these are the ones that will actually hurt in production, not just look incomplete in a table.

| # | Gap | Why it's critical before go-live | Where it's tracked |
|---|---|---|---|
| 1 | No live database — demo mode | Nothing persists. This blocks literally everything else. | 0.5 |
| 2 | `AccessScopeGuard` missing | Without it, tenant/site isolation and the whole `PolicyScope` model (Sec. 31) are unenforced — a bug away from cross-company or cross-site data leakage | 2.10/2.20 |
| 3 | No mobile apps | Drivers and employees have no real-world interface — trip lifecycle can't run without them | Phase 16 |
| 4 | GPS consent (§93) not built | Live-tracking employees without a working consent/withdrawal flow is a legal exposure, not just a feature gap | 18.5 |
| 5 | GST/tax rules not legally reviewed | §84 explicitly says tax rules "must be legally reviewed before production use" — this needs a human sign-off, not just code | 17.5 |
| 6 | No pen test / vulnerability scan run | Storing DL numbers, KYC docs, GPS trails, financial data with zero security testing is the highest-severity gap on this list | 18.10 |
| 7 | DR untested — no backup restore test | A backup nobody has restored is not a backup (per the "not valid until tested" rule elsewhere in this plan) | 19.7–19.9 |
| 8 | No load testing | Dispatch, GPS ingestion, and WebSocket connections are exactly the systems that fail silently under real concurrent load if untested | 19.10 |
| 9 | No comprehensive tests (4 spec files only) | Breakdown/no-show/ban workflows are stateful and easy to regress silently without test coverage | 2.2 (Critical Gaps) |
| 10 | Data deletion / retention / legal hold missing | Required for any GDPR-style request and for GPS data specifically (§92 — GPS data must not be retained forever) | 18.4, 18.6, 18.7 |
| 11 | No real notification delivery (push/SMS/WhatsApp) | Approval, dispatch, no-show, and breakdown flows all depend on notifications actually reaching someone | Phase 15 |
| 12 | No CI/CD pipeline | Manual deploys to a system handling live GPS/dispatch/payment data is itself a production risk | 21.3 |

**Recommendation:** items 2, 4, 5, 6, and 7 are the ones that create legal or security exposure rather than just missing functionality — treat those as the actual go-live blockers, independent of whichever phase order the rest of the build follows.

---

## SECTION 34: DOCUMENT STORAGE — GOOGLE DRIVE INTEGRATION + KYC/AML

### 34.1 Why not just point uploads straight at Drive

Google Drive can absolutely be the storage backend — but driver documents (license, KYC ID, vehicle RC/insurance) are exactly the sensitive-document case the plan's Phase 21 secrets/storage design already worries about, so a couple of guardrails matter more than for ordinary file storage:

- Use a **Shared Drive per company** (not a personal Drive folder), owned by a Move-In-Sync service account, so access is governed by the platform's own permission model rather than Google's sharing UI — a driver or Transport Admin should never get a raw "share" link.
- The **file lives in Drive; the metadata, verification status, and audit trail live in your own DB** — Drive is a blob store here, not the system of record. This also means switching providers later (S3, Azure Blob) doesn't touch your compliance workflow.
- Treat this as one implementation of the existing **storage-provider abstraction** (Phase 21 already targets pluggable storage) — Drive for companies that want it, S3-compatible for the default, same interface underneath.

### 34.2 Data Model

```prisma
model DriverDocument {
  id             String   @id @default(uuid())
  driverId       String
  companyId      String
  docType        DocType         // LICENSE_PHOTO | VEHICLE_PHOTO | RC | INSURANCE | PUC | PERMIT | FITNESS | KYC_ID | KYC_ADDRESS_PROOF

  storageProvider StorageProvider // GOOGLE_DRIVE | S3
  driveFileId     String?         // Drive file ID, if provider = GOOGLE_DRIVE
  fileUrl         String          // resolvable URL/reference regardless of provider
  fileHash        String          // integrity check, same pattern as §49 call-evidence hashing

  documentDate    DateTime        // date the DRIVER enters (e.g. license issue/renewal date) — not the upload timestamp
  uploadedAt      DateTime @default(now())

  verificationStatus VerificationStatus  // PENDING | MATCHED | MISMATCHED | EXPIRED
  verifiedByUserId   String?             // Compliance Team member
  verifiedAt         DateTime?
  mismatchReason     String?             // required if MISMATCHED

  expiryDate      DateTime?       // for license/insurance/PUC/permit/fitness
  createdAt       DateTime @default(now())
}

model KYCVerification {
  id            String   @id @default(uuid())
  driverId      String   // or vendorId, employeeId — KYC applies wherever identity verification is needed
  companyId     String
  kycType       KYCType         // IDENTITY | ADDRESS | AML_SCREENING
  status        KYCStatus       // PENDING | VERIFIED | REJECTED
  documentRef   String?         // links to DriverDocument.id if identity-doc based
  screeningResult Json?         // AML watchlist screening result, if kycType = AML_SCREENING
  verifiedByUserId String?
  verifiedAt      DateTime?
  notes           String?
}
```

### 34.2b Folder Structure, In-App Preview, and Direct Link

**Resolved**: "my drive folder" = a single Move-In-Sync-owned Drive, organized per company/driver — not each driver's personal Drive. The same abstraction also supports a company bringing its own Drive later without a redesign.

**Folder taxonomy** (auto-created on first upload, not manually maintained):
```
/MoveInSync-Documents/
  {companyId}/
    drivers/
      {driverId}/
        LICENSE_PHOTO/2026-09-04_license.jpg
        VEHICLE_PHOTO/2026-09-04_vehicle.jpg
        RC/...
        INSURANCE/...
```

**Why the raw Drive file shouldn't be linked directly to the app.** A license photo or KYC ID is exactly the kind of file where "anyone with the link can view" (Drive's normal sharing model) is the wrong default — a leaked link means a leaked ID document, and Drive's own sharing UI doesn't know about your `AccessScopeGuard`. So "visible in the app + a direct link" is built as two layers instead of one raw Drive link:

1. **In-app preview**: backend fetches file bytes from Drive via the service account (`files.get` with `alt=media`) and streams them through your own authenticated API — `GET /documents/{id}/view`. This *is* the "direct link" the app shows, it just resolves through your own auth/scope check first, so only someone authorized to see that driver's documents (Compliance Team, Transport Admin/Compliance in scope, SUPERADMIN) can actually open it — same guard as everything else in this plan, not a separate Drive-managed permission.
2. **Native Drive link (internal-only, optional)**: for Compliance Team members who already have access to the Shared Drive itself and prefer opening files natively in Drive's UI, also store the `webViewLink` — but this is only surfaced to roles that already have Shared Drive access, never to drivers/employees/managers.

Updated model:
```prisma
model DriverDocument {
  // ...fields from 34.2...
  driveFolderPath  String   // e.g. "{companyId}/drivers/{driverId}/LICENSE_PHOTO"
  driveWebViewLink String?  // native Drive link — Compliance Team (Shared Drive members) only
  thumbnailUrl     String?  // small preview for list views, generated server-side or via Drive's thumbnailLink
  // proxyViewUrl is NOT stored — it's derived: `/documents/{id}/view`, always resolved through AccessScopeGuard
}
```

**List views** (e.g. Compliance Team's review queue) use `thumbnailUrl` so scrolling a queue of 50 pending documents doesn't pull 50 full-size images. Opening a specific document calls `/documents/{id}/view` for the full-resolution proxied stream.

**New task:**

| # | Task | Phase |
|---|---|---|
| 25.v | Drive service-account integration: auto-create folder taxonomy, upload, `files.get(alt=media)` streaming proxy endpoint with AccessScopeGuard, thumbnail generation | Phase 4 |

### 34.3 Verification Workflow (Compliance Team)

```
Driver uploads document (license photo, vehicle photo, RC, insurance...)
       ↓
Driver enters the document's own date (issue/renewal/expiry — not upload date)
       ↓
File saved to company's Shared Drive folder, hash computed
       ↓
DriverDocument row created: status = PENDING
       ↓
Compliance Team reviews:
   - Does the photo match the driver/vehicle on file?
   - Is the document date valid / not expired?
       ↓
   MATCHED ─────────────────→ status = MATCHED, driver stays dispatch-eligible
       │
   MISMATCHED ───────────────→ status = MISMATCHED, mismatchReason required,
                                 driver/vehicle BLOCKED from dispatch (Rule 4),
                                 Transport Admin + driver notified
       ↓
Expiry approaching (60/30/15/7/1 day, per existing H.4 alerts) → re-upload requested
```

### 34.4 KYC & AML — Where They Actually Apply

- **KYC (driver/vendor onboarding)**: identity document verification (matches §25's existing "identity documentation" requirement) — now formalized as a `KYCVerification` row with explicit status, not just a document upload with no verification state.
- **AML**: most naturally applies to **financial flows**, not driver photos — specifically **vendor payouts** (Phase 11) and **large employee expense reimbursements** (Phase 12), where a screening check against sanctions/watchlists is the standard control. Add an `AML_SCREENING` KYC type that runs at vendor onboarding and periodically thereafter, not per-trip.
- **New tasks to add:**

| # | Task | Phase |
|---|---|---|
| 25.x | `DriverDocument` model + Google Drive storage-provider adapter + hash verification | Phase 4 (Driver + Vehicle) |
| 25.y | Compliance Team review queue UI (match/mismatch, reason capture) | Phase 22B (feeds Compliance Team's 22B.6 dashboard) |
| 25.z | `KYCVerification` model, identity + AML screening types | Phase 4 + Phase 11 (vendor onboarding) |
| 25.w | AML screening triggered at vendor onboarding + periodic re-screen | Phase 11 |

---

## SECTION 35: EMPLOYEE CSV TEMPLATE v2 + FLEXIBLE TEAM ASSIGNMENT

### 35.1 Updated Bulk Import Template

Confirming what's already required and adding what was missing explicitly:

| Column | Required | Notes |
|---|---|---|
| Employee ID | Yes | Unique within company (existing rule) |
| Employee Name | Yes | |
| Work Email / Mobile | Yes | |
| Address | Yes | Free text |
| Latitude, Longitude | Yes | Drives pickup-point clustering (§31 nodal optimization) — reject rows with invalid/missing coordinates rather than silently geocoding from address alone |
| Department / Business Unit / Cost Centre / Project / Region / Office / Shift | Yes | |
| Team Leader / Assistant Manager / Manager | **Optional if auto-assign is used** | See 35.2 |
| Transport Eligibility | Yes | Admin-set, never self-service (existing rule) |
| Nodal Preference | Optional | |

### 35.2 "Fill the Rest From Headcount" — Auto-Assign Reporting Chain

Rather than requiring every row to spell out Team Leader/AM/Manager by name (error-prone at scale, and what you're asking for), add an **auto-assign mode**:

- Company already has a **headcount roster** — the existing set of TLs/AMs/Managers and their current team sizes/capacity.
- Import can run in two modes:
  - **Explicit mode**: CSV specifies the manager chain per row (current behavior).
  - **Auto-assign mode**: CSV omits manager columns; the system distributes new employees across existing TLs in the target department/site/shift. **Resolved: capacity-based balancing** — each new employee goes to the TL with the fewest current reportees in that department/site/shift (toward equal team sizes over time), with round-robin as the tiebreaker when multiple TLs are equally loaded. Pure round-robin alone was rejected because it ignores existing team-size imbalance when onboarding employees mid-year. The proposed assignment is shown in the **Preview** step (existing 24-import-workflow step) for the Transport Admin to confirm or override before final import.
- Either way, every assignment — explicit or auto — writes through the same `EmployeeTeamAssignment` model below, so it's editable afterward the same way.

### 35.3 Moving and Duplicating Employees Across Team Leaders

Current model (`ManagerRelationship`, Phase 3.2) is implicitly 1:1. Replace/extend with a proper many-to-many assignment model, since you need both **moves** (with history preserved, already a stated rule in §22) and **duplicates** (an employee assigned to more than one Team Leader at once — e.g. dual reporting, temporary loaner to another team):

```prisma
model EmployeeTeamAssignment {
  id           String   @id @default(uuid())
  employeeId   String
  teamLeaderId String          // or AM/Manager — same model, different `assignmentLevel`
  assignmentLevel AssignmentLevel  // TEAM_LEADER | ASSISTANT_MANAGER | MANAGER
  isPrimary    Boolean  @default(true)   // exactly one PRIMARY per level at a time; others are secondary/duplicate
  effectiveFrom DateTime
  effectiveTo   DateTime?       // null = currently active; old rows kept, never deleted (§22 history rule)
  reason        String?         // e.g. "temporary loan to Team B", "reorg"
  changedByUserId String
}
```

- **Move**: close out the current PRIMARY row (`effectiveTo = now()`), open a new PRIMARY row under the new TL. Old row stays queryable for history.
- **Duplicate**: add a second row with `isPrimary = false` under a different TL — the employee now shows up in both TLs' team views.
- **Resolved**: a non-primary (secondary/duplicate) TL is **view-only** for that employee — cannot approve, reject, or act on their transport requests. Only the PRIMARY TL is the approver of record, unless the request is explicitly reassigned first. This avoids two TLs approving conflicting requests for the same employee.

---

## SECTION 36: MANAGER — FULL BOOKING/CANCEL/RESCHEDULE ON BEHALF OF EMPLOYEES

Updates Section 32's Manager row: Managers get more than approve/deny — they can act **on behalf of** any employee in their full downline (not just direct reports), for booking, cancellation, and rescheduling.

| Capability | Detail |
|---|---|
| Book on behalf of | Manager creates a booking for any employee in their org scope, same eligibility/policy checks apply as if the employee booked it themselves |
| Cancel on behalf of | Manager cancels an existing booking/trip for any employee in scope, subject to the same cancellation-policy windows (§Phase 6) |
| Reschedule on behalf of | Manager changes date/time/pickup-drop for an existing booking in scope |
| Audit requirement | Every on-behalf-of action records **acting user (Manager) + subject employee** separately in the audit log — never attributed to the employee alone, so it's clear later who actually initiated it |
| Scope | "All employees" = the Manager's full downline per the org hierarchy (AMs, TLs, and their employees) — not just direct reports, and still bounded by Rule: hierarchy access ≠ unrestricted, so this stays inside the Manager's assigned company/region/department scope |

**New task — Phase 6:**

| # | Task |
|---|---|
| 6.15 | Booking/cancel/reschedule "on behalf of" flow for Manager role, with dual-identity audit logging (acting user + subject employee) |

---

## SECTION 37: DOCUMENT TYPE TAXONOMY — MULTI-COUNTRY, CONFIGURABLE, WITH VENDOR DOCUMENTS

### 37.1 Why This Can't Be a Hardcoded Enum

"Identity documents for all countries" is the same shape of problem as the org hierarchy (§131) and policy scoping (Section 31): a fixed enum (`AADHAAR | PAN | SSN | ...`) means every new country requires a code change. Instead, document types are **configuration data**, scoped by country, that Compliance Team/SUPERADMIN can add to without a release — the upload dropdown is just a filtered query against this table, not a hardcoded list in the frontend.

This also generalizes `DriverDocument` (Section 34) into a shared model, since vendors need documents too (registration proof, vendor ID card) and shouldn't get a separate, duplicate table.

### 37.2 Data Model

```prisma
model DocumentTypeDefinition {
  id              String   @id @default(uuid())
  countryCode     String?         // ISO 3166-1 alpha-2 (e.g. "IN", "US", "AE") — null = universal/global type
  category        DocCategory     // IDENTITY | VEHICLE | VENDOR | OTHER
  appliesTo       PartyType[]     // DRIVER | EMPLOYEE | VENDOR
  code            String          // e.g. "AADHAAR", "PAN", "RC", "PUC", "VENDOR_REG_CERT"
  label           String          // "Aadhaar Card" — shown in the dropdown
  requiresExpiry  Boolean         // drives whether the upload form asks for an expiry date
  isMandatory     Boolean         // for onboarding-completion checks
  isActive        Boolean  @default(true)
  createdByUserId String?         // SUPERADMIN/Compliance Team who added it (custom types are audited like anything else)

  @@unique([countryCode, category, code])
}

enum DocCategory { IDENTITY, VEHICLE, VENDOR, OTHER }
enum PartyType   { DRIVER, EMPLOYEE, VENDOR }

// Section 34's DriverDocument generalizes to PartyDocument (renamed, same fields otherwise):
model PartyDocument {
  id                String   @id @default(uuid())
  partyType         PartyType       // DRIVER | EMPLOYEE | VENDOR
  partyId           String          // driverId / employeeId / vendorId depending on partyType
  companyId         String
  docTypeId         String          // FK → DocumentTypeDefinition
  otherLabel        String?         // free-text label, only used when docType.code = "OTHER"

  storageProvider   StorageProvider
  driveFolderPath   String
  driveWebViewLink  String?
  thumbnailUrl      String?

  fileHash          String
  documentDate      DateTime        // date the uploader enters (issue/renewal date)
  uploadedAt        DateTime @default(now())

  verificationStatus VerificationStatus  // PENDING | MATCHED | MISMATCHED | EXPIRED
  verifiedByUserId   String?
  verifiedAt         DateTime?
  mismatchReason     String?
  expiryDate         DateTime?
}
```

### 37.3 Seed Data — Representative, Not Exhaustive

Seed a starting set; Compliance Team/SUPERADMIN extends it per country as new companies onboard from new geographies.

**Identity (India, `countryCode: "IN"`)**: Aadhaar Card, PAN Card, Voter ID, Passport, Driving Licence (also valid as ID proof)
**Identity (universal, `countryCode: null`)**: Passport (accepted everywhere as a fallback identity doc)
**Identity (extend per country as needed, e.g. `"US"`, `"AE"`, `"GB"`)**: seed only when a company from that country actually onboards — no need to pre-populate every country upfront

**Vehicle (universal)**: Registration Certificate (RC), Insurance, Fitness Certificate, Vehicle Tax / Road Tax Receipt
**Vehicle (India-specific)**: PUC (Pollution Under Control) — this is an India-only requirement; other countries have an equivalent emissions test that gets its own `code` under that country's `countryCode` rather than being forced into "PUC" universally
**Vehicle (universal)**: Permit (commercial vehicle permit, format varies by country but the category is universal)

**Vendor**: Vendor Registration Certificate, Company Incorporation Proof, GST/Tax Registration Certificate, Vendor ID Card, Vendor Insurance/Liability Certificate, Bank Account Proof (for payouts)

**Other (universal, always available)**: `code: "OTHER"` — free-text `otherLabel` field captures anything not in the list, so onboarding never blocks on a missing document type; Compliance Team can later promote a frequently-used "Other" label into a proper seeded type.

### 37.4 Upload UI Behavior

- Dropdown is filtered by: `partyType` (is this a driver, employee, or vendor upload?) + `category` (identity/vehicle/vendor) + the party's `countryCode`, falling back to universal (`countryCode: null`) types.
- Selecting "Other" reveals the free-text `otherLabel` field.
- `requiresExpiry` on the selected type drives whether the form shows a mandatory expiry-date field (identity docs like Aadhaar typically don't expire; PUC/insurance/permits do).

### 37.5 New Tasks

| # | Task | Phase |
|---|---|---|
| 25.u | `DocumentTypeDefinition` model + seed data (India + universal set) + admin CRUD for Compliance Team/SUPERADMIN to add new country/doc types | Phase 4 |
| 25.t | Generalize `DriverDocument` → `PartyDocument` (adds vendor + employee document support on the same model/workflow as Section 34) | Phase 4 + Phase 11 (vendor onboarding) |
| 25.s | Upload dropdown: country + category + partyType filtered query against `DocumentTypeDefinition`, with "Other" free-text fallback | Phase 4 + Phase 22 (frontend) |

---

## SECTION 38: TRAINING & SANDBOX ENVIRONMENTS

Two different sandboxes, for two different audiences — worth keeping separate rather than one generic "sandbox," since they have different owners, different data, and different lifecycles.

### 38.1 Company Training Sandbox (for a company's own users)

**Purpose**: let a company's Transport Admins, Coordinators, Managers, TLs, and even drivers/employees *practice* the real workflows — booking, approval, dispatch, no-show, breakdown — without touching production trips, GPS, or billing.

- Each company gets a **Training Environment**, structurally a clone of their real org (same sites/processes/shifts/roles), but pointed at synthetic data — reuses the existing `Sandbox` concept from §88/17.x rather than inventing a second mechanism.
- A user's session carries a `TRAINING` vs `PRODUCTION` mode flag (same identity, explicit mode switch — not a separate login), so the same Transport Admin who manages real drivers on Monday can run new-hire training in the sandbox on Tuesday without a second account.
- **Nothing in Training mode reaches Production**: no real driver/vehicle gets dispatched, no real billing event fires, no real notification goes to a real phone number. This needs to be enforced at the service layer (every write checks the session's mode flag), not just hidden in the UI.
- Typical use: onboarding a new Transport Coordinator, or a company piloting a new site's process before going live with real employees.

### 38.2 SUPERADMIN Training/Release Sandbox (for platform updates)

**Purpose**: formalizes what's already implied by the existing rollout-group design (Phase 20, §102's Internal Test Company) into an explicit update workflow, rather than leaving "how does an update actually get validated before reaching a real company" implicit.

```
New platform update built
       ↓
Auto-enrolled into INTERNAL_TEST (the synthetic internal test company, §102)
       ↓
Automated test suite runs against it
       ↓
   FAIL ──────────────→ update blocked, feature flag stays OFF, reported back to build
       ↓ PASS
Manual/QA sign-off in the test sandbox
       ↓
   FAIL ──────────────→ blocked, same as above
       ↓ PASS
SUPERADMIN promotes to CANARY (small subset of real companies, opted in)
       ↓
Monitor error rates / rollback triggers for a defined window
       ↓
   ISSUE FOUND ────────→ ABORT (existing 20.5/§104) — affected tenants revert automatically
       ↓ CLEAN
SUPERADMIN promotes to EARLY, then GENERAL — same monitor/abort gate at each step
```

This is the same rollout-group machinery already in the plan (Phase 20, tasks 20.7–20.8, §102–104) — Section 38.2 just makes explicit that **nothing skips the INTERNAL_TEST sandbox first**, and that promotion between every stage is a SUPERADMIN action with a monitoring window, not an automatic timer.

**New tasks:**

| # | Task | Phase |
|---|---|---|
| 20.9 | Company Training Environment: session-level `TRAINING`/`PRODUCTION` mode flag, enforced at service layer on every write | Phase 20 |
| 20.10 | Formal update pipeline: auto-enroll in INTERNAL_TEST → automated tests → QA sign-off → CANARY → EARLY → GENERAL, each promotion is an explicit SUPERADMIN action | Phase 20 |
| 22B.10 | SUPERADMIN release-sandbox dashboard — current stage of each pending update, test results, promote/abort controls | Phase 22B |

---

## SECTION 39: VENDOR-LESS DRIVERS — "OTHERS" GROUP FOR BILLING

### 39.1 The Problem

Phase 11's billing/reconciliation logic (rate cards, invoice reconciliation, vendor performance) is built around `Vendor` as the billing unit. A company can have **directly-employed (in-house) drivers** who aren't linked to any third-party vendor — without a group to sit in, they either break the vendor-centric billing path or need special-case `IF vendorId IS NULL` branches scattered through Phase 11, which is exactly the kind of scattered special-casing this plan has avoided elsewhere (Section 31, 37).

### 39.2 Design — A Real Vendor Row, Not a Null Case

Rather than making `vendorId` nullable and special-casing it, **every company gets a system-seeded placeholder vendor** at company setup:

```
Vendor {
  name: "In-House / Unassigned Drivers"
  vendorType: INTERNAL          // new enum value alongside existing vendor types
  isSystemManaged: true         // cannot be deleted, cannot be renamed by the company
  companyId: <the company>
}
```

- A driver not linked to a real third-party vendor gets `vendorId = <the company's INTERNAL vendor>` by default — never left null.
- Billing, rate cards, trip cost calculation, and reconciliation (Phase 11) all run through the **exact same code path** as any other vendor — the INTERNAL vendor just has no external invoice to reconcile against (or reconciles against the company's own payroll/direct-payment record instead of a submitted vendor invoice).
- If a company later signs a real vendor contract for some of these drivers, they're simply **reassigned** to the real `Vendor` row — same `DriverVehicleAssignment`-style reassignment pattern as the employee-TL reassignment in Section 35.3, with history preserved.
- Transport Admins scoped to a site/process (per Section 31/32) see the INTERNAL vendor's drivers within their own scope only, same as any real vendor's drivers — no special visibility rule needed.

### 39.3 New Tasks

| # | Task | Phase |
|---|---|---|
| 11.16 | `vendorType: INTERNAL` enum value + auto-seed one system-managed INTERNAL vendor per company at setup | Phase 11 |
| 11.17 | Driver-to-vendor reassignment flow (INTERNAL ↔ real vendor), history preserved | Phase 11 |
| 11.18 | INTERNAL vendor billing reconciles against direct/payroll records instead of a submitted vendor invoice | Phase 11 |

---

## SECTION 40: SUPERADMIN SAAS PRICING RULES ENGINE + MULTI-CURRENCY + PRODUCTION SCALE

Covers *what Move-In-Sync charges each company* (SaaS billing, Rule 17 — never mixed with transport billing, which is company-pays-vendor). Same override pattern used throughout this plan (Section 31's `PolicyScope`, Section 37's document types): pricing is **configuration**, resolved with a most-specific-wins order, not hardcoded per company.

### 40.1 Pricing Models Supported

| Model | How it works | Typical use |
|---|---|---|
| `PER_EMPLOYEE` | Flat rate × active employee headcount, billed per cycle | Predictable cost for the company, simplest for Move-In-Sync to forecast revenue on |
| `PER_KM` | Rate × total kilometers traveled across all trips in the period | Aligns Move-In-Sync's revenue with actual platform usage/value delivered |
| `PER_TRIP` | Flat rate × number of completed trips | Cab-aggregator-style — simple, but doesn't account for trip length |
| `PER_EMPLOYEE_PER_TRIP` | Flat rate (e.g. ₹5) × (employee, trip) — charged every time an employee takes a trip | Your literal example — charges scale with actual usage per person, not just headcount |
| `COMMISSION_PERCENT` | Percentage of the company's total transport spend (the vendor billing amount) | Marketplace-style — Move-In-Sync's revenue grows with the company's transport spend |
| `HYBRID` | Base subscription fee + one of the above as metered overage beyond an included allowance | Most enterprise SaaS pricing actually looks like this — predictable floor, usage-based ceiling |

### 40.2 Data Model — Same Override Pattern as PolicyScope

```prisma
model BillingPricingRule {
  id            String   @id @default(uuid())
  companyId     String?         // null = platform/plan-level default
  planId        String?         // null = company-specific override
  pricingModel  PricingModel    // PER_EMPLOYEE | PER_KM | PER_TRIP | PER_EMPLOYEE_PER_TRIP | COMMISSION_PERCENT | HYBRID
  currency      String          // ISO 4217 — "INR", "USD", "AED"...
  rateValue     Decimal         // stored as fixed-point, never float
  tierConfig    Json?           // volume tiers, e.g. [{upTo: 10000, rate: 5}, {upTo: null, rate: 4}]
  includedAllowance Json?       // for HYBRID: what's covered by the base fee before metering kicks in
  billingCycle  BillingCycle    // MONTHLY | QUARTERLY | ANNUAL
  effectiveFrom DateTime
  effectiveTo   DateTime?
  setByUserId   String          // SUPERADMIN or Finance Team
  @@unique([companyId, planId, pricingModel, effectiveFrom])
}
```

**Resolution**: company-specific rule (negotiated enterprise deal) → else the company's plan's default rule → else platform default. Identical shape to Section 31's resolver, different table.

### 40.3 Multi-Currency

- Every company has a **billing currency** set at signup (their local currency — INR, USD, AED, etc.). Invoices are always generated and legally issued in that currency — never converted after the fact.
- **Store money as integers in minor units** (paise/cents), never as float — this is a production-correctness requirement, not a nice-to-have; floating-point rounding errors compound across thousands of invoices.
- Platform-level consolidated revenue reporting (for internal use, e.g. SUPERADMIN's dashboard) converts to one **base reporting currency** using a **period-end FX snapshot rate**, stored alongside the report — never a live rate, so a report generated today and re-run next month shows the same historical number.
- Tax rules (GST for India, VAT elsewhere, sales tax in parts of the US) follow the **same configurable pattern as Section 37's document types** — a `TaxRuleDefinition` scoped by `countryCode`, not a hardcoded GST-only implementation. §84 already flags GST needs legal review before production; the same applies per-country as you expand.

### 40.4 Usage Aggregation → Invoice Pipeline

```
Nightly/period-end job (per company, run in parallel — see 40.5)
       ↓
Aggregate: active employees, total trips, total km, transport spend
       ↓
Resolve BillingPricingRule for this company (most-specific-wins)
       ↓
Apply tiering/allowance if HYBRID
       ↓
Generate invoice line items, freeze the rate + usage numbers used
       ↓
Invoice becomes IMMUTABLE (same rule as trip cost history, Rule 9/10)
       ↓
Payment collection → webhook → subscription state update
```

### 40.5 Production-Scale Considerations — This Is the Part That Actually Breaks at Volume

| Concern | Why it matters at scale | Approach |
|---|---|---|
| **Idempotent billing runs** | A retried or re-triggered billing job must never double-charge a company | Idempotency key = `(companyId, billingPeriod)`; job checks for an existing invoice before creating one |
| **Invoice immutability** | Once generated, usage/rate numbers must never silently change | Same pattern as historical trip pricing — invoice snapshots its inputs, doesn't re-query live data |
| **Parallel, sharded aggregation** | Aggregating usage for thousands of companies sequentially won't finish in a nightly window | Queue one job per company (BullMQ, already in stack), fan out across workers, not one giant cross-company query |
| **Table partitioning** | GPS pings and trip records are the highest-volume tables by far | Partition by date (e.g. monthly partitions) so a billing-period aggregation query only scans relevant partitions, not the whole table |
| **Read replicas for aggregation** | Billing queries shouldn't compete with live dispatch/booking write traffic | Aggregation reads from a replica, not the primary |
| **FX rate snapshotting** | Live FX rates would make historical reports non-reproducible | Snapshot rate stored with each cross-currency report, sourced from a rate provider on a fixed schedule (e.g. daily), not per-request |
| **Dunning at scale** | Thousands of companies means payment failures are a constant background stream, not an edge case | Automated retry schedule + state machine (ACTIVE→PAST_DUE→GRACE_PERIOD→SUSPENDED, already in Phase 17) rather than manual follow-up |
| **Billing anomaly detection** | A metering bug (e.g. GPS duplicate events double-counting km) silently inflates every affected company's invoice | Reuse Phase F's billing-forensics pattern (already designed for vendor invoices) on the platform's own usage metering — flag period-over-period usage spikes above a threshold for Finance Team review before the invoice goes out |

### 40.6 New Tasks

| # | Task | Phase |
|---|---|---|
| 17.11 | `BillingPricingRule` model + resolver (company → plan → platform default) | Phase 17 |
| 17.12 | Multi-currency: minor-unit storage, per-company billing currency, FX snapshot for consolidated reporting | Phase 17 |
| 17.13 | `TaxRuleDefinition` — country-configurable tax rules (GST/VAT/sales tax), same pattern as Section 37 | Phase 17 |
| 17.14 | Sharded, idempotent nightly usage-aggregation → invoice generation pipeline | Phase 17 |
| 17.15 | Platform-side billing anomaly detection (usage spike flagging before invoice issuance) | Phase 17 + Phase F |
| 22B.11 | SUPERADMIN pricing-rule admin UI — set/override per company or plan, view resolved rate | Phase 22B |


---

# PHASE 22C: ENTERPRISE DASHBOARDS + REPORTING PLATFORM — NEW

**Status**: REQUIRED  
**Goal**: Build a complete, role-aware enterprise analytics and reporting platform for MOVE-IN-SYNC. Dashboards must be useful for real operational decisions, not decorative charts. Every dashboard, metric, drill-down, report, export, schedule, and data delivery operation must enforce the same server-side tenant, role, permission, scope, hierarchy, and data-masking rules as the rest of the platform.

## 22C.0 Core principles

1. **Role determines capabilities; scope determines data visibility.**
2. **Permission determines whether an action is allowed.**
3. **Reporting hierarchy never grants unrestricted company access.**
4. **A dashboard must never expose data that the underlying user could not retrieve directly through an authorized API.**
5. **Frontend hiding is not authorization. Backend enforcement is mandatory.**
6. **Client-supplied company/site/LOB/process/shift/employee IDs are never trusted for authorization.**
7. **Every report query must resolve the authenticated tenant/platform context server-side.**
8. **Internal platform roles and external tenant roles remain separate security domains.**
9. **SUPERADMIN cross-tenant access is explicit platform authority and is still fully audited.**
10. **Finance data remains separated from transport operational data according to existing domain rules.**
11. **GPS and sensitive PII must be masked or precision-reduced where the role does not require exact information.**
12. **Exports are security-sensitive actions and must be audited.**
13. **Large exports must run asynchronously through a job queue.**
14. **Reports must be reproducible where financial, compliance, legal, or executive decisions depend on them.**
15. **Historical reports must not change because a later rate card, policy, hierarchy, or configuration changed.**
16. **Dashboard metrics require documented definitions and calculation lineage.**
17. **Every dashboard must support useful drill-down into the authorized underlying records.**
18. **AI summaries may explain data but may not fabricate metrics or bypass authorization.**

---

# 22C.1 Dashboard architecture

Implement a reusable dashboard framework rather than hard-coding separate chart pages.

### Core objects

```text
Dashboard
DashboardLayout
DashboardWidget
WidgetDefinition
WidgetPermission
DashboardRoleAssignment
DashboardUserPreference
SavedFilter
SavedView
DashboardSnapshot
DashboardMetric
MetricDefinition
MetricVersion
MetricDataSource
MetricLineage
DashboardAlert
DashboardTarget
DashboardBenchmark
```

### Widget types

Support at minimum:

- KPI card
- KPI comparison
- Trend line
- Bar chart
- Stacked bar
- Area chart
- Pie/donut
- Table
- Pivot table
- Heatmap
- Funnel
- Gauge
- Progress indicator
- Map
- Live map
- Timeline
- SLA timer
- Alert queue
- Leaderboard
- Sankey/flow where useful
- Calendar
- Status distribution
- Geographic density
- Exception list
- AI insight card

### Widget actions

Every applicable widget should support:

```text
View
View details
Filter
Drill down
Open record
Export data
Save view
Pin
Refresh
Compare
```

Do not expose actions that the current user is not authorized to perform.

---

# 22C.2 Universal dashboard filters

All applicable dashboards should use a common filter framework.

### Filters

```text
Date
Date range
Company
Region
Site
LOB
Process
Shift
Department
Business Unit
Cost Centre
Office
Vendor
Driver
Vehicle
Guard
Employee
Manager
Trip type
Transport mode
Booking status
Trip status
Compliance status
Invoice status
Subscription status
Severity
Incident type
```

### Filter behavior

- Filters must cascade.
- Only authorized values appear.
- Selecting a site must restrict LOB/process/shift options.
- Selecting a manager must restrict employees to the manager's authorized reportees.
- Filters must never expand authorization.
- Saved filters must be revalidated every time they are used.
- A previously saved filter must not become a privilege escalation path after role/scope changes.

---

# 22C.3 Dashboard personalization

Authorized users can:

- Drag widgets
- Resize widgets
- Hide widgets
- Show widgets
- Reorder widgets
- Pin widgets
- Choose default dashboard
- Save multiple dashboard layouts
- Save filters
- Save date ranges
- Save table columns
- Save sorting
- Save grouping
- Save chart configuration

### Restrictions

Personalization changes presentation only. It must never:

- grant permission
- expand scope
- reveal hidden columns
- bypass masking
- bypass hierarchy
- bypass tenant isolation

---

# 22C.4 SUPERADMIN — PLATFORM COMMAND CENTER

SUPERADMIN receives the complete platform-wide command center.

## Executive platform KPIs

```text
Total Companies
Active Companies
Trial Companies
Companies in Grace Period
Past Due Companies
Suspended Companies
Cancelled Companies
Total Employees
Transport-Eligible Employees
Active Drivers
Active Vehicles
Active Vendors
Trips Today
Active Trips
Completed Trips
Cancelled Trips
No-Shows
Breakdowns
SOS Incidents
Platform MRR
Platform ARR
Platform Revenue
Open Critical Issues
System Health
```

## Company health

For every company:

```text
Company
Plan
Subscription Status
Employees
Sites
Drivers
Vehicles
Trips
Transport Spend
Utilization
SLA
Safety Score
Compliance Score
No-show Rate
Cancellation Rate
Open Issues
Integration Health
Last Activity
```

## SaaS health

```text
MRR
ARR
New Customers
New Trials
Trial Conversion
Renewals
Upgrades
Downgrades
Churn
Expansion Revenue
Contraction Revenue
Past Due
Failed Payments
Refunds
Credits
Revenue by Plan
Revenue by Country
Revenue by Currency
```

## Platform operations

```text
Trips Today
Active Trips
Unassigned Trips
Dispatch Failures
Driver Declines
No-Shows
Breakdowns
Replacement Events
SOS
Route Deviations
Delayed Trips
Notification Failures
GPS Stale Devices
API Failures
Queue Backlog
```

## Platform health

```text
API Availability
p50
p95
p99
Error Rate
Database Health
Redis Health
Queue Health
WebSocket Connections
GPS Events/sec
ML Service Health
Notification Delivery
Storage
Search
Background Jobs
```

## Security

```text
Failed Logins
MFA Failures
Locked Accounts
Suspicious Sessions
New Devices
Privilege Changes
Role Changes
Permission Changes
Cross-Tenant Attempts
Scope Violations
Break-Glass Sessions
Support Impersonation
API Key Anomalies
Security Incidents
```

## Release control

```text
Current Version
Previous Known-Good Version
INTERNAL_TEST
CANARY
EARLY
GENERAL
Failed Deployments
Feature Flags
Rollback Available
Active Incidents
```

## SUPERADMIN attention queue

Prioritize:

```text
CRITICAL
HIGH
MEDIUM
LOW
INFO
```

Each alert contains:

```text
Company
Domain
Problem
Severity
Detected At
Owner
SLA
Impact
Recommended Action
Current Status
```

---

# 22C.5 MOVE-IN-ADMIN — PLATFORM OPERATIONS DASHBOARD

MOVE-IN-ADMIN must have a distinct operational dashboard and must not simply inherit an undifferentiated SUPERADMIN view.

### Main widgets

```text
Companies Requiring Attention
Onboarding Companies
Failed Imports
Go-Live Readiness
Configuration Issues
Integration Failures
Notification Failures
Support Issues
Compliance Alerts
Companies Near Usage Limits
Subscription Issues
Recent Incidents
Recent Administrative Changes
Pending Tasks
```

### Company onboarding health

```text
Company
Onboarding %
Admin
Sites
LOBs
Processes
Shifts
Employees
Hierarchy
Locations
Roles
Policies
Drivers
Vehicles
Integrations
Billing
Notifications
GPS
Test Booking
Go-Live Status
```

---

# 22C.6 Project Manager dashboard

Only assigned companies are visible.

### Portfolio KPIs

```text
Assigned Companies
Companies Live
Companies Onboarding
At-Risk Companies
Transport Cost
Cost/Trip
Cost/KM
Utilization
Trip Volume
SLA
Safety Score
Compliance
No-Show
Cancellation
Employee Satisfaction
Vendor Performance
Open Issues
```

### Company comparison

Allow comparison of authorized companies by:

```text
Cost/KM
Cost/Trip
Utilization
No-Show
Cancellation
Breakdown
SLA
Safety
Compliance
Vendor Score
Employee Satisfaction
Carbon
```

---

# 22C.7 Project Coordinator dashboard

### Onboarding command center

```text
Companies Onboarding
Imports Running
Imports Failed
Imports Awaiting Review
Sites Configured
LOBs Configured
Processes Configured
Shifts Configured
Employees Imported
Hierarchy Mapped
Locations Mapped
Roles Configured
Policies Configured
Integrations Configured
Test Bookings
Go-Live Ready
```

### Import health

```text
Total Rows
Successful
Failed
Duplicates
Invalid Email
Invalid Mobile
Missing Manager
Invalid Coordinates
Missing Site
Missing LOB
Missing Process
Missing Shift
Missing Required Fields
```

### Go-live readiness

Show percentage plus blocking conditions:

```text
87% READY

✓ Company
✓ Admin
✓ Sites
✓ LOB
✓ Processes
✓ Shifts
✓ Employees
✓ Roles

✗ Driver Compliance
✗ Notification Provider
✗ GPS Integration
✗ Billing Configuration
```

---

# 22C.8 Platform Finance dashboard

Finance remains restricted to the platform billing domain.

### Revenue

```text
MRR
ARR
Gross Revenue
Net Revenue
Revenue Growth
Revenue by Plan
Revenue by Company
Revenue by Country
Revenue by Currency
GST
CGST
SGST
IGST
Taxable Amount
```

### Collections

```text
Paid
Pending
Failed
Past Due
Grace Period
Suspended
Dunning
Days Outstanding
```

### Subscription analytics

```text
New Subscriptions
Renewals
Upgrades
Downgrades
Cancellations
Churn
Trial Conversion
Expansion
Contraction
```

### Reconciliation

```text
Invoices
Payments
Gateway Settlements
Unmatched Payments
Webhook Failures
Refunds
Credits
Disputes
```

Finance must not receive transport operational data merely because it appears in another report.

---

# 22C.9 Platform Compliance dashboard

### Global compliance

```text
Compliant Companies
At-Risk Companies
Critical Companies
Compliant Drivers
Non-Compliant Drivers
Compliant Vehicles
Non-Compliant Vehicles
Compliant Vendors
Non-Compliant Vendors
Expiring Documents
Expired Documents
Dispatch-Blocked Assets
```

### Expiry radar

```text
60 Days
30 Days
15 Days
7 Days
1 Day
Expired
```

### Company compliance score

Display:

```text
Overall Score
Driver Score
Vehicle Score
Vendor Score
Insurance
License
PUC
Permit
Fitness
Training
Inspection
```

---

# 22C.10 Security Administrator dashboard

### Security Command Center

```text
Threat Level
Failed Logins
MFA Failures
Account Lockouts
Suspicious Devices
Suspicious Sessions
Privilege Changes
Permission Changes
Role Changes
Break-Glass Requests
Impersonation Sessions
Cross-Tenant Attempts
Scope Violations
Rate-Limit Events
API Key Anomalies
Security Incidents
```

### Access audit

Each event:

```text
WHO
ROLE
WHAT
WHEN
TENANT
RESOURCE
BEFORE
AFTER
IP
DEVICE
AUTH METHOD
REASON
APPROVAL
RESULT
```

---

# 22C.11 Support Engineer dashboard

### Support KPIs

```text
Open Tickets
Critical Tickets
High Tickets
SLA Breached
SLA Approaching
First Response Time
Average Resolution Time
Reopened Tickets
Escalated Tickets
Tickets by Category
Tickets by Company
```

### Categories

```text
Technical
Billing
Account
Onboarding
API
Integration
Security
Subscription
Feature Request
```

Transport complaints remain in the tenant transport support domain and are not automatically merged into platform support.

---

# 22C.12 Platform Auditor dashboard

Read-only.

### Audit Command Center

```text
Administrative Actions
Permission Changes
Role Changes
Policy Changes
Financial Changes
Subscription Changes
Break-Glass
Impersonation
Exports
Deletes
Security Events
Authorization Failures
Cross-Tenant Attempts
```

### Audit Explorer

Filters:

```text
Company
User
Role
Action
Resource
Date
IP
Device
Severity
Success
Failure
```

No write actions are available.

---

# 22C.13 Transport Admin dashboard

### Today's operations

```text
Bookings
Approved
Pending Approval
Dispatched
Active
Completed
Cancelled
No-Show
Breakdown
Replacement
SOS
```

### Fleet

```text
Available
Assigned
On Trip
Maintenance
Blocked
Compliance Expired
Charging
```

### Cost

```text
Today's Cost
MTD Cost
Cost/KM
Cost/Trip
Vendor Cost
Empty KM
Overtime
Tolls
Parking
Guard Cost
Emergency Cost
```

### Performance

```text
On-Time Pickup
On-Time Drop
Dispatch SLA
Driver Acceptance
Driver Decline
No-Show
Cancellation
Breakdown
Customer Satisfaction
```

---

# 22C.14 Transport Coordinator dashboard

Operational-first dashboard:

```text
Live Trips
Pending Dispatch
Unassigned Trips
Driver Declines
Replacement Required
No-Show Queue
Passenger Issues
Documents Awaiting Review
Upcoming Shifts
Capacity Warnings
Delayed Trips
GPS Stale
```

Include a live map and dispatch board.

---

# 22C.15 Transport Compliance dashboard

```text
Driver Compliance
Vehicle Compliance
Vendor Compliance
Expiring Documents
Expired Documents
Dispatch Blocked
Inspection Failures
Training Exceptions
Compliance Tasks
Audit History
```

Mandatory document failures remain hard blocks according to the existing compliance rules.

---

# 22C.16 Director dashboard

Executive view only.

```text
Transport Spend
Cost/Employee
Cost/Trip
Cost/KM
Utilization
Service Level
Safety Score
Employee Satisfaction
Vendor Performance
No-Show
Cancellation
Carbon Emissions
EV Adoption
```

### Site comparison

Show only authorized sites/divisions.

---

# 22C.17 Senior Manager dashboard

Aggregate only authorized teams/managers.

```text
Employees
Transport Usage
Bookings
Approvals
Pending Approvals
No-Shows
Expenses
Cost
Utilization
Issues
```

Include manager/team comparison within authorized scope.

---

# 22C.18 Manager dashboard

The Manager dashboard must support the manager's existing authority to book, cancel, and reschedule for authorized reportees.

### KPIs

```text
My Employees
Today's Trips
Upcoming Trips
Pending Approvals
No-Shows
Expenses
Eligibility Requests
Transport Issues
```

### Team table

```text
Employee
Shift
Pickup
Drop
Transport Status
Booking
Driver
Vehicle
ETA
Trip Status
```

### Authorized actions

```text
Book for Employee
Cancel
Reschedule
Approve
Reject
Review Expense
Review No-Show Appeal
```

Server determines the actual reportee/approver relationship.

---

# 22C.19 Assistant Manager dashboard

Only delegated scope:

```text
Team Members
Today's Trips
Pending Approvals
Transport Issues
No-Shows
Expenses
Upcoming Shifts
```

Approval authority remains policy-gated.

---

# 22C.20 Team Leader dashboard

Simple team-focused dashboard:

```text
My Team
Today's Transport
Upcoming Transport
Pickup ETA
Trip Status
Issues
Pending Approvals
```

Only direct/authorized team scope is visible.

---

# 22C.21 Employee dashboard

Employee self-service:

```text
Next Trip
Driver
Vehicle
ETA
Pickup
Drop
Booking
Trip History
Expenses
No-Show Appeals
Notifications
Feedback
```

Include:

**Where is my cab?**

with live tracking where permitted.

---

# 22C.22 Control Room dashboard

This is a dedicated large-screen real-time operational interface.

## Live map

Each active vehicle can display:

```text
Vehicle
Driver
Trip
Passengers
Current Location
Speed where authorized
ETA
Route
Trip Status
GPS Last Seen
```

## Alert queue

```text
SOS
Breakdown
Route Deviation
Overspeed
Delayed Pickup
Capacity Issue
Driver Decline
GPS Stale
Geofence Violation
```

## Dispatch board

```text
Unassigned
Assigned
Accepted
Driver En Route
Arrived
Boarding
Started
In Transit
Dropping
Completed
```

---

# 22C.23 Vendor dashboard

```text
Assigned Trips
Completed Trips
Rejected Trips
Driver Acceptance
On-Time %
Cancellation
Breakdown
Vehicle Utilization
Compliance
Invoice Value
Invoice Discrepancies
SLA Penalties
Performance Score
```

Vendor sees only its own authorized company/vendor scope.

---

# 22C.24 Driver dashboard

For driver web/mobile where applicable:

```text
Today's Trips
Upcoming Trips
Earnings
Hours Worked
Breaks
Acceptance Rate
Declines
Rating
Compliance
Vehicle
Documents
Preferred Areas
Performance
```

---

# 22C.25 Fleet dashboard

Dedicated fleet view:

```text
Total Vehicles
Available
Assigned
On Trip
Maintenance
Blocked
Charging
Insurance Expiring
PUC Expiring
Permit Expiring
Fitness Expiring
Inspection Failures
Utilization
Idle Time
Mileage
Fuel/EV Cost
```

---

# 22C.26 Safety dashboard

```text
SOS
Incidents
Breakdowns
Route Deviations
Overspeed
Geofence Violations
Driver Safety Score
Vehicle Safety Score
Near-Miss Events
Replacement Events
Passenger Transfer Events
Safety SLA
Open Investigations
```

Include incident trend and root-cause analysis.

---

# 22C.27 Maintenance dashboard

```text
Vehicles Due for Service
Overdue Service
Open Work Orders
Breakdowns
Repeat Breakdowns
Maintenance Cost
Cost/Vehicle
Downtime
Parts Cost
Labor Cost
Vehicle Availability
```

---

# 22C.28 Dashboard targets and benchmarking

Every company may configure authorized KPI targets.

Examples:

```text
Target Cost/KM
Target Cost/Trip
Target Utilization
Target No-Show
Target Cancellation
Target Pickup SLA
Target Drop SLA
Target Safety Score
Target Vendor SLA
Target Employee Satisfaction
Target Compliance
```

Display:

```text
Actual
Target
Variance
Trend
Status
```

Example:

```text
Cost/KM     ₹32     ₹28     +14.3%  CRITICAL
Utilization 82%     85%     -3.0%   WARNING
No-Show     3.2%    <5%     PASS
Safety      94%     95%     -1.0%   WARNING
```

Benchmarking must never expose another company's confidential information unless an explicit platform-approved anonymized benchmarking feature exists.

---

# 22C.29 "What needs my attention?" intelligence

Every role dashboard should have an actionable queue.

Examples:

```text
3 Critical
11 High
24 Medium
48 Informational
```

Examples:

```text
Driver license expires tomorrow
12 trips are unassigned
Vendor invoice has 7 discrepancies
Pune empty KM increased 18%
5 employees have unresolved no-show appeals
Subscription payment failed
14 vehicles are maintenance overdue
```

Sort by:

```text
Severity
Business Impact
SLA
Age
Risk
```

---

# 22C.30 Anomaly cards

Analytics should detect unusual conditions.

Examples:

```text
Transport cost increased 23%
Empty KM increased 31%
Vendor rejection rate doubled
No-show rate increased 18%
Vehicle utilization dropped below target
Breakdown frequency increased
Invoice amount differs from expected by 12%
GPS event volume dropped
Notification delivery rate dropped
```

AI/ML may provide an explanation only when grounded in actual data.

---

# 22C.31 Universal report engine

Do not implement reports as isolated pages.

Create a reusable report engine.

### Report definition

```text
Report
ReportVersion
ReportColumn
ReportFilter
ReportSort
ReportGroup
ReportCalculation
ReportPermission
ReportSchedule
ReportDelivery
ReportExecution
ReportExport
ReportSnapshot
ReportAccessLog
```

### Every report supports

```text
View
Filter
Sort
Group
Aggregate
Drill Down
Save
Clone
Export
Schedule
Share
```

---

# 22C.32 Report Builder

Authorized users can build custom reports.

Workflow:

```text
Select Data Source
↓
Select Columns
↓
Add Filters
↓
Add Groups
↓
Add Sorting
↓
Add Calculated Fields
↓
Choose Visualization
↓
Preview
↓
Save
↓
Set Permissions
↓
Schedule if required
```

### Calculated fields

Examples:

```text
Cost/KM
Cost/Trip
Utilization %
No-Show %
Cancellation %
SLA %
Acceptance %
Breakdown Rate
Cost/Employee
```

Only approved calculation functions may be used. Prevent arbitrary SQL from the UI.

---

# 22C.33 Report catalog

Initial target: **60–100 enterprise reports**, with at least the following baseline set.

## Transport

1. Daily Trip Report
2. Monthly Trip Report
3. Booking Report
4. Cancellation Report
5. No-Show Report
6. Dispatch Report
7. Driver Acceptance Report
8. Driver Decline Report
9. Vehicle Utilization
10. Fleet Utilization
11. Route Performance
12. Route Deviation
13. ETA Performance
14. Pickup SLA
15. Drop SLA
16. Empty KM
17. Passenger Utilization
18. Seat Utilization
19. Shift Utilization
20. Site Utilization

## Safety

21. SOS Report
22. Incident Report
23. Breakdown Report
24. Replacement Report
25. Safety Score Report
26. Overspeed Report
27. Geofence Violation
28. Route Deviation Investigation
29. Driver Safety Report
30. Vehicle Safety Report

## Compliance

31. Driver Compliance
32. Vehicle Compliance
33. Vendor Compliance
34. Document Expiry
35. Inspection Report
36. Training Report
37. Dispatch Block Report

## Finance

38. Transport Cost
39. Cost/KM
40. Cost/Trip
41. Budget vs Actual
42. Department Cost
43. Business Unit Cost
44. Cost Centre Cost
45. Vendor Cost
46. Rate Card Usage
47. Invoice Reconciliation
48. Invoice Exceptions
49. GST Report
50. Employee Expense Report

## Employee

51. Transport Eligibility
52. Employee Utilization
53. Employee No-Show
54. Employee Cancellation
55. Employee Expenses
56. Employee Satisfaction
57. Transport Adoption

## Vendor

58. Vendor Performance
59. Vendor SLA
60. Vendor Penalty
61. Vendor Invoice
62. Vendor Reconciliation
63. Vendor Driver Performance

## Platform

64. Company Health
65. SaaS Revenue
66. Subscription Status
67. Churn
68. Usage
69. API Usage
70. Platform Compliance
71. Security Audit
72. Support SLA
73. Integration Health
74. Feature Adoption

Add more reports without changing the underlying report framework.

---

# 22C.34 Excel export

Support:

**`.xlsx`**

Requirements:

- Multiple worksheets
- Header formatting
- Freeze panes
- Auto filters
- Column widths
- Number/date formatting
- Currency formatting
- Totals
- Subtotals
- Formula fields where appropriate
- Charts where appropriate
- Metadata sheet
- Applied-filter sheet
- Generated-at timestamp
- Report version
- Company name
- Confidentiality classification

For large datasets use asynchronous generation.

---

# 22C.35 PDF export

Support professional PDF reports:

```text
Company Logo
Company Name
Report Name
Date Range
Generated At
Prepared For
Filters
Executive Summary
Charts
Tables
Totals
Page Numbers
Confidentiality Notice
Report Version
```

PDF branding follows the authorized tenant's white-label configuration.

---

# 22C.36 Additional export formats

Support:

```text
XLSX
PDF
CSV
JSON
Print
```

Optional enterprise delivery:

```text
SFTP
Webhook
API
Secure Download Link
```

---

# 22C.37 Asynchronous export engine

Never hold an HTTP request open for large exports.

Workflow:

```text
User
↓
Authorization
↓
Create ExportJob
↓
BullMQ Queue
↓
Report Query
↓
Generate File
↓
Encrypt/Store
↓
Checksum
↓
Mark Complete
↓
Notify User
↓
Secure Download
```

### ExportJob states

```text
QUEUED
RUNNING
COMPLETED
FAILED
CANCELLED
EXPIRED
```

### Retry

Use controlled retries and dead-letter handling.

---

# 22C.38 Export security

Before every export:

```text
Authenticate
↓
Resolve platform/tenant context
↓
Resolve role
↓
Resolve permissions
↓
Resolve access scope
↓
Resolve reporting hierarchy
↓
Apply data masking
↓
Validate filters
↓
Execute authorized query
↓
Generate export
```

Never allow:

```text
Frontend → companyId → export arbitrary company
```

Never trust client-supplied:

```text
companyId
siteId
lobId
processId
shiftId
employeeId
managerId
role
approvalLevel
```

---

# 22C.39 Export audit logging

Every export must create an immutable audit record:

```text
Export ID
User
Role
Company
Report
Report Version
Filters
Scope
Rows
Format
File Size
Created At
Completed At
Downloaded At
IP
Device
Result
```

Also log:

- export cancelled
- export failed
- export expired
- export downloaded
- export permission denied

---

# 22C.40 Export history

Users with permission can view their own authorized export history.

Platform/Security/Auditor roles may view broader export history according to their permissions.

Example:

```text
Report
Format
Requested
Status
Rows
Created
Expires
Downloaded
```

---

# 22C.41 Secure download

Generated files must not become permanent public URLs.

Use:

- short-lived signed URLs
- authorization check before download
- expiry
- revocation
- encrypted storage
- access logging

Default retention:

```text
24 hours for ordinary exports
7 days for explicitly retained reports
```

Make retention configurable by policy.

---

# 22C.42 Data masking in reports

Support field-level report masking.

Examples:

```text
Mobile → 98******21
Email → s******@company.com
Exact Home Address → hidden
Exact GPS → rounded/hidden
Emergency Contact → hidden
Bank/financial information → hidden
Government ID → masked
```

Masking depends on role, permission, purpose and policy.

---

# 22C.43 GPS privacy

Different roles may see different precision.

Example:

```text
Employee:
Own trip exact location

Manager:
Authorized reportee trip operational location

Transport Control Room:
Exact live operational GPS

Director:
Aggregated operational location

Auditor:
Only where required for audit

Finance:
No GPS
```

GPS must never leak into financial reports accidentally.

---

# 22C.44 Report scheduling

Users with the required permission can schedule reports.

### Frequencies

```text
Daily
Weekly
Monthly
Quarterly
Custom
```

### Delivery

```text
Email
In-App
Secure Download
SFTP
Webhook
API
```

### Schedule definition

```text
Report
Filters
Recipients
Format
Timezone
Frequency
Start
End
Retention
Password/encryption policy
```

---

# 22C.45 Scheduled report security

When a schedule runs:

1. Re-authenticate the schedule owner context.
2. Re-evaluate current role.
3. Re-evaluate current scope.
4. Re-evaluate current company membership.
5. Re-evaluate current report permission.
6. Re-evaluate recipient authorization.
7. Generate only currently authorized data.

A user who lost access after creating a schedule must not continue receiving data.

---

# 22C.46 Saved reports and saved views

Allow users to save:

```text
Report Definition
Filters
Columns
Sorting
Grouping
Chart
Date Range
Dashboard Placement
```

Saved reports must be versioned.

---

# 22C.47 Dashboard drill-down

Every major KPI should lead to the authorized underlying data.

Example:

```text
No-Show Rate = 4.8%
↓
127 No-Shows
↓
No-Show Report
↓
Trip
↓
Employee
↓
Driver
↓
GPS Evidence
↓
Call Evidence
↓
Appeal
```

The drill-down must inherit authorization at every step.

---

# 22C.48 Dashboard → report

Every applicable widget should expose:

```text
View Details
Open Report
Export XLSX
Export PDF
```

Example:

```text
No-Show Rate: 4.8%

[View 127 Trips]
[Export XLSX]
[Export PDF]
```

---

# 22C.49 Data dictionary

Every metric must have a documented definition.

Example:

```text
Metric:
Completed Trips

Definition:
Trips whose final state is COMPLETED.

Source:
Trip domain.

Calculation:
COUNT(DISTINCT trip.id)

Dimensions:
Company
Site
LOB
Process
Shift
Date

Refresh:
Near real-time / scheduled

Owner:
Transport Analytics

Version:
1.2
```

Users with appropriate permission can click:

**How is this number calculated?**

---

# 22C.50 Metric lineage

Every important KPI should expose:

```text
Metric
Source Tables
Source Events
Filters
Calculation
Transformations
Aggregation
Refresh Time
Version
Owner
```

This prevents disputes between Transport, Finance, HR and leadership.

---

# 22C.51 Report snapshots

Financial, compliance, legal and executive reports can be frozen.

Snapshot metadata:

```text
Snapshot ID
Report ID
Report Version
Generated At
Data Cutoff
Rate Card Version
Policy Version
Hierarchy Version
Filter Definition
Query Version
Generated By
SHA-256 Hash
```

Frozen reports must remain reproducible.

---

# 22C.52 Report versioning

Changing a report definition creates a new version.

Example:

```text
Transport Cost Report v1
Transport Cost Report v2
Transport Cost Report v3
```

Historical snapshots continue referencing the original version.

---

# 22C.53 Report retention

Configure:

```text
Temporary Export
Saved Report
Scheduled Report
Financial Snapshot
Compliance Snapshot
Audit Export
```

Each may have a different retention period.

Automatic purge must be:

- policy-controlled
- audited
- legally hold aware
- tenant aware

---

# 22C.54 Report permissions

Introduce report-specific permissions such as:

```text
REPORT_VIEW
REPORT_CREATE
REPORT_EDIT
REPORT_DELETE
REPORT_EXPORT
REPORT_SCHEDULE
REPORT_SHARE
REPORT_SNAPSHOT
REPORT_ADMIN
```

Then combine with normal:

```text
Role
Permission
Company
Site
LOB
Process
Shift
Hierarchy
```

---

# 22C.55 Report sharing

A report can be shared only with explicitly authorized recipients.

Support:

```text
Individual User
Role
Team
Site
LOB
Company
Platform Team
```

Never allow:

**"Anyone with link."**

Use authorized secure links only.

---

# 22C.56 Report subscriptions

A user may subscribe to an authorized report.

Example:

```text
Every Monday 08:00 IST
Previous Week Transport Performance
XLSX + PDF
Recipients: Transport Admin + Director
```

The system revalidates authorization every run.

---

# 22C.57 KPI alerts

Allow threshold alerts.

Examples:

```text
No-show > 5%
Cost/KM > ₹35
Utilization < 70%
Compliance < 90%
SLA < 95%
Breakdown rate > threshold
Invoice variance > 5%
```

Channels:

```text
In-App
Push
Email
SMS/WhatsApp where configured
```

Respect notification preferences and security restrictions.

---

# 22C.58 Executive automated summaries

Generate daily/weekly/monthly summaries:

```text
What happened?
What changed?
Why did it change?
What is at risk?
What requires action?
```

AI may summarize only data the requesting role is authorized to access.

No fabricated numbers.

---

# 22C.59 AI dashboard insights

Examples:

```text
"Transport cost increased 18% versus the previous period."

"Most of the increase came from Site A, primarily from higher empty KM."

"Vendor X has the largest variance."

"Confidence: 91%."
```

Every AI insight should link back to the supporting metrics/report.

AI should expose:

```text
Data Used
Time Range
Metric Definitions
Model/Prompt Version
Generated At
```

---

# 22C.60 Report data freshness

Every dashboard/report must show:

```text
Live
Updated 30 seconds ago
Updated 5 minutes ago
Updated 1 hour ago
Updated at 09:31 IST
```

For delayed analytics, clearly show the freshness timestamp.

Never present stale analytics as real-time.

---

# 22C.61 Analytics data architecture

For operational scale, avoid expensive unrestricted queries directly against transactional tables for every dashboard.

Use appropriate:

```text
PostgreSQL
Materialized Views
Aggregated Tables
Read Replicas where needed
Redis Cache
Analytics Tables
Event Streams
Background Aggregation
```

Do not introduce a separate data warehouse unless scale actually requires it.

### Cache rules

Cache keys must include:

```text
Tenant
Scope
Role-relevant dimensions
Filters
Metric Version
Time Range
```

Never share a cached tenant result with another tenant.

---

# 22C.62 Real-time vs analytical data

### Real-time

Use for:

- Control Room
- Active Trips
- GPS
- Dispatch
- SOS
- Breakdown
- Live alerts

### Near-real-time

Use for:

- Utilization
- Trip KPIs
- SLA
- Driver performance
- Fleet

### Batch

Use for:

- Monthly finance
- Historical analytics
- Long-term trends
- Executive reports
- ML features

The UI must identify which category each metric belongs to.

---

# 22C.63 Dashboard observability

Track:

```text
Dashboard load time
Widget load time
Query duration
Cache hit/miss
Error rate
Export duration
Export failures
Report generation duration
Queue depth
Data freshness
```

Slow reports should be identifiable by:

```text
Report
Tenant
Query
Duration
Rows
Date Range
```

---

# 22C.64 Dashboard performance rules

Target:

```text
Initial dashboard load: < 2 seconds where cached
Standard widget query: < 500 ms target
Heavy analytics: asynchronous
Large export: asynchronous
Control Room updates: real-time
```

Use pagination for large tables.

Never load millions of rows into the browser.

---

# 22C.65 Role-scoped dashboard registry

Create a registry such as:

```text
PLATFORM_SUPERADMIN
PLATFORM_MOVE_IN_ADMIN
PLATFORM_FINANCE
PLATFORM_PROJECT_MANAGER
PLATFORM_PROJECT_COORDINATOR
PLATFORM_COMPLIANCE
PLATFORM_SECURITY
PLATFORM_SUPPORT
PLATFORM_AUDITOR

TENANT_TRANSPORT_ADMIN
TENANT_TRANSPORT_COORDINATOR
TENANT_TRANSPORT_COMPLIANCE
TENANT_DIRECTOR
TENANT_SENIOR_MANAGER
TENANT_MANAGER
TENANT_ASSISTANT_MANAGER
TENANT_TEAM_LEADER
TENANT_EMPLOYEE
TENANT_TRAINER
TENANT_VENDOR
TENANT_DRIVER
TENANT_CONTROL_ROOM
TENANT_FLEET
TENANT_SAFETY
TENANT_MAINTENANCE
```

Do not hard-code business authorization decisions to these names. The dashboard registry maps configurable roles/permissions to capabilities.

---

# 22C.66 Company-specific dashboards

Companies may enable/disable dashboards according to their subscription and configuration.

Example:

```text
Executive Dashboard       ON
Control Room              ON
Carbon Dashboard          OFF
AI Copilot                ON
Vendor Analytics          ON
Advanced Benchmarking     OFF
```

Feature flags and entitlements must be enforced server-side.

---

# 22C.67 Dashboard access preview

Admins need:

**Preview as role**

Example:

```text
Preview:
Manager
Site: Pune
LOB: Finance
Process: Night Shift
```

Show:

```text
Dashboards visible
Widgets visible
Reports visible
Fields masked
Actions allowed
Export allowed
```

Preview must never create a real privilege or session.

---

# 22C.68 Effective report access

Provide:

```text
User
↓
Role
↓
Permissions
↓
Company
↓
Site
↓
LOB
↓
Process
↓
Shift
↓
Reporting Hierarchy
↓
Data Masking
↓
Effective Report Access
```

This must use the same authorization architecture as the rest of the platform.

---

# 22C.69 Report export approval for sensitive data

Companies may require approval for certain exports.

Examples:

```text
Large employee PII export
Exact GPS history
Emergency contacts
Audit data
Financial data
Government IDs
```

Workflow:

```text
Request Export
↓
Risk Classification
↓
Approval if required
↓
Generate
↓
Audit
```

---

# 22C.70 Export size and abuse controls

Implement:

- maximum rows
- maximum file size
- per-user export rate limit
- per-tenant export quota
- concurrency limits
- cancellation
- queue priority
- suspicious export detection

Example:

```text
Normal user:
5 large exports/hour

Admin:
higher configurable limit

Platform:
policy-defined
```

Do not hard-code limits that prevent enterprise configuration.

---

# 22C.71 Sensitive report classification

Every report can have:

```text
PUBLIC_INTERNAL
CONFIDENTIAL
RESTRICTED
HIGHLY_RESTRICTED
```

Classification controls:

- export
- sharing
- retention
- download
- email
- masking
- approval

---

# 22C.72 Financial report immutability

Transport billing reports must reference:

```text
Rate Card Version
Rate Card Effective Date
Tax Configuration
Vendor Contract Version
Trip Cost Version
```

Changing a rate card later must not change completed historical invoices or frozen reports.

---

# 22C.73 Multi-currency and localization

Reports should support:

```text
INR
USD
EUR
GBP
AED
Other configured currencies
```

Display:

```text
Original Currency
Reporting Currency
FX Rate
FX Rate Date
```

Dates/times must respect tenant/user timezone.

---

# 22C.74 Multi-language reporting

Where configured:

- dashboard labels
- report titles
- PDF labels
- notifications

Metric definitions should remain unambiguous across languages.

---

# 22C.75 Report APIs

Expose authorized APIs such as:

```text
GET /reports
GET /reports/:id
POST /reports
PATCH /reports/:id
DELETE /reports/:id

POST /reports/:id/run
POST /reports/:id/export
GET /exports/:id
GET /exports/:id/download

POST /reports/:id/schedules
PATCH /report-schedules/:id
DELETE /report-schedules/:id

GET /dashboards
GET /dashboards/:id
PATCH /dashboards/:id/layout
GET /metrics/:id
```

Exact API naming may follow the existing NestJS conventions.

Every endpoint must enforce authorization server-side.

---

# 22C.76 Report database design

At minimum consider:

```text
ReportDefinition
ReportVersion
ReportPermission
ReportSchedule
ReportRecipient
ReportExecution
ReportExport
ReportSnapshot
ReportAccessLog
MetricDefinition
MetricVersion
MetricLineage
Dashboard
DashboardWidget
DashboardLayout
DashboardRoleAssignment
DashboardUserPreference
SavedFilter
SavedView
KpiTarget
KpiBenchmark
DashboardAlert
```

Reuse existing models where they already provide the required behavior. Do not create duplicate concepts unnecessarily.

---

# 22C.77 Dashboard testing

### Authorization tests

```text
Company A cannot export Company B
Manager cannot export unrelated employee
Manager cannot export another manager's team
Finance cannot access GPS
Employee cannot access team report
Vendor cannot access another vendor
Project Manager sees only assigned companies
Project Coordinator sees only assigned companies
Platform Auditor cannot modify reports
```

### Export tests

```text
XLSX generated
PDF generated
CSV generated
Large export queued
Failed export retried
Expired link rejected
Revoked access rejected
Masking applied
Audit record created
```

### Metric tests

Every important metric requires fixture-based tests against known expected values.

### Snapshot tests

Historical reports remain unchanged after:

```text
Rate Card Change
Policy Change
Hierarchy Change
Role Change
Site Change
Employee Change
```

---

# 22C.78 Acceptance tests

### SUPERADMIN

```text
Login
→ See platform command center
→ See all authorized companies
→ View SaaS health
→ View security
→ View compliance
→ Export platform report
→ Audit export
```

### Finance

```text
Login
→ Platform billing dashboard
→ Revenue
→ Invoice
→ GST
→ Export XLSX
→ Export PDF
```

Finance attempting GPS:

```text
403
```

### Manager

```text
Login
→ Manager dashboard
→ Only authorized reportees
→ View team trips
→ Drill into trip
→ Export authorized team report
```

Manager attempting unrelated employee:

```text
403
```

### Transport Admin

```text
Login
→ Transport dashboard
→ Live operations
→ Cost
→ Fleet
→ Safety
→ Reports
→ Export
```

### Employee

```text
Login
→ Employee dashboard
→ Own trip
→ Own history
→ Own expense
```

Employee attempting another employee:

```text
403
```

### Export

```text
Create report
→ Filter
→ Preview
→ Export XLSX
→ Export PDF
→ Job completes
→ Secure link
→ Download
→ Audit entry
```

### Schedule

```text
Create schedule
→ Weekly
→ XLSX
→ Authorized recipients
→ Run
→ Revalidate access
→ Generate
→ Deliver
```

---

# 22C.79 Files/modules expected

Implement within the existing architecture rather than rebuilding working modules.

Suggested backend structure:

```text
apps/api-gateway/src/
  analytics/
  dashboards/
  reports/
  exports/
  metrics/
  report-scheduler/
```

Suggested frontend:

```text
apps/web/
  dashboards/
  reports/
  exports/
  analytics/
```

Suggested shared packages:

```text
packages/types/
  dashboard.types.ts
  report.types.ts
  metric.types.ts
  export.types.ts
```

If the repository already contains equivalent modules, extend them instead of duplicating them.

---

# 22C.80 Queue jobs

At minimum:

```text
REPORT_GENERATE
REPORT_EXPORT_XLSX
REPORT_EXPORT_PDF
REPORT_EXPORT_CSV
REPORT_SNAPSHOT
REPORT_SCHEDULED_RUN
REPORT_DELIVERY
DASHBOARD_AGGREGATION
METRIC_REFRESH
```

All jobs need:

```text
tenant/platform context
correlation ID
authorization context
retry policy
dead-letter handling
observability
```

Never place unrestricted authorization credentials into a background job.

Persist enough immutable authorization context to safely revalidate access at execution time.

---

# 22C.81 "What is missing?" final gap checklist

The existing plan already contains analytics, dashboards and a report goal, but the following should now be treated as explicit requirements rather than optional enhancements:

### Critical

```text
[ ] Universal dashboard framework
[ ] Role-specific dashboard registry
[ ] SUPERADMIN command center
[ ] MOVE-IN-ADMIN dashboard
[ ] Finance dashboard
[ ] Project Manager dashboard
[ ] Project Coordinator dashboard
[ ] Platform Compliance dashboard
[ ] Security dashboard
[ ] Support dashboard
[ ] Auditor dashboard
[ ] Transport Admin dashboard
[ ] Transport Coordinator dashboard
[ ] Transport Compliance dashboard
[ ] Director dashboard
[ ] Senior Manager dashboard
[ ] Manager dashboard
[ ] Assistant Manager dashboard
[ ] Team Leader dashboard
[ ] Employee dashboard
[ ] Control Room dashboard
[ ] Vendor dashboard
[ ] Driver dashboard
[ ] Fleet dashboard
[ ] Safety dashboard
[ ] Maintenance dashboard

[ ] Universal report engine
[ ] Report catalog
[ ] Report Builder
[ ] Report permissions
[ ] Report versioning
[ ] Metric definitions
[ ] Metric lineage
[ ] Data dictionary
[ ] Drill-down
[ ] Saved reports
[ ] Saved filters
[ ] Dashboard personalization

[ ] XLSX export
[ ] PDF export
[ ] CSV export
[ ] JSON export
[ ] Async export jobs
[ ] Export audit
[ ] Export history
[ ] Secure downloads
[ ] Data masking
[ ] GPS masking
[ ] Export rate limits
[ ] Export retention
[ ] Sensitive export approval

[ ] Scheduled reports
[ ] Email delivery
[ ] SFTP delivery
[ ] Webhook delivery
[ ] API delivery

[ ] KPI targets
[ ] Benchmarking
[ ] Anomaly detection
[ ] Attention queue
[ ] AI summaries
[ ] Dashboard alerts

[ ] Financial snapshots
[ ] Compliance snapshots
[ ] Report snapshots
[ ] Historical reproducibility
```

### Enterprise

```text
[ ] Multi-currency
[ ] Localization
[ ] Multi-language reports
[ ] Confidentiality classification
[ ] Report sharing
[ ] Report subscriptions
[ ] Data freshness indicators
[ ] Dashboard observability
[ ] Report observability
[ ] Query performance tracking
[ ] Export abuse detection
[ ] Report API
[ ] Feature-flag controlled dashboards
[ ] Subscription entitlement control
[ ] Tenant-specific dashboard configuration
```

---

# 22C.82 Implementation rule

Mimo/OpenCode/Claude Code must:

1. Inspect the existing repository first.
2. Detect existing analytics/report/dashboard models and services.
3. Reuse working code.
4. Do not rebuild working modules.
5. Add missing models only where required.
6. Add migrations safely.
7. Implement backend authorization before frontend dashboard exposure.
8. Implement report engine before building dozens of report pages.
9. Implement export engine before wiring export buttons.
10. Implement metric definitions before hard-coding KPI calculations.
11. Implement dashboard registry before hard-coding role pages.
12. Implement async export queue before allowing large exports.
13. Add tests for tenant isolation and scope isolation.
14. Add export audit logging.
15. Add data masking.
16. Run lint.
17. Run typecheck.
18. Run unit tests.
19. Run integration tests.
20. Run build.
21. Update `docs/IMPLEMENTATION_STATUS.md`.
22. Document every new endpoint, model, permission and queue.
23. Do not mark a feature COMPLETE until it is implemented and tested end-to-end.

---

# 22C.83 Final dashboard/reporting acceptance flow

```text
User Login
↓
Authentication
↓
Role Resolution
↓
Permission Resolution
↓
Tenant / Platform Context
↓
Access Scope Resolution
↓
Reporting Hierarchy Resolution
↓
Dashboard Registry
↓
Authorized Dashboard
↓
Authorized Widgets
↓
Authorized Filters
↓
Metric Calculation
↓
Drill Down
↓
Report
↓
Data Masking
↓
Export Permission
↓
Export Job
↓
XLSX/PDF/CSV
↓
Encrypted Storage
↓
Audit Log
↓
Secure Download
```

For scheduled reports:

```text
Schedule
↓
Current Authorization Revalidation
↓
Current Scope Revalidation
↓
Current Recipient Authorization
↓
Generate
↓
Audit
↓
Deliver
```

---

# 22C.84 Definition of done

Phase 22C is complete only when:

- Every required role has a useful dashboard.
- Dashboards are scope-aware.
- Dashboard widgets are permission-aware.
- All KPI definitions are documented.
- Metrics have lineage.
- At least 60 baseline reports exist.
- Reports can drill down.
- XLSX export works.
- PDF export works.
- CSV export works.
- Large exports are asynchronous.
- Export history works.
- Export auditing works.
- Secure downloads work.
- PII/GPS masking works.
- Scheduled reports work.
- Scheduled reports revalidate authorization.
- Report snapshots work.
- Report versioning works.
- Dashboard personalization works.
- Saved reports work.
- Saved filters work.
- KPI targets work.
- Alerts work.
- Anomaly cards work where the required analytics are available.
- AI summaries are grounded and authorization-aware.
- Finance data remains separated.
- Cross-tenant export is impossible.
- Unauthorized hierarchy export is impossible.
- All critical authorization tests pass.
- All export tests pass.
- Lint/typecheck/test/build pass.
- Documentation is updated.
- No feature is marked complete without verification.

---

# FINAL MASTER-PLAN ADDENDUM — REPORTING PRINCIPLE

MOVE-IN-SYNC must not be implemented as a collection of static dashboards.

It is a **governed enterprise data and decision platform**.

The architecture must therefore treat:

```text
Dashboard
Metric
Report
Export
Schedule
Snapshot
Audit
Data Masking
Authorization
```

as one connected system.

The same underlying truth must power:

```text
SUPERADMIN
MOVE-IN-ADMIN
Finance
Project Management
Compliance
Security
Support
Auditor
Transport Admin
Control Room
Director
Manager
Team Leader
Employee
Vendor
Driver
```

while ensuring that each user sees only the information and actions permitted by:

```text
Role
+
Permission
+
Company
+
Site
+
LOB
+
Process
+
Shift
+
Reporting Hierarchy
+
Policy
+
Data Classification
```

**Never implement dashboard visibility as frontend-only role checks.**

**Never implement export as an unrestricted database query.**

**Never allow a dashboard to become a side-channel around the core authorization system.**

**Never allow an AI summary, cached metric, scheduled report, export worker, or background job to bypass authorization.**

This reporting architecture is part of the core MOVE-IN-SYNC security boundary, not merely a UI feature.


---

# PHASE 41: TRANSPORT CONTROL, SCHEDULING & OPERATIONAL POLICY ENGINE

**Status:** REQUIRED  
**Purpose:** Make MOVE-IN-SYNC a genuinely transport-operable platform. Authorized customer transport personnel must be able to decide when cabs and shuttles are available, define schedules, configure routes and clubbing, manage no-show policies, control dispatch and respond to operational changes—without mixing customer authority with internal MOVE-IN-SYNC authority.

## 41.1 HRMS is optional — never a transport authority

HRMS/HCM integration is **NOT a required dependency for transport operations**.

MOVE-IN-SYNC remains the authoritative transport operational system.

HRMS, if integrated later, may provide reference information, but an HRMS event must never automatically:

- cancel a booking
- cancel recurring transport
- suspend transport
- remove transport eligibility
- change a route
- change a schedule
- change a no-show policy
- dispatch or un-dispatch a vehicle
- terminate an active trip

Instead:

```text
Optional HRMS Event
        ↓
Reference-data change detected
        ↓
HRMS Change Alert
        ↓
Authorized Transport Admin review
        ↓
Approve / Ignore / Investigate
        ↓
Authorized transport action
```

Any future automatic HRMS workflow must be explicitly enabled by the customer, versioned, auditable, reversible, and protected by impact analysis.

---

# 41.2 THREE AUTHORITY DOMAINS

The platform must maintain three distinct security domains.

## Domain A — INTERNAL PLATFORM

These are MOVE-IN-SYNC personnel.

```text
SUPERADMIN
PLATFORM FINANCE
PROJECT MANAGER
PROJECT COORDINATOR
PLATFORM COMPLIANCE
SECURITY ADMINISTRATOR
SUPPORT ENGINEER
PLATFORM AUDITOR
```

## Domain B — CUSTOMER / TENANT

These are customer-company personnel.

```text
TRANSPORT ADMIN
TRANSPORT SUB-ADMIN
TRANSPORT COORDINATOR
TRANSPORT COMPLIANCE
DIRECTOR
SENIOR MANAGER
MANAGER
ASSISTANT MANAGER
TEAM LEADER
EMPLOYEE
TRAINER
```

## Domain C — TRANSPORT PARTNERS

```text
VENDOR ADMIN
VENDOR DISPATCHER
DRIVER
GUARD
```

### Absolute rule

No role automatically inherits authority from another domain.

Examples:

```text
SUPERADMIN ≠ Transport Admin
Project Manager ≠ Transport Admin
Director ≠ SUPERADMIN
Driver ≠ Employee
Vendor Admin ≠ Transport Admin
Security Admin ≠ Transport Admin
Platform Finance ≠ Customer Finance
```

---

# 41.3 TWO SEPARATE CUSTOMER HIERARCHIES

The customer environment contains two independent authority structures.

## Organizational hierarchy

```text
DIRECTOR
   ↓
SENIOR MANAGER
   ↓
MANAGER
   ↓
ASSISTANT MANAGER
   ↓
TEAM LEADER
   ↓
EMPLOYEE
```

This controls employee/reporting visibility and organizational approvals.

## Transport operational hierarchy

```text
TRANSPORT ADMIN
       ↓
TRANSPORT SUB-ADMIN
       ↓
TRANSPORT COORDINATOR
       ↓
TRANSPORT OPERATIONS
       ↓
DRIVERS / VENDORS / GUARDS
```

This controls transport operations.

**Neither hierarchy automatically grants the permissions of the other.**

A Manager does not become Transport Coordinator merely because they manage employees.

A Director does not become Transport Admin merely because they have executive authority.

A Transport Admin does not automatically become the employee's Manager.

---

# 41.4 FINAL AUTHORIZATION MODEL

Every protected action must evaluate:

```text
Identity
+
Authority Domain
+
Role
+
Permission
+
Tenant
+
Scope
+
Organizational Hierarchy where applicable
+
Policy
+
Data Classification
+
Resource Ownership
+
Effective Date
```

Never use:

```text
if role == "ADMIN"
```

as the complete authorization decision.

Use:

```text
domain
→ role
→ permission
→ tenant
→ scope
→ hierarchy
→ policy
→ resource
```

---

# 41.5 MULTI-SCOPE AUTHORIZATION

Transport Admin, Transport Sub-Admin and authorized Coordinators must support multiple:

```text
Company Codes
Site Codes
LOB Codes
Process Codes
Shift Codes
Department Codes
Business Unit Codes
Cost Centre Codes
```

Use scope tuples rather than independent lists where necessary.

Example:

```text
CONC001 | PUN01 | BPO | VOICE01 | NIGHT
CONC001 | PUN01 | BPO | CHAT01  | NIGHT
CONC001 | MUM01 | FIN | FIN01   | DAY
```

Do not accidentally interpret:

```text
Sites = PUN01, MUM01
Processes = VOICE01, FIN01
```

as permission for every possible combination.

---

# 41.6 TRANSPORT ADMIN

Transport Admin is the highest normal customer transport authority within assigned scope.

Can, subject to policy:

```text
Configure transport schedules
Create/edit/publish availability
Configure cab availability
Configure shuttle availability
Configure routes
Configure stops
Configure nodals
Configure clubbing rules
Configure no-show policies
Configure booking policies
Configure cancellation policies
Configure dispatch policies
Configure fleet operations
Manage drivers
Manage vendors
Manage replacements
Manage breakdown operations
Manage transport dashboards
Run reports
Export authorized reports
Configure authorized operational policies
```

Transport Admin cannot access another tenant.

Transport Admin's authority is still restricted by assigned company/site/LOB/process/shift scope.

---

# 41.7 TRANSPORT SUB-ADMIN

Transport Sub-Admin is a customer role.

It receives a delegated operational scope from Transport Admin or another authorized company administrator.

Example:

```text
Sub-Admin A
Company: CONC001
PUN01
VOICE01
CHAT01
NIGHT

Sub-Admin B
Company: CONC001
MUM01
FIN01
DAY
```

Sub-Admin can perform only the permissions granted for those scopes.

---

# 41.8 TRANSPORT COORDINATOR

Transport Coordinator is operational.

Can, where explicitly permitted:

```text
Open slots
Close slots
Manage today's availability
Assign vehicles
Assign drivers
Manage dispatch
Move authorized bookings
Handle breakdowns
Arrange replacements
Manage no-show queue
Manage clubbing operations
Create emergency slots
Operate routes
```

A Coordinator does not automatically receive policy-writing authority.

For example:

```text
DISPATCH_MANAGE = YES
SCHEDULE_OPERATE = YES
NO_SHOW_POLICY_EDIT = NO
RATE_CARD_EDIT = NO
COMPANY_POLICY_EDIT = NO
```

---

# 41.9 TRANSPORT COMPLIANCE

Transport Compliance may:

```text
Review compliance
Review documents
Review driver eligibility
Review vehicle eligibility
Review vendor compliance
Review training
Review inspections
Flag failures
Block non-compliant dispatch
Clear blocks when requirements are satisfied
```

It does not automatically receive:

```text
Route editing
Schedule editing
Clubbing policy editing
Rate card editing
Company transport policy editing
```

Safety/compliance blocks may prevent unsafe dispatch, but the role must not be turned into a general transport administrator.

---

# 41.10 DIRECTOR

Director receives executive visibility and configured approval authority.

Can:

```text
View company transport KPIs
View authorized sites
View authorized processes
Compare performance
Review costs
Review safety
Review compliance
Review major incidents
Approve configured high-value exceptions
Review escalations
```

Director does not automatically receive:

```text
Dispatch
Route configuration
Schedule configuration
No-show policy configuration
Fleet CRUD
Driver CRUD
Vendor CRUD
```

unless separately authorized.

---

# 41.11 SENIOR MANAGER

Senior Manager sees authorized subordinate-manager aggregates.

```text
Teams
Managers
Employees
Transport usage
Cost
Approvals
No-shows
Expenses
Issues
Performance
```

No unrestricted company access unless explicitly assigned.

---

# 41.12 MANAGER

Manager authority follows authorized reporting relationships.

Can:

```text
Book transport for authorized reportees
Cancel authorized bookings
Reschedule authorized bookings
Approve configured requests
Review expenses
Review no-show appeals
View authorized team transport
```

Manager cannot use the organizational hierarchy to access transport administration functions.

---

# 41.13 ASSISTANT MANAGER

Assistant Manager receives delegated manager capabilities.

Every delegated permission has:

```text
Granted By
Scope
Permission
Effective From
Effective To
Reason
Audit
```

---

# 41.14 TEAM LEADER

Team Leader sees and operates within direct/authorized team scope.

```text
Team transport
Upcoming trips
Trip status
Issues
Configured approvals
```

No transport policy authority unless separately granted.

---

# 41.15 EMPLOYEE

Employee is self-service only:

```text
Own profile
Own bookings
Own trips
Own driver/vehicle information where permitted
Own expenses
Own no-show records
Own appeals
Own feedback
Own notifications
```

---

# 41.16 TRAINER

Trainer is restricted to:

```text
Training
Safety training
Certification
Training records
```

---

# 41.17 VENDOR / DRIVER / GUARD

Partner roles are isolated from customer administrative roles.

Vendor:

```text
Own assigned contract/scope
Assigned trips
Drivers
Vehicles
Invoices
Performance
```

Driver:

```text
Own assigned trips
Own vehicle
Navigation
Passenger boarding
Trip lifecycle
SOS
Breakdown
```

Guard:

```text
Assigned safety duties
Assigned trips
Safety events
```

---

# 41.18 TRANSPORT SCHEDULE BUILDER

Authorized customer transport users can create arbitrary operational time slots.

Examples:

```text
01:00
01:30
02:00
02:30
03:00
...
23:00
23:30
00:00
00:30
```

Do not assume a fixed set of times.

Support:

```text
15-minute
20-minute
30-minute
45-minute
60-minute
Custom
```

Example:

```text
01:00
01:20
01:40
02:00
```

---

# 41.19 CAB AVAILABILITY

Transport Admin or authorized Sub-Admin/Coordinator can configure:

```text
Site
Process
Shift
Transport Type
Vehicle Type
Capacity
AC / Non-AC
Vendor
Driver Pool
Available From
Available Until
Booking Cutoff
Cancellation Cutoff
```

Example:

```text
Night Transport

22:00
22:30
23:00
23:30
00:00
00:30
01:00
01:30
02:00
```

---

# 41.20 SHUTTLE AVAILABILITY

Configure:

```text
Route
Departure
Return
Frequency
Stops
Capacity
Vehicle
Driver
Boarding Window
Booking Required
Seat Reservation
```

Example:

```text
PUN01 → Phase 2

06:00
06:30
07:00
07:30
08:00
```

---

# 41.21 TRANSPORT AVAILABILITY CALENDAR

Provide a visual calendar showing:

```text
Time
Day
Transport Type
Capacity
Demand
Availability
Status
```

Example:

```text
01:00   ✓
01:30   ✓
02:00   ⚠ shortage
02:30   ✓
03:00   ✕ unavailable
```

Clicking a slot opens its configuration.

---

# 41.22 BOOKING WINDOW

Configurable by company/site/process/shift:

```text
Booking opens
Booking closes
Same-day booking
Last-minute booking
Emergency booking
Approval requirement
```

Example:

```text
Booking opens: 7 days before
Booking closes: 60 minutes before
Same-day: YES
Emergency: YES
Late booking: approval
```

---

# 41.23 CANCELLATION POLICY

Configurable:

```text
Normal cancellation cutoff
Late cancellation
Emergency cancellation
Penalty
Reason requirement
Approval
```

Changing the policy must not silently alter already completed/historical trips.

---

# 41.24 NO-SHOW POLICY BUILDER

Authorized Transport Admin/Sub-Admin can configure within scope:

```text
Grace Period
Call Attempts
Call Interval
GPS Requirement
Geofence Requirement
Evidence Requirement
Appeal Window
Escalation
Temporary Ban
Penalty
Ban Threshold
```

Existing mandatory evidence requirements remain enforced.

---

# 41.25 CLUBBING POLICY BUILDER

Configure:

```text
Maximum passengers
Maximum pickup points
Maximum detour
Maximum ride time
Maximum distance
Same site
Same process
Same shift
Vehicle type
Vendor restrictions
Safety restrictions
```

Example:

```text
Clubbing: ON
Max passengers: 4
Max pickup points: 4
Max detour: 15 min
Max ride: 90 min
Same site: YES
Same shift: YES
```

---

# 41.26 CLUBBING ENGINE

Evaluate:

```text
Pickup compatibility
Drop compatibility
Time compatibility
Capacity
Detour
Ride duration
Safety
Compliance
Vehicle
Driver
Vendor
```

Return:

```text
CLUB
DO NOT CLUB
```

with an explanation.

Example:

```text
Cannot club:
Expected detour = 21 min
Maximum allowed = 15 min
```

---

# 41.27 ROUTE BUILDER

Authorized users can configure:

```text
Route
Route Version
Stops
Stop Sequence
Pickup Points
Drop Points
Nodals
Restricted Areas
Preferred Roads
Avoid Areas
Maximum Duration
Maximum Distance
```

Route types:

```text
Fixed
Semi-Dynamic
Dynamic
```

---

# 41.28 ROUTE VERSIONING

Never destructively modify an active historical route.

Use:

```text
PUN-NIGHT-01 v1
PUN-NIGHT-01 v2
PUN-NIGHT-01 v3
```

Future operations use the new version.

Historical trips retain the route version actually used.

---

# 41.29 POLICY INHERITANCE

Support:

```text
Company
 ↓
Site
 ↓
LOB
 ↓
Process
 ↓
Shift
```

Example:

```text
Company cancellation: 30 min
Site override: 45 min
Process override: 60 min
Shift override: 90 min

Effective rule: 90 min
```

Display why the effective rule exists.

---

# 41.30 SCHEDULE PUBLISHING

Schedule lifecycle:

```text
DRAFT
↓
VALIDATING
↓
APPROVED
↓
PUBLISHED
↓
ACTIVE
↓
EXPIRED
↓
ARCHIVED
```

Validation checks:

```text
Driver availability
Vehicle availability
Capacity
Compliance
Maintenance
Vendor capacity
Route conflicts
Shift conflicts
Safety requirements
```

---

# 41.31 PROTECT EXISTING BOOKINGS

A schedule change must not silently cancel already-booked transport.

Instead:

```text
Schedule Change
↓
Impact Analysis
↓
Affected bookings
↓
Affected employees
↓
Affected trips
↓
Rebooking options
↓
Explicit confirmation
```

Options:

```text
Keep old slot
Move slot
Alternative cab
Alternative shuttle
Manual review
Cancel with reason
```

---

# 41.32 CHANGE IMPACT ANALYSIS

Before a material change show:

```text
Employees affected
Bookings affected
Trips affected
Vehicles affected
Drivers affected
Vendors affected
Cost impact
Capacity impact
Safety impact
```

---

# 41.33 POLICY SIMULATOR

Transport Admin can simulate future changes without publishing them.

Example:

```text
Site: PUN01
Process: VOICE01
Shift: NIGHT
Date: Tomorrow
```

Output:

```text
Expected employees
Expected demand
Available seats
Projected shortage
Clubbed trips
Solo trips
Shuttle passengers
Expected cost
Expected empty KM
```

---

# 41.34 EMERGENCY OVERRIDE

Authorized users can temporarily:

```text
Open extra slots
Close slots
Add vehicles
Change route
Change vendor
Create emergency transport
Move pickup
Disable clubbing temporarily
```

Every override requires:

```text
Reason
Scope
Actor
Before
After
Timestamp
```

---

# 41.35 CONTROL CENTER

Transport Admin gets:

```text
Today's Demand
Available Capacity
Shortage
Active Trips
No-Shows
Breakdowns
```

plus:

```text
Schedule
Routes
Clubbing
No-Show Policy
Availability
Dispatch
Fleet
Vendors
```

Sub-Admin sees the same framework filtered to assigned scope.

Coordinator sees operational controls allowed by permission.

---

# 41.36 ROLE-SCOPED DASHBOARD SEPARATION

## Platform dashboards

```text
SUPERADMIN
PLATFORM FINANCE
PROJECT MANAGER
PROJECT COORDINATOR
PLATFORM COMPLIANCE
SECURITY ADMINISTRATOR
SUPPORT ENGINEER
PLATFORM AUDITOR
```

## Customer dashboards

```text
TRANSPORT ADMIN
TRANSPORT SUB-ADMIN
TRANSPORT COORDINATOR
TRANSPORT COMPLIANCE
DIRECTOR
SENIOR MANAGER
MANAGER
ASSISTANT MANAGER
TEAM LEADER
EMPLOYEE
TRAINER
```

## Partner dashboards

```text
VENDOR ADMIN
VENDOR DISPATCHER
DRIVER
GUARD
```

No shared "admin dashboard" that exposes all functions.

---

# PHASE 42: ENTERPRISE ARCHITECTURE HARDENING

## 42.1 Event-driven architecture

Create a platform event model.

Examples:

```text
EmployeeCreated
EmployeeUpdated
EmployeeTransportStatusChanged

BookingCreated
BookingApproved
BookingCancelled
BookingRescheduled

TripAssigned
DriverAccepted
DriverDeclined
TripStarted
TripCompleted

BreakdownDetected
ReplacementAssigned
PassengerTransferred

NoShowCreated
AppealSubmitted
AppealResolved

InvoiceCreated
InvoiceReconciled
PaymentReceived

ComplianceExpired
ComplianceCleared

SOSCreated
IncidentCreated

ReportGenerated
ExportCreated
ExportDownloaded
```

Consumers:

```text
Notifications
Analytics
AI
Audit
Billing
Webhooks
Search
ML
Reporting
```

---

# 42.2 Idempotency

Every important write operation must support idempotency.

Examples:

```text
Booking
Payment
Invoice
Dispatch
Replacement
GPS batch
Webhook
Notification
Import
```

Use:

```text
Idempotency-Key
Request ID
Correlation ID
Command ID
```

Repeated requests must not create duplicate business records.

---

# 42.3 Central state-machine architecture

Use a common state-machine framework for:

```text
Booking
Trip
Approval
Expense
Invoice
No-Show
Subscription
Vehicle
Driver
Incident
Breakdown
Replacement
```

Every transition has:

```text
From
To
Permission
Guard
Action
Audit
Reason
```

Invalid transitions must return a controlled error.

---

# 42.4 Effective-dated history

Preserve historical truth for:

```text
Employee organization
Manager relationship
Scope
Policy
Rate card
Vendor contract
Driver assignment
Vehicle assignment
Eligibility
Subscription
Pricing
```

Historical reports must answer:

> What was true at the time of the transaction?

---

# 42.5 Data governance center

Add:

```text
Data Classification
Data Owner
Purpose
Retention
Legal Basis
Residency
Encryption
Access History
Deletion
Legal Hold
```

Employee privacy capabilities:

```text
View my data
Download my data
Correction request
Deletion request
Consent
GPS usage explanation
```

---

# 42.6 Evidence vault

Maintain immutable evidence for:

```text
No-show calls
No-show screenshots
Approvals
Expense receipts
KYC
Documents
Incidents
SOS
Breakdowns
Export approvals
Break-glass
Impersonation
```

Each evidence object has:

```text
Hash
Timestamp
Actor
Source
Device
IP
Retention
Legal Hold
```

---

# 42.7 Disaster recovery hardening

Backups must include:

```text
Encrypted backups
Immutable backups
Cross-region backup
Restore verification
PITR
WAL
Database recovery
Redis recovery
Object storage recovery
Secrets recovery
Configuration recovery
```

Run periodic recovery drills.

Success means:

```text
Platform rebuilt
Data restored
Integrity verified
Applications started
Critical journeys passed
```

---

# 42.8 Degraded-mode architecture

Optional dependencies must not take down core transport operations.

Examples:

```text
Routing provider down
→ fallback provider

ML unavailable
→ deterministic dispatch

SMS unavailable
→ push/email

GPS delayed
→ last known position + stale indicator

AI unavailable
→ normal application

HRMS unavailable
→ transport continues normally
```

---

# 42.9 Provider abstraction

Use interfaces for:

```text
Routing
Maps
GPS
SMS
Email
WhatsApp
Push
Payments
Storage
OCR
AI
Identity
HRMS
```

Each provider supports:

```text
Health
Latency
Cost
Failure Rate
Timeout
Retry
Circuit Breaker
Fallback
```

---

# 42.10 Circuit breaker

Implement:

```text
Timeout
Retry
Exponential Backoff
Circuit Breaker
Bulkhead
Fallback
Dead Letter
Provider Health
```

---

# 42.11 Tenant lifecycle

Use:

```text
PROVISIONING
TRIAL
ACTIVE
GRACE_PERIOD
PAST_DUE
SUSPENDED
READ_ONLY
OFFBOARDING
EXPORTING
DELETED
```

SaaS suspension must not destroy transport history.

---

# 42.12 Tenant offboarding

Support:

```text
Data Export
Report Export
Document Export
Audit Export
Configuration Export
Employee Export
Trip Export
Financial Export
```

Workflow:

```text
Request
↓
Approval
↓
Freeze
↓
Export
↓
Verification
↓
Retention
↓
Deletion
```

---

# 42.13 Legal hold

Authorized roles can place:

```text
Company
Employee
Trip
Invoice
Document
Incident
Audit range
```

under legal hold.

Retention jobs must respect legal holds.

---

# 42.14 Financial ledger

Create or extend an immutable financial ledger for:

```text
Transport billing
Vendor settlement
Expenses
SaaS billing
Payments
Refunds
Credits
Tax
FX
Adjustments
```

Every financial mutation must be traceable.

---

# 42.15 Reconciliation engine

Support:

```text
Trip ↔ Vendor invoice
Trip ↔ Rate card
Invoice ↔ Payment
Payment ↔ Settlement
SaaS invoice ↔ Gateway
Usage ↔ Subscription
Tax ↔ Invoice
```

States:

```text
MATCHED
UNMATCHED
PARTIALLY_MATCHED
EXCEPTION
```

---

# 42.16 Enterprise SSO / SCIM

SSO is separate from transport authority.

Support where configured:

```text
SAML
OIDC
Microsoft Entra ID
Google Workspace
Okta
SCIM
JIT Provisioning
Domain Verification
SSO-only tenant
```

SCIM deprovisioning must revoke access safely, but must not automatically cancel active transport unless the customer has explicitly configured and approved such a workflow.

---

# 42.17 Integration Hub

Optional integrations:

```text
HRMS
Identity
GPS
Routing
Notification
Payment
OCR
AI
Storage
```

Each integration has:

```text
Connected
Healthy
Degraded
Failed
Disabled
```

Integration failure must not silently change transport decisions.

---

# 42.18 Master-data management

Canonical codes:

```text
Company Code
Region Code
Site Code
LOB Code
Process Code
Department Code
BU Code
Cost Centre Code
Project Code
Office Code
Shift Code
```

Employee assignments must use these canonical records.

CSV import must validate hierarchy:

```text
Company
↓
Site
↓
LOB
↓
Process
↓
Shift
```

Do not create new masters because of CSV typos.

---

# 42.19 Employee organization history

Use effective-dated assignments.

```text
EmployeeOrgAssignment

Employee
Company
Site
LOB
Process
Department
BU
Cost Centre
Project
Shift
Effective From
Effective To
Changed By
Reason
```

---

# 42.20 Headcount snapshots

Track:

```text
Active
Suspended
Inactive
Total
```

by:

```text
Company
Site
LOB
Process
Department
BU
Cost Centre
Project
Shift
```

This powers:

```text
Active Headcount
Site Headcount
Process Headcount
Shift Headcount
Director View
Transport Admin View
Sub-Admin View
Reports
```

---

# 42.21 Developer portal

Support:

```text
OpenAPI
API Keys
OAuth
Scopes
Webhooks
Webhook Logs
Webhook Replay
Sandbox
Usage
Rate Limits
SDK Examples
API Versioning
Deprecation
```

---

# 42.22 API versioning

Use explicit API versions where required.

```text
/v1
/v2
```

Track:

```text
Version
Usage
Deprecation
Migration Guide
Breaking Changes
```

---

# 42.23 Global search

Search:

```text
Employees
Drivers
Vehicles
Trips
Bookings
Invoices
Vendors
Incidents
Documents
Reports
Audit
Companies
```

Search results must be authorization-filtered.

---

# 42.24 Notification preference center

Configure:

```text
Event
Channel
Priority
Quiet Hours
Timezone
Language
Opt-in
Opt-out
Mandatory/Optional
```

Critical safety notifications cannot be accidentally disabled.

---

# 42.25 Mobile security

Support:

```text
Device Registration
Device Trust
Biometric Lock
Root/Jailbreak Detection
App Integrity
Minimum App Version
Forced Upgrade
Offline Encryption
Encrypted Local Storage
GPS Spoof Detection
Mock Location Detection
Remote Logout
```

---

# 42.26 GPS fraud detection

Detect:

```text
Impossible Speed
Teleportation
GPS Jumps
Mock Location
Repeated Coordinates
Timestamp Manipulation
Route Mismatch
Driver/Device Mismatch
```

Detection creates an investigation signal, not an automatic punishment solely from an ML score.

---

# 42.27 SLA engine

Support:

```text
Booking SLA
Approval SLA
Dispatch SLA
Pickup SLA
Drop SLA
Incident SLA
SOS SLA
Breakdown SLA
Replacement SLA
No-show Appeal SLA
Support SLA
Compliance SLA
Invoice SLA
```

With:

```text
Business Hours
Holiday Calendar
Pause
Resume
Escalation
Breach
Penalty
Notification
```

---

# 42.28 Workflow builder

Authorized company admins can define:

```text
Trigger
Condition
Approval
Notification
Escalation
Action
```

Examples:

```text
Expense > threshold
→ Manager
→ Director
→ Finance

Document expires
→ Compliance
→ Transport Admin
→ Dispatch Block

No-show appeal
→ Manager
→ Transport Admin
→ Director
```

Workflow actions must respect role permissions.

---

# 42.29 Synthetic end-to-end monitoring

Use the internal test tenant.

Run:

```text
Create company
Login
Create employee
Book
Approve
Dispatch
Driver accept
GPS
Complete
Invoice
Report
Export
```

If a critical synthetic journey fails:

```text
Alert
+
Release Gate
```

---

# 42.30 AI governance

Add:

```text
AI Model Registry
Prompt Registry
Model Version
Prompt Version
Evaluation Dataset
Accuracy
Hallucination Rate
Cost
Latency
Provider
Fallback
Safety Policy
Human Approval
AI Decision Log
```

AI must never:

```text
Grant permissions
Bypass scope
Fabricate metrics
Invent evidence
Override compliance
Automatically make high-risk operational decisions without configured authorization
```

---

# 42.31 ESG / carbon ledger

Add:

```text
Carbon Ledger
Emission Factor
Vehicle Emission Profile
Trip Emission
Fleet Emission
Carbon Reduction
ESG Report
```

Support:

```text
ICE
Hybrid
EV
Public Transport
Carpool
```

---

# 42.32 Configuration center

Authorized administrators can manage:

```text
Policies
Feature Flags
Integrations
Notifications
SLA
Pricing
Rate Cards
Retention
Security
Approval Limits
Export Limits
GPS Precision
Branding
Language
Timezone
Currency
```

All changes have:

```text
Before
After
Who
When
Reason
Approval
Effective Date
Rollback Version
```

---

# 42.33 RELEASE / CHANGE MANAGEMENT

Every significant production change should have:

```text
Change Request
Risk
Affected Tenants
Affected Scope
Migration
Approval
Release
Verification
Rollback
Release Notes
Post-Release Review
```

Use:

```text
INTERNAL_TEST
CANARY
EARLY
GENERAL
```

---

# 42.34 FINAL AUTHORITY TEST MATRIX

The implementation is not complete until these pass.

```text
SUPERADMIN
✓ Platform access
✗ Not automatically customer Transport Admin

PLATFORM FINANCE
✓ SaaS billing
✗ GPS
✗ Dispatch
✗ Customer transport policy

PROJECT MANAGER
✓ Assigned company portfolio
✗ Unassigned companies
✗ Customer transport administration

PROJECT COORDINATOR
✓ Assigned onboarding
✗ Customer transport policy unless separately authorized

PLATFORM COMPLIANCE
✓ Platform compliance
✗ General dispatch administration

SECURITY ADMIN
✓ Platform security
✗ Transport administration

SUPPORT
✓ Authorized support
✗ Permanent customer-admin authority

AUDITOR
✓ Read-only
✗ Write

TRANSPORT ADMIN
✓ Customer transport administration within scope
✗ Other tenant

SUB-ADMIN
✓ Assigned transport scope
✗ Outside scope

COORDINATOR
✓ Authorized operational controls
✗ Policy changes without permission

TRANSPORT COMPLIANCE
✓ Compliance blocks
✗ General transport administration

DIRECTOR
✓ Executive visibility/approved exceptions
✗ Automatic Transport Admin authority

MANAGER
✓ Authorized reportees
✗ Other teams

EMPLOYEE
✓ Self
✗ Other employees

VENDOR
✓ Own authorized partner scope
✗ Customer administration

DRIVER
✓ Own assigned trips
✗ Administration
```

---

# 42.35 FINAL TRANSPORT AUTHORITY FLOW

```text
USER
 ↓
AUTHENTICATION
 ↓
AUTHORITY DOMAIN
 ├── INTERNAL PLATFORM
 ├── CUSTOMER TENANT
 └── PARTNER
 ↓
ROLE
 ↓
PERMISSION
 ↓
TENANT
 ↓
SCOPE
 ↓
HIERARCHY IF APPLICABLE
 ↓
POLICY
 ↓
RESOURCE
 ↓
ACTION
 ↓
AUDIT
```

---

# 42.36 FINAL TRANSPORT OPERATING FLOW

```text
COMPANY CONFIGURATION
        ↓
SITE CONFIGURATION
        ↓
LOB / PROCESS
        ↓
SHIFT
        ↓
TRANSPORT AVAILABILITY
        ↓
SCHEDULE
        ↓
ROUTE
        ↓
CLUBBING
        ↓
BOOKING
        ↓
APPROVAL
        ↓
DISPATCH
        ↓
SAFETY / COMPLIANCE
        ↓
LIVE TRIP
        ↓
NO-SHOW / BREAKDOWN / SOS
        ↓
COMPLETION
        ↓
BILLING
        ↓
REPORTING
```

---

# 42.37 ABSOLUTE BUSINESS RULES

1. Internal and external roles are separate.
2. Partner roles are separate.
3. No implicit cross-domain role inheritance.
4. Organizational hierarchy and transport hierarchy are separate.
5. Role alone never grants authorization.
6. Scope is always server-enforced.
7. Multiple site/process/shift scope is supported.
8. Scope combinations must be explicit.
9. HRMS is optional.
10. HRMS cannot silently cancel transport.
11. MOVE-IN-SYNC remains transport authority.
12. Transport Admin controls authorized transport operations.
13. Sub-Admin operates delegated scope.
14. Coordinator operates authorized operations.
15. Compliance can block unsafe/non-compliant operations.
16. Director does not automatically become Transport Admin.
17. Manager does not automatically become Transport Admin.
18. Platform Finance does not automatically receive transport data.
19. Security Admin does not automatically receive transport operations.
20. Support access is time-limited and audited.
21. Auditor is read-only.
22. Existing bookings are protected from silent schedule changes.
23. Policy changes are versioned.
24. Route changes are versioned.
25. Rate cards are immutable historically.
26. No-show evidence is preserved.
27. Safety takes priority over cost.
28. Compliance takes priority over preference.
29. AI cannot bypass authorization.
30. Exports cannot bypass authorization.
31. Background jobs cannot bypass authorization.
32. Cached data cannot bypass tenant isolation.
33. Scheduled reports must revalidate authorization.
34. Historical data must remain historically accurate.
35. Every administrative change is auditable.
36. Every high-risk override requires a reason.
37. Production releases must be reversible.
38. No feature is COMPLETE until tested end-to-end.

---

# FINAL DEFINITION OF DONE FOR THE MASTER PLAN

MOVE-IN-SYNC is considered specification-complete only when the implementation provides:

```text
✓ Multi-tenant isolation
✓ Separate internal/external/partner authority domains
✓ Separate organizational and transport hierarchies
✓ Company/Site/LOB/Process/Shift scope
✓ Multiple site/process/shift assignments
✓ Canonical master-data codes
✓ Effective-dated employee organization
✓ Active headcount tracking
✓ Transport schedule builder
✓ Arbitrary cab/shuttle time slots
✓ Route builder
✓ Route versioning
✓ Clubbing engine
✓ No-show policy builder
✓ Booking policy builder
✓ Cancellation policy
✓ Dispatch engine
✓ Compliance blocking
✓ Breakdown/replacement
✓ SOS
✓ GPS
✓ Fleet
✓ Vendor
✓ Expenses
✓ Transport billing
✓ SaaS billing
✓ Dashboards
✓ 60+ enterprise reports
✓ Report Builder
✓ XLSX/PDF/CSV/JSON
✓ Async exports
✓ Export audit
✓ Secure downloads
✓ Scheduled reports
✓ Data masking
✓ KPI targets
✓ Anomaly detection
✓ AI summaries
✓ Data dictionary
✓ Metric lineage
✓ Snapshots
✓ Versioning
✓ SSO/SCIM
✓ Integration Hub
✓ Event architecture
✓ Idempotency
✓ State machines
✓ Effective-dated history
✓ Data governance
✓ Evidence vault
✓ Disaster recovery
✓ Degraded mode
✓ Provider abstraction
✓ Circuit breakers
✓ Tenant lifecycle
✓ Offboarding
✓ Legal hold
✓ Financial ledger
✓ Reconciliation
✓ Developer portal
✓ API versioning
✓ Global search
✓ Mobile security
✓ GPS fraud detection
✓ SLA engine
✓ Workflow engine
✓ Synthetic monitoring
✓ AI governance
✓ ESG/carbon
✓ Configuration center
✓ Release/change management
```

**FINAL IMPLEMENTATION INSTRUCTION**

The coding agent must first inspect the existing repository and the current implementation status. It must extend existing working modules rather than rebuild them. It must never claim a feature is implemented merely because models/interfaces/placeholders exist. For every new capability:

```text
Inspect
→ Design
→ Implement
→ Migrate
→ Authorize
→ Test
→ Integrate
→ Verify
→ Document
→ Mark status
```

The final system must be tested through real end-to-end journeys.

**Do not mix roles. Do not trust client authorization. Do not make HRMS a transport dependency. Do not silently cancel existing transport. Do not allow dashboards/reports/exports/AI/background jobs to bypass authorization.**


---

# PHASE 43: CROSS-PROCESS CLUBBING + FLEXIBLE BILLING ARCHITECTURE

**Status:** REQUIRED

This phase formally locks the latest business requirement into the master specification.

## 43.1 Site is the primary operational boundary

A company may have any number of sites.

```text
COMPANY
├── SITE 001
│   ├── PROCESS A
│   ├── PROCESS B
│   └── PROCESS C
├── SITE 002
│   ├── PROCESS A
│   └── PROCESS D
├── SITE 003
│   ├── PROCESS X
│   ├── PROCESS Y
│   └── PROCESS Z
└── SITE N
```

There is no hard-coded maximum number of sites.

Each site may independently have:

- Processes
- LOBs
- Departments
- Business Units
- Cost Centres
- Projects
- Shifts
- Transport schedules
- Routes
- Nodals
- Pickup points
- Vendors
- Fleet
- Policies
- Billing configuration

---

## 43.2 Process is not automatically a clubbing boundary

Employees belonging to different processes within the **same site** may be clubbed.

Example:

```text
COMPANY: CONC001
SITE: PUN01

VOICE01 → 2 employees
CHAT01  → 1 employee
FIN01   → 1 employee
```

The system may create:

```text
ONE 4-SEATER CAB
```

if all applicable conditions are satisfied.

Required checks:

```text
Same operational site
Compatible pickup time
Compatible drop time
Compatible direction
Capacity available
Maximum detour satisfied
Maximum ride time satisfied
Safety rules satisfied
Driver compliant
Vehicle compliant
Vendor allowed
Shift rules satisfied
Route constraints satisfied
```

**Process is an organizational/reporting/billing dimension. It is not inherently a transport boundary.**

---

## 43.3 Clubbing modes

The clubbing engine must support configurable modes:

```text
SAME_PROCESS_ONLY
SAME_SITE_ANY_PROCESS
SAME_LOB_ANY_PROCESS
CROSS_LOB_ALLOWED
CUSTOM_SCOPE
```

The customer may choose its permitted mode.

Recommended default:

```text
SAME_SITE_ANY_PROCESS
```

subject to all safety, timing, capacity, routing and compliance rules.

---

## 43.4 Cross-process optimization

Optimization must be performed at the site operational level.

Do not independently optimize each process when cross-process clubbing is permitted.

Incorrect:

```text
VOICE01 → optimize
CHAT01  → optimize
FIN01   → optimize
```

Correct:

```text
PUN01
 ↓
All eligible transport demand
 ↓
VOICE01
CHAT01
FIN01
TECH01
 ↓
Optimization Engine
 ↓
Best grouping
```

Optimization objectives may include:

```text
Minimize total transport cost
Minimize empty KM
Minimize detour
Minimize ride time
Maximize vehicle utilization
Minimize unused capacity
Meet pickup/drop SLA
```

Constraints always include:

```text
Safety
Compliance
Vehicle capacity
Driver eligibility
Pickup window
Drop window
Maximum ride duration
Maximum detour
Vehicle restrictions
Vendor restrictions
Configured company/site/process rules
```

**Safety and compliance always override cost and optimization.**

---

## 43.5 Process-independent clubbing example

```text
PUN01
│
├── VOICE01 ─┐
├── CHAT01  ─┼──→ CLUBBING ENGINE → ONE CAB
├── FIN01   ─┤
└── TECH01 ──┘
```

The trip must preserve every passenger's organizational dimensions.

```text
Trip
├── Passenger A
│   └── Process: VOICE01
├── Passenger B
│   └── Process: CHAT01
├── Passenger C
│   └── Process: FIN01
└── Passenger D
    └── Process: TECH01
```

---

## 43.6 Operational optimization and billing are independent

A clubbed vehicle may carry employees from multiple processes.

The system must not create separate physical trips merely because billing is separated.

Example:

```text
ONE PHYSICAL TRIP
│
├── VOICE01 passenger
├── CHAT01 passenger
├── FIN01 passenger
└── TECH01 passenger
```

The billing engine can still produce:

```text
VOICE01 → allocated cost
CHAT01  → allocated cost
FIN01   → allocated cost
TECH01  → allocated cost
```

Therefore:

> **Operational clubbing and financial allocation are separate decisions.**

---

# 43.7 Company billing modes

Each company must support:

```text
COMPANY
SITE
PROCESS
```

billing levels.

### Company billing

```text
CONC001
 ↓
One consolidated transport billing account
```

### Site billing

```text
CONC001
├── PUN01 → Billing Account A
├── MUM01 → Billing Account B
└── BLR01 → Billing Account C
```

### Process billing

```text
PUN01
├── VOICE01 → Billing Account A
├── CHAT01  → Billing Account B
└── FIN01   → Billing Account C
```

---

## 43.8 Billing configuration must be flexible

Do not force every company into one global billing model.

Support:

```text
Company Default
+
Site Override
+
Process Override
```

Example:

```text
Company:
COMPANY billing

PUN01:
SITE billing

MUM01:
PROCESS billing

BLR01:
SITE billing
```

Another company may use:

```text
Company:
SITE billing

PUN01:
PROCESS billing

MUM01:
SITE billing

BLR01:
PROCESS billing
```

---

## 43.9 Effective billing level

For every transport charge, determine:

```text
Company
↓
Site
↓
Process
```

and resolve the effective billing configuration.

Example:

```text
CONC001
Default = COMPANY

PUN01
Override = SITE

MUM01
Override = PROCESS

MUM01 / FIN01
Billing Account = FINANCE-001
```

The effective configuration must be visible to authorized users.

---

## 43.10 Billing must not alter operational clubbing

Example:

```text
PUN01

VOICE01 → Cost Centre A
FIN01   → Cost Centre B
```

They may still share:

```text
ONE CAB
```

The billing engine then allocates the physical trip cost.

---

## 43.11 Shared-trip cost allocation

A clubbed trip may have:

```text
Total Trip Cost = ₹1,200
```

The system supports configurable allocation methods:

```text
Equal Passenger Split
Passenger Count
Distance Based
Attributable KM
Seat Based
Weighted Allocation
Process Allocation
Cost Centre Allocation
Custom Allocation Rule
```

Example:

```text
VOICE01 → ₹300
CHAT01  → ₹300
FIN01   → ₹300
TECH01  → ₹300
```

or an attributable-distance calculation where passenger distances differ.

---

## 43.12 Billing account

Create/extend:

```text
BillingAccount
```

with:

```text
Company
Site
Process
Cost Centre
Business Unit
Project
Currency
Tax Configuration
Rate Card
Invoice Recipient
Payment Terms
Billing Mode
Effective From
Effective To
```

---

## 43.13 Billing dimensions

Transport charges must retain:

```text
Company Code
Site Code
LOB Code
Process Code
Department Code
Business Unit Code
Cost Centre Code
Project Code
Shift Code
Vendor
Vehicle
Trip
Booking
Passenger
```

This enables:

```text
Company cost
Site cost
Process cost
Shift cost
Cost-centre cost
Project cost
Vendor cost
Vehicle cost
```

without changing the physical trip structure.

---

## 43.14 Billing reports

The reporting engine must support:

```text
Company transport cost
Site transport cost
Process transport cost
Cross-process clubbing savings
Cost per employee
Cost per trip
Cost per KM
Cost per passenger
Cost per site
Cost per process
Vendor cost
Empty KM cost
Clubbing savings
```

---

## 43.15 Clubbing savings

Show:

```text
Without clubbing:
10 individual cabs
Estimated cost = ₹X

With clubbing:
6 cabs
Actual/estimated cost = ₹Y

Savings = ₹(X-Y)
```

Also report:

```text
Capacity utilization
Empty KM reduction
CO2 reduction
Average ride-time impact
SLA impact
```

---

## 43.16 Clubbing explanation

Every automated clubbing decision should be explainable.

Example:

```text
CLUBBED

Passengers:
4

Processes:
VOICE01
CHAT01
FIN01
TECH01

Reason:
Same site
Pickup difference: 8 min
Same direction
Capacity available: 4
Estimated detour: 9 min
Maximum allowed: 15 min
Estimated ride time: 71 min
Maximum allowed: 90 min
```

Rejected clubbing:

```text
NOT CLUBBED

Reason:
Estimated detour = 21 min
Maximum allowed = 15 min
```

---

## 43.17 Admin control

Transport Admin can configure whether cross-process clubbing is:

```text
Allowed
Not Allowed
Allowed only for selected processes
Allowed only for selected shifts
Allowed only for selected sites
Allowed only for selected transport types
```

Transport Sub-Admin and Coordinator inherit only their explicitly authorized operational scope/permissions.

They must never be able to expand their scope through clubbing configuration.

---

## 43.18 Scope and clubbing separation

Authorization scope determines **which records an administrator can operate**.

Clubbing rules determine **which passengers may be operationally grouped**.

These must remain separate.

Example:

A Sub-Admin may operate:

```text
PUN01 / VOICE01
```

but may not use a clubbing rule to gain access to:

```text
MUM01 / FIN01
```

If cross-process clubbing produces a trip involving multiple authorized scopes, the system must ensure the operator has appropriate permission for every affected record or route the action through an authorized workflow.

---

## 43.19 Cross-process reporting

Reports must preserve process-level attribution even when transport is clubbed.

Example:

```text
Physical Trips:
100

Clubbed Physical Trips:
40

Passenger Movements:
160

Process usage:
VOICE01 → 60
CHAT01  → 45
FIN01   → 35
TECH01  → 20
```

Do not count one physical trip as one process trip for every process without clearly distinguishing:

```text
Physical Trip
Passenger Movement
Allocated Cost
Process Usage
```

---

## 43.20 Billing and clubbing audit

Every material decision must record:

```text
Clubbing Rule Version
Billing Rule Version
Rate Card Version
Policy Version
Route Version
Optimization Version
Actor
Timestamp
Scope
Input
Decision
Allocation
```

Historical billing and reporting must remain reproducible.

---

## 43.21 Final conceptual model

```text
COMPANY
   │
   ├── SITE 001
   │     ├── PROCESS A
   │     ├── PROCESS B
   │     └── PROCESS C
   │
   ├── SITE 002
   │     ├── PROCESS A
   │     └── PROCESS D
   │
   └── SITE N
         └── MANY PROCESSES
```

Operationally:

```text
SITE
 ↓
ALL ELIGIBLE PROCESS DEMAND
 ↓
CLUBBING / OPTIMIZATION
 ↓
PHYSICAL TRIP
```

Financially:

```text
PHYSICAL TRIP
 ↓
PASSENGER / PROCESS ATTRIBUTION
 ↓
BILLING MODE
 ↓
COMPANY / SITE / PROCESS
 ↓
COST ALLOCATION
 ↓
INVOICE / REPORT
```

### Final rules

1. A company can have unlimited sites.
2. A site can contain multiple processes.
3. Processes within the same site can be clubbed.
4. Cross-process clubbing is configurable.
5. Process is not automatically a transport boundary.
6. Optimization should evaluate eligible demand across processes.
7. Safety and compliance override optimization.
8. Billing is independent from clubbing.
9. Billing can be company-wise, site-wise or process-wise.
10. Company defaults and site/process overrides are supported.
11. One physical trip can contain multiple processes.
12. Cost can be allocated back to each process/cost centre.
13. Historical route, rate, policy, clubbing and billing versions are immutable.
14. Clubbing must never expand administrator authorization scope.
15. Existing bookings must not be silently cancelled because of optimization or billing configuration changes.
16. All material clubbing and billing decisions are auditable.


---

# PHASE 44: DRIVER AVAILABILITY + MOBILE GPS + DYNAMIC DISPATCH + MULTI-ADMIN CONTROL

## 44.1 Purpose

MOVE-IN-SYNC must support a production-grade transport control model in which:

- Drivers may be employees, vendor-provided drivers, or freelancers/independent drivers.
- Drivers may work fixed shifts or flexible/freelance availability windows.
- A driver must explicitly mark themselves ACTIVE/AVAILABLE in the MOVE-IN-SYNC driver application before receiving new trips, unless an explicitly authorized operational override exists.
- Driver and vehicle location tracking uses mobile application GPS data transmitted over the device's available internet connection.
- Drivers can configure favourite geographic areas on a map.
- Favourite areas are soft dispatch preferences, never hard restrictions.
- Drivers can mark breaks, rest, feeling sleepy, fueling, vehicle checks, emergency, or other configured unavailable states.
- Transport Admins can reassign an entire trip to another driver/vehicle.
- Transport Admins can move one passenger, multiple passengers, or all passengers between cabs.
- Passenger movement must preserve historical trip and booking identity.
- Driver arrival is determined through configured GPS geofences around pickup/site/drop coordinates.
- Employee boarding is confirmed using configured one-time codes/OTP/QR mechanisms.
- Drivers can mark an employee as NO-SHOW after the configured evidence and waiting rules are satisfied.
- A confirmed passenger no-show immediately releases vehicle capacity and triggers dynamic re-optimization.
- Released capacity may be used to club/chain another eligible passenger where all safety, compliance, authorization, route, SLA, capacity, timing, and policy rules pass.
- Each company may have multiple Transport Admins and multiple internal transport roles.
- Only appropriately authorized administrators may create/delegate additional administrators.
- An administrator may never delegate authority outside their own authorized scope.
- Employee transport onboarding/offboarding is controlled by authorized transport administration and is separate from deleting the employee master record.

This phase integrates the previously defined scheduling, policy, multi-site, cross-process clubbing, GPS, no-show, safety, billing, and role-scope architecture.

---

## 44.2 Driver Types

The system must support at least:

```text
EMPLOYEE_DRIVER
VENDOR_DRIVER
FREELANCER_DRIVER
CONTRACT_DRIVER
TEMPORARY_DRIVER
```

The driver type must not change the fundamental safety, compliance, GPS, dispatch, or authorization rules.

Freelancers may have no fixed company shift but must still:

```text
LOGIN
  ↓
SELECT/CONFIRM AUTHORIZED ASSIGNMENT
  ↓
PASS ELIGIBILITY CHECKS
  ↓
GO ACTIVE
  ↓
BECOME DISPATCH ELIGIBLE
```

---

## 44.3 Driver Organizational Mapping

Every driver must be organizationally traceable.

Required dimensions:

```text
Company Code
Site Code
Process Code
```

Additional supported dimensions:

```text
Region Code
LOB Code
Department Code
Business Unit Code
Cost Centre Code
Project Code
Shift Code
Vendor Code
```

Driver assignments must be effective-dated where a driver can move between sites/processes.

Example:

```text
Driver D000145
Company: CONC001

Current assignments:
PUN01 / BPO / VOICE01
PUN01 / BPO / CHAT01
```

A driver may have multiple authorized site/process assignments where the company policy allows it.

Do not silently create organizational masters because of an invalid CSV or mobile selection.

---

## 44.4 Vendor Organizational Mapping

Every vendor must also be mapped to:

```text
Company Code
Site Code
Process Code
```

Vendor scope may cover:

- one site
- multiple sites
- one process
- multiple processes
- multiple site/process tuples

Scope must be represented as explicit authorized tuples where necessary.

Never interpret independent lists as an unrestricted Cartesian product.

Example:

```text
VALID:
CONC001 / PUN01 / VOICE01
CONC001 / PUN01 / CHAT01

NOT IMPLIED:
CONC001 / MUM01 / FIN01
```

unless explicitly authorized.

---

## 44.5 Driver Availability State Machine

Minimum driver availability states:

```text
OFFLINE
AVAILABLE
ACTIVE
ON_TRIP
ON_BREAK
SLEEPY
RESTING
FUELING
VEHICLE_CHECK
UNAVAILABLE
EMERGENCY
SUSPENDED
COMPLIANCE_BLOCKED
```

Operational dispatch eligibility must be explicit.

```text
AVAILABLE / ACTIVE
    ↓
Eligible for new dispatch

ON_TRIP
    ↓
Eligible only for compatible chained/reassigned work

ON_BREAK / SLEEPY / RESTING / UNAVAILABLE
    ↓
Not eligible for new dispatch

COMPLIANCE_BLOCKED / SUSPENDED
    ↓
Not dispatchable
```

The exact state transitions must be enforced server-side.

---

## 44.6 GO ACTIVE / GO OFFLINE

The driver application must provide an explicit:

```text
GO ACTIVE
GO OFFLINE
```

control.

Before allowing ACTIVE, the platform must validate:

```text
Authenticated driver
✓ Driver status active
✓ Company/site/process authorization
✓ Required documents valid
✓ Required training valid
✓ Vehicle valid
✓ Vehicle capacity valid
✓ Required inspection valid
✓ Required guard/safety conditions where applicable
✓ Device/application health
✓ GPS available or acceptable fallback
✓ Internet connectivity state
✓ Shift/availability policy
✓ No blocking suspension
```

A shift schedule defines expected availability; it does not automatically mean the driver is operationally ACTIVE.

A freelancer may become ACTIVE without a fixed shift if all applicable eligibility rules pass.

---

## 44.7 Fixed Shift Driver Availability

Example:

```text
Shift:
22:00–07:00

21:45
→ Driver reminder

22:00
→ Driver can GO ACTIVE

22:00–07:00
→ Dispatch eligible while ACTIVE

07:00
→ New dispatch normally blocked

Existing active trip
→ Continue safely until completion or authorized handover
```

Shift extensions must be explicitly recorded.

Do not silently continue assigning new trips outside the authorized availability window.

---

## 44.8 Break Management

Drivers without an active trip may select:

```text
MEAL_BREAK
PERSONAL_BREAK
REST
PRAYER_BREAK
FUELING
CHARGING
VEHICLE_CHECK
FEELING_SLEEPY
MEDICAL
EMERGENCY
OTHER
```

Each state must record:

```text
startedAt
endedAt
reason
driverId
companyCode
siteCode
processCode
GPS
deviceId
shiftId
```

Transport Admins can view active breaks within their authorized scope.

---

## 44.9 Feeling Sleepy / Fatigue Safety State

"Feeling Sleepy" must be a safety state, not a normal idle reason.

When a driver selects:

```text
FEELING_SLEEPY
```

the system must:

```text
Remove driver from new-trip dispatch
Record event
Start rest/fatigue timer
Notify configured control-room workflow
Evaluate current trip if any
Prevent unsafe continuation
Require configured return-to-duty confirmation
```

A driver who is currently transporting passengers cannot simply become SLEEPY and continue without the configured safety workflow.

The platform must never optimize cost at the expense of fatigue safety.

---

## 44.10 Driver Availability Ledger

Create an immutable/effective-dated availability history.

Suggested model:

```text
DriverAvailabilityEvent

id
tenantId
driverId
vendorId

companyCode
siteCode
processCode
shiftId

previousStatus
newStatus
reason

startedAt
endedAt

gpsLatitude
gpsLongitude
gpsAccuracy

deviceId
tripId

createdBy
createdAt
auditId
```

Required reporting:

```text
Scheduled hours
Actual active hours
Available hours
On-trip hours
Break hours
Sleepy/rest hours
Offline hours
Unavailable hours
Compliance blocked hours
```

---

## 44.11 Mobile GPS + Mobile Internet Tracking

Core live tracking must use:

```text
Mobile Application
    +
Device GPS
    +
Available Internet
```

The platform must not require dedicated GPS/telematics hardware for core tracking.

Dedicated telematics remains an optional provider/integration.

GPS is the location source.

Mobile internet is the transport channel for transmitting location data.

The application is responsible for collection, validation, local buffering, synchronization, and secure transmission.

---

## 44.12 GPS Location Payload

Minimum location payload:

```text
tripId
tenantId
driverId
deviceId

vehicleId

latitude
longitude
accuracy
altitude
speed
bearing

gpsTimestamp
receivedAt

sequenceNumber

networkType
batteryLevel where permitted

mockLocationIndicator where available
locationProvider
```

Use GPS timestamp for the actual observed location time.

Use server receipt time separately.

Do not overwrite historical GPS timestamps with server arrival timestamps.

---

## 44.13 GPS Tracking Frequency

Tracking frequency must be configurable by trip state.

Example defaults:

```text
ACTIVE TRIP:
5–10 seconds

DRIVER EN ROUTE:
10–30 seconds

IDLE / WAITING:
30–60 seconds

NON-TRIP:
No continuous tracking unless explicitly authorized
```

Company/site policy may configure permitted intervals within platform safety and battery limits.

---

## 44.14 Offline GPS Buffering

Mobile internet is not guaranteed.

The driver application must:

```text
Collect GPS
   ↓
Internet unavailable
   ↓
Encrypt and buffer location points locally
   ↓
Internet returns
   ↓
Upload buffered points
   ↓
Server validates sequence/timestamps
   ↓
Historical route reconstructed
```

Control Room must clearly distinguish:

```text
LIVE
STALE
OFFLINE
RECONNECTING
GPS ERROR
```

The UI must never display stale GPS as if it were live.

---

## 44.15 GPS Quality and Fraud Signals

The platform must evaluate:

```text
GPS accuracy
Timestamp age
Sequence continuity
Impossible speed
Teleportation/jump distance
Repeated coordinates
Device/driver mismatch
Device/vehicle mismatch
Mock-location indicators
Clock anomalies
Provider anomalies
```

Potential spoofing must create an investigation signal.

Do not automatically accuse or penalize a driver solely from a technical anomaly without the configured investigation workflow.

---

## 44.16 Driver Favourite Areas

Drivers may configure favourite areas from a map.

Supported interaction:

```text
Driver App
 → My Preferences
 → Favourite Areas
 → Add Area
 → Search / Pin / Draw Circle
 → Set Radius
 → Set Preference Type
 → Save
```

Preference types:

```text
PICKUP
DROP
CORRIDOR
GENERAL
```

Priority:

```text
PRIMARY
PREFERRED
SECONDARY
```

Optional strength:

```text
STRONG
NORMAL
FLEXIBLE
```

The stored representation should be geographic geometry, not merely a text label.

---

## 44.17 Favourite Area Dispatch Rule

Favourite areas are soft optimization preferences.

They must:

```text
Increase candidate ranking
Improve route alignment
Reduce unnecessary repositioning
Improve driver preference satisfaction
```

They must never:

```text
Override safety
Override female passenger safety
Override compliance
Override capacity
Override authorization
Override legal restrictions
Override work/rest/fatigue limits
```

A driver with no favourite areas configured is eligible for any otherwise-authorized trip.

---

## 44.18 Demand Expansion Beyond Favourite Areas

When demand increases, the optimizer may expand beyond preferred areas.

Demand expansion factors include:

```text
Unassigned trip count
Upcoming pickup deadlines
SLA risk
Available driver count
Site demand
Process demand
Shift demand
Vehicle availability
Safety-compatible coverage
Route compatibility
```

Driver preference ranking may be reduced as operational pressure increases.

The driver must still remain within all authorization and safety constraints.

Where appropriate, show:

```text
EXPANDED AREA ASSIGNMENT

Demand is currently high.
This assignment is outside your preferred area.
Additional coverage is required.
```

---

## 44.19 No Favourite Area

If a driver has:

```text
Favourite Areas = NONE
```

then there is no geographic preference score.

Dispatch evaluates the driver based on:

```text
Safety
Compliance
Authorization
Capacity
Availability
Route
ETA
SLA
Work/rest
Cost
Optimization
```

The driver may receive any eligible pickup or drop.

---

## 44.20 Intelligent Trip Chaining

A driver currently performing a trip may receive another trip when the new trip is operationally compatible.

Example:

```text
Current:
Office
 ↓
Hinjewadi
 ↓
Wakad

New pickup:
Wakad

New drop:
Office
```

The optimizer may chain:

```text
Current trip
 ↓
Compatible pickup
 ↓
New trip
```

Candidate evaluation must include:

```text
Current GPS
Current route
Current passengers
Current destination
New pickup
New destination
Pickup time window
ETA
Capacity
Detour
Additional ride time
SLA
Driver work/rest
Vehicle eligibility
Vendor rules
Site/process scope
Female passenger safety
Guard requirement
Clubbing policy
```

---

## 44.21 Cross-Process Chaining

Where Phase 43 permits cross-process clubbing:

```text
SITE PUN01
 ├── VOICE01
 ├── CHAT01
 └── FIN01

      ↓

Eligible demand pool
      ↓
Trip chaining / clubbing
```

Process membership must remain attached to each passenger movement and billing attribution.

Clubbing must never grant an administrator or driver unauthorized data access.

---

## 44.22 Complete Trip Reassignment

Transport Admins with the required permission can:

```text
Unassign driver
Unassign vehicle
Select another eligible driver
Select another eligible vehicle
Recalculate ETA
Recalculate route
Recalculate capacity
Confirm reassignment
```

The original Trip ID must remain unchanged.

Example:

```text
Trip MIS-105421

Original:
Driver D145
Vehicle MH12AB1234

New:
Driver D219
Vehicle MH12CD5678

Trip ID:
MIS-105421
```

Required audit:

```text
actor
timestamp
old driver
old vehicle
new driver
new vehicle
reason
scope
GPS
trip state
affected passengers
```

---

## 44.23 Single Passenger Reassignment

Transport Admin can select one employee within a trip and move that employee to another eligible cab.

Example:

```text
Cab A:
Employee A
Employee B
Employee C
Employee D

Move Employee D

Cab B:
Employee D
```

The platform must validate:

```text
Capacity
Route
Pickup
Drop
ETA
SLA
Driver
Vehicle
Site
Process
Clubbing policy
Female passenger safety
Guard requirements
Compliance
```

---

## 44.24 Multiple Passenger Reassignment

Transport Admin may select multiple passengers:

```text
Employee A
Employee C
Employee F
```

and move them as a group to another vehicle/trip.

The system must preview:

```text
New passenger count
New capacity utilization
ETA impact
Detour
Cost impact
Safety status
Guard status
Process impact
Billing impact
```

before confirmation.

---

## 44.25 Passenger Movement as a First-Class Record

Do not permanently bind passengers to one physical trip record.

Maintain:

```text
Physical Trip
Passenger Movement
Booking
Vehicle Assignment
Driver Assignment
Allocated Cost
```

Suggested model:

```text
PassengerMovement

id
employeeId
originalTripId
currentTripId

originalVehicleId
currentVehicleId

originalDriverId
currentDriverId

companyCode
siteCode
processCode

movedAt
movedBy
reason

transferOtp
transferStatus

createdAt
updatedAt
auditId
```

This also supports breakdown/replacement workflows.

---

## 44.26 Admin Reassignment Preview

Before manual reassignment:

```text
REASSIGNMENT PREVIEW

Current vehicle:
MH12AB1234

Passengers moved:
3

New vehicle:
MH12CD5678

Capacity:
5 / 6

Additional distance:
+1.4 km

ETA impact:
+3 minutes

Cost impact:
-₹420

Female safety:
PASS

Guard:
AVAILABLE

Process clubbing:
ALLOWED

SLA:
WITHIN LIMIT
```

Only after confirmation is the assignment changed.

---

## 44.27 Emergency Reassignment

Authorized Transport Admins may have an emergency override.

Emergency override requires:

```text
Reason
Actor
Timestamp
Scope
Affected passengers
Old assignment
New assignment
```

Emergency override must still enforce mandatory safety, legal, capacity, and compliance constraints unless a separately governed emergency policy explicitly defines a controlled exception.

---

## 44.28 Live Transport Control Center

The dashboard must continuously display current operational counts.

Minimum cards:

```text
AVAILABLE DRIVERS
ON TRIP
CAB ASSIGNED
WAITING FOR DISPATCH
DRIVER EN ROUTE
ARRIVED
BOARDING
IN TRANSIT
DROPPING
ON BREAK
SLEEPY
OFFLINE
COMPLIANCE BLOCKED
VEHICLE ISSUE
```

Example:

```text
AVAILABLE DRIVERS       42
ON TRIP                 67
CAB ASSIGNED             31
WAITING DISPATCH          9
ON BREAK                 11
SLEEPY                    2
OFFLINE                  18
COMPLIANCE BLOCKED        4
```

Counts must be live and tenant/scope aware.

---

## 44.29 Control Center Drilldown

Every count must drill into authorized records.

Example:

```text
AVAILABLE DRIVERS 42
 ↓
Company
 ↓
Site
 ↓
Process
 ↓
Driver
 ↓
Vehicle
 ↓
GPS
 ↓
Current assignment
```

The same dashboard must support universal filters:

```text
Company
Region
Site
LOB
Process
Department
BU
Cost Centre
Project
Shift
Vendor
Driver
Vehicle
Trip
Status
```

---

## 44.30 Trip Assignment Funnel

Control Room must display:

```text
BOOKED
 ↓
APPROVED
 ↓
WAITING FOR DISPATCH
 ↓
CAB ASSIGNED
 ↓
DRIVER ASSIGNED
 ↓
DRIVER EN ROUTE
 ↓
ARRIVED
 ↓
BOARDING
 ↓
STARTED
 ↓
IN TRANSIT
 ↓
DROPPING
 ↓
COMPLETED
```

Breakdown, replacement, no-show, cancellation, and passenger transfer remain auditable branches.

---

## 44.31 GPS Arrival Geofence

Cab arrival at a site/pickup/drop must be determined using geofencing.

Required inputs:

```text
Configured coordinate
Configured geofence radius
Vehicle GPS
GPS accuracy
Timestamp
```

Example:

```text
Site coordinate
+
150m geofence
+
vehicle enters radius
=
ARRIVED
```

Do not require exact latitude/longitude equality.

The event must be timestamped and auditable.

---

## 44.32 Employee Boarding OTP

After arrival:

```text
ARRIVED
 ↓
BOARDING
 ↓
Employee provides one-time code
 ↓
Server validates
 ↓
Employee = BOARDED
```

Supported policies:

```text
ALL_REQUIRED
MINIMUM_COUNT
CONFIGURED_THRESHOLD
ADMIN_OVERRIDE
```

Where policy is `ALL_REQUIRED`, all required passengers must provide valid one-time confirmation before the trip can start.

Where policy allows a threshold, the trip can start after the configured minimum and unresolved passengers follow the no-show workflow.

---

## 44.33 Trip Start

A trip can transition to STARTED only when the configured boarding requirements are satisfied.

Start event must record:

```text
Trip ID
Driver
Vehicle
GPS
Timestamp
Boarded employees
OTP verification result
Unresolved employees
Policy version
Actor/source
```

The driver application must not be able to fabricate a successful OTP.

OTP validation is server-side.

---

## 44.34 Driver No-Show for Employee

During pickup, driver can select an employee and choose:

```text
CALL EMPLOYEE
MARK NO-SHOW
```

No-show eligibility must require the configured policy, including where applicable:

```text
Vehicle inside pickup geofence
Waiting/grace period satisfied
Required call attempts
Call timing
Evidence capture
```

The existing no-show evidence requirements remain mandatory.

---

## 44.35 No-Show as a Capacity Release Event

A confirmed no-show is not merely a reporting event.

It must immediately create:

```text
PASSENGER_CAPACITY_RELEASED
```

Example:

```text
Vehicle capacity = 6
Confirmed passengers = 6
Employee F = NO-SHOW

Effective passenger load = 5
Released capacity = 1
```

The released seat becomes available to the dynamic dispatch optimizer.

---

## 44.36 Immediate Re-Optimization After No-Show

When capacity is released:

```text
NO_SHOW CONFIRMED
       ↓
RELEASE CAPACITY
       ↓
RECALCULATE CURRENT ROUTE
       ↓
FIND COMPATIBLE DEMAND
       ↓
CHECK SAFETY
       ↓
CHECK COMPLIANCE
       ↓
CHECK CAPACITY
       ↓
CHECK ETA / SLA
       ↓
CHECK PROCESS CLUBBING
       ↓
CHECK DRIVER WORK/REST
       ↓
CHECK COST
       ↓
ASSIGN IF BENEFICIAL
```

The engine must never add a passenger solely because a seat is empty.

---

## 44.37 Example: No-Show Creates Cost Optimization

Existing cab:

```text
Capacity: 6

Passengers:
A
B
C
D
E
F
```

F becomes a confirmed no-show.

New eligible demand:

```text
Employee X
Pickup near B
Drop near E
```

Optimization:

```text
Seat available       ✓
Same site             ✓
Cross-process allowed ✓
Pickup compatible     ✓
Drop compatible       ✓
Detour acceptable     ✓
ETA acceptable        ✓
SLA acceptable        ✓
Female safety         ✓
Guard requirement     ✓
Driver eligible       ✓
```

Result:

```text
Assign X to existing cab
```

instead of dispatching another cab.

---

## 44.38 Re-Optimization Events

The optimizer should be event-driven.

Minimum events:

```text
DRIVER_BECAME_ACTIVE
DRIVER_WENT_OFFLINE
DRIVER_STARTED_BREAK
DRIVER_MARKED_SLEEPY
TRIP_CREATED
TRIP_CANCELLED
PASSENGER_CANCELLED
PASSENGER_NO_SHOW
PASSENGER_BOARDED
PASSENGER_MOVED
DRIVER_DECLINED
VEHICLE_CHANGED
CAPACITY_CHANGED
TRIP_DELAYED
BREAKDOWN
REPLACEMENT_CREATED
GPS_ROUTE_CHANGED
DEMAND_SURGE
SLA_RISK_DETECTED
```

Every optimization decision must be reproducible from the relevant event and versioned inputs.

---

## 44.39 Dispatch Priority

The dispatch engine must prioritize:

```text
1. Safety
2. Female passenger safety
3. Legal/mandatory policy
4. Driver/vehicle compliance
5. Authorization/scope
6. Vehicle capacity
7. Driver availability/work/rest
8. Pickup/drop SLA
9. Route compatibility
10. Guard requirements
11. Passenger ride-time constraints
12. Clubbing compatibility
13. Driver preference
14. Utilization
15. Cost
16. Other optimization objectives
```

Cost must never override a higher-priority constraint.

---

## 44.40 Multi-Admin Company Model

Each company may have multiple transport administrators.

Do not model:

```text
Company → single transportAdminId
```

Instead:

```text
Company
 ├── Transport Admin A
 ├── Transport Admin B
 ├── Transport Admin C
 ├── Transport Sub-Admin A
 ├── Transport Coordinator A
 ├── Compliance
 ├── Fleet
 ├── Dispatch
 ├── Finance
 ├── Safety
 └── Reporting
```

Each user receives:

```text
Role
+
Permissions
+
Company
+
Scope
+
Effective dates
+
Delegation authority
```

---

## 44.41 Internal Transport Roles

Companies may configure multiple internal operational roles, including:

```text
TRANSPORT_ADMIN
TRANSPORT_SUB_ADMIN
TRANSPORT_COORDINATOR
DISPATCH_MANAGER
FLEET_MANAGER
TRANSPORT_COMPLIANCE
TRANSPORT_FINANCE
VENDOR_MANAGER
SAFETY_MANAGER
SITE_TRANSPORT_LEAD
PROCESS_TRANSPORT_LEAD
REPORTING_ADMIN
```

The platform must preserve the distinction between:

```text
Platform Internal Roles
Customer/Company Roles
Partner/Vendor Roles
```

No implicit role inheritance across domains.

---

## 44.42 Transport Admin Authority

Transport Admin is the highest normal customer transport-operational authority within the administrator's authorized scope.

Within permission/scope, the Transport Admin may:

```text
Onboard employee into transport
Offboard employee from transport
Suspend/reactivate transport access
Create/delegate authorized transport administrators
Manage schedules
Manage policies
Manage no-show rules
Manage clubbing rules
Manage routes
Manage driver availability
Assign drivers
Assign vehicles
Reassign trips
Reassign passengers
Manage vendors
View live GPS
Manage dispatch
View operational dashboards
Run/export authorized reports
```

Higher-risk functions remain governed by explicit permissions and platform rules.

---

## 44.43 Transport Sub-Admin

A Transport Sub-Admin receives delegated scope.

Example:

```text
Company:
CONC001

Sites:
PUN01
MUM01

Processes:
VOICE01
CHAT01
```

The Sub-Admin may operate only within that scope.

They cannot expand their own scope.

---

## 44.44 Transport Coordinator

Coordinator is operational rather than automatically administrative.

Example permissions:

```text
VIEW_LIVE_TRIPS
ASSIGN_DRIVER
ASSIGN_VEHICLE
REASSIGN_PASSENGER
MANAGE_DISPATCH
VIEW_DRIVER_STATUS
```

Policy-writing permissions must be explicit.

Coordinator cannot create a Transport Admin unless separately authorized.

---

## 44.45 Creating Another Administrator

Only a user with explicit delegation permission may create another administrator.

Creation workflow:

```text
Administration
 → Users & Roles
 → Create User
 → Select Role
 → Select Scope
 → Select Permissions
 → Select Effective Date
 → Access Preview
 → Confirm
```

The system must enforce:

```text
New Admin Scope ⊆ Creator Scope
```

and:

```text
New Admin Permissions ⊆ Creator Delegatable Permissions
```

unless a higher-authority governed workflow explicitly authorizes otherwise.

A Transport Coordinator or ordinary manager must not be able to create unrestricted administrators.

---

## 44.46 Access Preview Before Admin Creation

Display:

```text
USER
Amit Sharma

ROLE
Transport Sub-Admin

COMPANY
CONC001

SITES
PUN01, MUM01

PROCESSES
VOICE01, CHAT01

PERMISSIONS
Booking
Dispatch
Driver assignment
Passenger reassignment
No-show management
Live tracking
Reports

ACTIVE TRANSPORT HEADCOUNT
9,721
```

The creator must see exactly what the new administrator can access before confirming.

---

## 44.47 Administrator Scope Model

Authorization must remain:

```text
Identity
+
Authority Domain
+
Role
+
Permission
+
Tenant
+
Scope
+
Hierarchy where applicable
+
Policy
+
Data Classification
+
Resource
```

Do not infer authority merely from:

```text
Job title
Company membership
Manager relationship
Being able to see a record
Being a driver
Being a vendor
```

---

## 44.48 Employee Transport Onboarding

Authorized Transport Admins or explicitly delegated roles can onboard an employee into transport.

Required validation:

```text
Employee exists
Company Code valid
Site Code valid
Process Code valid
Assignment valid
Transport eligibility valid
Applicable policy valid
```

Onboarding must create/update transport eligibility without deleting or duplicating the employee master.

Status:

```text
TRANSPORT_ACTIVE
```

---

## 44.49 Employee Transport Offboarding

Authorized administrators can offboard an employee from transport.

Status becomes:

```text
TRANSPORT_INACTIVE
```

or configured suspended state.

Offboarding must not delete historical:

```text
Bookings
Trips
Passenger movements
OTP records where retained
No-show records
Expenses
Billing allocations
Audit records
```

---

## 44.50 Offboarding Impact Preview

Before offboarding:

```text
EMPLOYEE
Shubham

CURRENT STATUS
TRANSPORT_ACTIVE

UPCOMING BOOKINGS
3

ACTIVE/FUTURE TRIP PARTICIPATION
2

ACTION
Offboard from transport
```

The system must show the impact and route affected passenger movements through cancellation/reoptimization rules.

No historical data may be silently destroyed.

---

## 44.51 Onboard/Offboard Audit

Every status change records:

```text
Employee
Previous status
New status
Company Code
Site Code
Process Code
Reason
Effective date
Actor
Actor role
Actor scope
Timestamp
IP/device where applicable
Affected bookings/trips
Audit ID
```

---

## 44.52 Headcount Dashboard

Every authorized admin dashboard must show scoped transport headcount:

```text
Total employees
Transport active
Transport suspended
Transport inactive

Today's bookings
Active trips
Completed trips
No-shows
Pending dispatch
```

Drilldown:

```text
Company
 ↓
Region
 ↓
Site
 ↓
LOB
 ↓
Process
 ↓
Department
 ↓
BU
 ↓
Cost Centre
 ↓
Project
 ↓
Shift
 ↓
Employee
```

Counts must use the effective-dated organizational assignments.

---

## 44.53 Live Driver Availability Dashboard

Minimum dashboard:

```text
AVAILABLE             42
ACTIVE                 8
ON TRIP               67
CAB ASSIGNED           31
WAITING DISPATCH        9
ON BREAK               11
SLEEPY                  2
RESTING                 3
FUELING                 4
OFFLINE                18
COMPLIANCE BLOCKED      4
```

The exact states may be configured, but the system must retain a canonical operational state.

---

## 44.54 Driver Dashboard

Driver app must show:

```text
Current status
Current shift
Today's active time
Current trip
Next trip
Pickup
Drop
Passenger count
Vehicle
GPS status
Network status
Break controls
Sleepy/rest controls
Favourite areas
Trip accept/decline where configured
No-show workflow
OTP validation
Emergency/SOS
```

---

## 44.55 Transparent Assignment Explanation

Where practical, the driver and Control Room should see why a driver was selected.

Example:

```text
DRIVER SELECTED

✓ Active
✓ Compliant
✓ Vehicle available
✓ Capacity available
✓ Route compatible
✓ Pickup 7 min away
✓ Preferred area
✓ SLA compliant
```

Outside preference:

```text
✓ Safety passed
✓ Compliance passed
✓ Capacity passed
✓ SLA passed
⚠ Outside favourite area
⚠ High demand expansion active
```

---

## 44.56 Reassignment and Billing Independence

Changing a driver or moving a passenger must not silently change:

```text
Historical rate-card version
Billing mode
Process attribution
Company/site/process account
Contract
Invoice period
```

The system recalculates future operational cost where required while preserving historical financial facts.

---

## 44.57 Reassignment and Clubbing Independence

A passenger may move between cabs because of:

```text
No-show capacity release
Breakdown
SLA recovery
Driver shortage
Route optimization
Safety
Manual Transport Admin action
Cost optimization
```

This does not automatically change their billing attribution.

Operational movement and financial attribution remain separate.

---

## 44.58 Audit of Every Dispatch Decision

For every material automatic or manual dispatch decision, record:

```text
Trip
Passenger(s)
Driver
Vehicle
Company
Site
Process
Candidate drivers
Candidate vehicles
Eligibility results
Safety checks
Compliance checks
Capacity
ETA
Route
Detour
Driver preference
Clubbing rule
Cost estimate
Optimization version
Decision
Decision reason
Actor/system
Timestamp
```

---

## 44.59 Dispatch Explainability

The Control Room must support:

```text
Why was this driver selected?
Why was another driver rejected?
Why was this passenger moved?
Why was this cab not used?
Why was a trip not clubbed?
Why was a favourite-area preference overridden?
Why was a no-show seat reused?
Why was a new cab dispatched instead?
```

Reasons must be based on actual evaluated data, not fabricated AI explanations.

---

## 44.60 Data Model Additions

Recommended entities:

```text
DriverAvailabilityEvent
DriverShiftAssignment
DriverAreaPreference
DriverAreaPreferenceVersion
DriverDevice
DriverLocationPoint
DriverLocationSession
PassengerMovement
TripAssignmentHistory
VehicleAssignmentHistory
DispatchDecision
DispatchCandidateEvaluation
DispatchOptimizationRun
EmployeeTransportStatusHistory
AdminDelegation
AdminScopeAssignment
```

All relevant entities must include tenant isolation and organizational dimensions.

---

## 44.61 API Requirements

Examples:

```text
POST   /drivers/me/availability/active
POST   /drivers/me/availability/offline
POST   /drivers/me/availability/break
POST   /drivers/me/availability/sleepy
POST   /drivers/me/availability/rest
GET    /drivers/me/availability
GET    /drivers/me/favourite-areas
POST   /drivers/me/favourite-areas
PATCH  /drivers/me/favourite-areas/:id
DELETE /drivers/me/favourite-areas/:id

POST   /driver-locations
POST   /driver-locations/batch
GET    /trips/:id/live-location

POST   /trips/:id/reassign
POST   /trips/:id/unassign
POST   /trips/:id/passengers/move
POST   /trips/:id/passengers/move-bulk

POST   /trips/:id/arrive
POST   /trips/:id/start
POST   /trips/:id/passengers/:employeeId/no-show

POST   /employees/:id/transport/onboard
POST   /employees/:id/transport/offboard

POST   /admin-users
POST   /admin-users/:id/delegate
GET    /admin-users/:id/access-preview

GET    /control-room/driver-availability
GET    /control-room/dispatch-funnel
GET    /control-room/live-capacity
```

All endpoints must enforce server-side tenant, permission, scope, policy, and resource checks.

---

## 44.62 WebSocket Events

Minimum real-time events:

```text
driver.status.changed
driver.location.updated
driver.location.stale
driver.offline
driver.sleepy
driver.break.started
driver.break.ended

trip.assignment.created
trip.assignment.changed
trip.assignment.cancelled
trip.reassignment.created

passenger.boarded
passenger.no_show
passenger.moved

trip.arrived
trip.started
trip.completed

capacity.released
capacity.changed

dispatch.optimization.started
dispatch.optimization.completed
dispatch.candidate.rejected

safety.alert
compliance.block
```

Events must be tenant-aware and authorization-filtered.

---

## 44.63 No Client-Side Trust

The mobile application may request:

```text
GO ACTIVE
MARK NO-SHOW
ARRIVE
START
MOVE/TRANSFER
```

but the server must decide whether the action is valid.

Never trust:

```text
client-provided companyId
client-provided siteCode
client-provided processCode
client-provided role
client-provided permissions
client-provided trip status
client-provided GPS validity
client-provided OTP success
```

---

## 44.64 Security and Privacy

GPS is sensitive operational data.

Requirements:

```text
Encrypted in transit
Encrypted at rest where required
Tenant isolated
Scope controlled
Access audited
Retention configured
Location history access restricted
Driver consent/notice as required
Employee location tracking limited to authorized transport use cases
```

Do not collect continuous employee location merely because the employee installed the application.

---

## 44.65 Failure Handling

If GPS fails:

```text
Use last known location
Mark STALE
Attempt reconnection
Use configured fallback
Notify Control Room when threshold exceeded
```

If mobile internet fails:

```text
Buffer GPS
Show OFFLINE/RECONNECTING
Synchronize later
```

If dispatch optimization fails:

```text
Do not lose current valid assignments
Use deterministic fallback dispatch
Escalate to Control Room
```

If a driver application crashes:

```text
Preserve server-side trip identity
Detect stale heartbeat
Notify Control Room
Follow fallback/reassignment policy
```

---

## 44.66 Capacity Recovery Rule

Capacity can be recovered from:

```text
Passenger NO-SHOW
Passenger CANCELLATION
Passenger TRANSFER
Vehicle CAPACITY CHANGE
Passenger OFFBOARDING
Trip SPLIT
Trip COMPLETION
```

Every capacity release must trigger an eligibility-aware optimization event where applicable.

---

## 44.67 Dynamic Optimization Objective

The optimization engine should minimize:

```text
Safety violations
Compliance violations
SLA violations
Unnecessary vehicles
Empty KM
Detour
Excess ride time
Driver idle time
Driver overload
Vendor cost
Operational complexity
```

while maximizing:

```text
Safety
On-time performance
Vehicle utilization
Seat utilization
Driver utilization
Preferred-area satisfaction
Clubbing savings
Passenger service quality
```

Safety remains the hard upper-level constraint.

---

## 44.68 Acceptance Test — Driver Availability

```text
1. Create fixed-shift driver.
2. Driver cannot receive new trip before eligible shift/availability.
3. Driver GO ACTIVE.
4. Server validates compliance.
5. Driver becomes ACTIVE.
6. Eligible trips become dispatchable.
7. Driver starts break.
8. New trip is not assigned.
9. Driver selects FEELING SLEEPY.
10. Driver is removed from dispatch.
11. Driver completes configured rest workflow.
12. Driver GO ACTIVE again.
13. Driver becomes eligible.
```

---

## 44.69 Acceptance Test — Favourite Areas

```text
1. Driver adds primary favourite circle around Hinjewadi.
2. Driver adds preferred circle around Wakad.
3. Matching trip appears.
4. Driver receives higher preference score.
5. Trip outside preferred areas remains eligible.
6. Demand increases.
7. Engine expands candidate area.
8. Outside-area trip is assigned.
9. Safety/compliance constraints remain enforced.
10. Driver with no favourite areas receives any otherwise-authorized eligible trip.
```

---

## 44.70 Acceptance Test — Whole Trip Reassignment

```text
1. Create trip with driver A and vehicle A.
2. Transport Admin opens trip.
3. Admin selects Reassign Entire Trip.
4. System evaluates driver B and vehicle B.
5. Preview shows capacity, ETA, safety, compliance and cost.
6. Admin confirms.
7. Trip ID remains unchanged.
8. Driver B receives assignment.
9. Vehicle B receives assignment.
10. Driver A becomes eligible according to availability state.
11. Audit history records complete reassignment.
```

---

## 44.71 Acceptance Test — Passenger Reassignment

```text
1. Create trip with six passengers.
2. Select one passenger.
3. Move passenger to another eligible cab.
4. Validate capacity.
5. Validate route.
6. Validate ETA.
7. Validate safety.
8. Confirm.
9. Original passenger movement history remains.
10. New passenger movement is created.
11. Billing/process attribution remains correct.
12. Audit is complete.
```

---

## 44.72 Acceptance Test — OTP Start

```text
1. Cab reaches pickup geofence.
2. Trip becomes ARRIVED.
3. Trip enters BOARDING.
4. Employee provides valid OTP.
5. Server validates OTP.
6. Employee becomes BOARDED.
7. Configured boarding threshold is satisfied.
8. Trip becomes STARTED.
9. GPS tracking remains active.
```

---

## 44.73 Acceptance Test — No-Show Capacity Optimization

```text
1. Cab has six seats.
2. Five employees board.
3. One employee does not arrive.
4. Driver follows configured waiting/call evidence process.
5. Driver marks employee no-show.
6. Server validates no-show eligibility.
7. No-show is confirmed.
8. One seat becomes available.
9. PASSENGER_CAPACITY_RELEASED event is generated.
10. Optimizer finds a compatible nearby passenger.
11. Safety/compliance/capacity/SLA/process rules pass.
12. Passenger is assigned to existing cab.
13. No unnecessary new cab is dispatched.
14. Cost savings are recorded.
15. Complete audit trail is retained.
```

---

## 44.74 Acceptance Test — Multi-Admin Delegation

```text
1. Company has Transport Admin A.
2. Admin A has PUN01/VOICE01 scope.
3. Admin A creates Sub-Admin B.
4. System prevents B from receiving MUM01 scope.
5. System prevents B from receiving permissions A cannot delegate.
6. Access Preview shows exact scope.
7. B operates only within PUN01/VOICE01.
8. B cannot expand own scope.
9. Every delegation action is audited.
```

---

## 44.75 Acceptance Test — Employee Onboard/Offboard

```text
1. Employee exists in company.
2. Transport Admin opens employee.
3. Admin validates Company/Site/Process.
4. Admin onboards employee into transport.
5. Employee becomes TRANSPORT_ACTIVE.
6. Employee receives eligible transport services.
7. Admin initiates offboarding.
8. System displays future-trip impact.
9. Admin confirms.
10. Employee becomes TRANSPORT_INACTIVE.
11. Historical records remain intact.
12. Affected future trips are re-optimized according to policy.
13. Audit records actor, reason and impact.
```

---

## 44.76 Definition of Done

Phase 44 is complete only when all of the following are true:

```text
[ ] Freelancer drivers supported
[ ] Fixed-shift drivers supported
[ ] GO ACTIVE / GO OFFLINE implemented
[ ] Driver availability state machine implemented
[ ] Break/rest/sleepy workflows implemented
[ ] Driver/vendor Company/Site/Process mapping implemented
[ ] Mobile GPS tracking implemented
[ ] Mobile internet transmission implemented
[ ] Offline GPS buffering implemented
[ ] Live/stale/offline states implemented
[ ] GPS quality/spoofing signals implemented
[ ] Favourite map areas implemented
[ ] Favourite areas are soft preferences
[ ] Demand-based preference expansion implemented
[ ] No-preference drivers supported
[ ] Route-aware trip chaining implemented
[ ] Cross-process chaining integrated with Phase 43
[ ] Whole-trip reassignment implemented
[ ] Single-passenger reassignment implemented
[ ] Multi-passenger reassignment implemented
[ ] PassengerMovement history implemented
[ ] Live driver availability dashboard implemented
[ ] Live dispatch funnel implemented
[ ] GPS geofencing implemented
[ ] Employee OTP boarding implemented
[ ] Configurable trip-start threshold implemented
[ ] Driver no-show workflow implemented
[ ] No-show releases capacity
[ ] Capacity release triggers re-optimization
[ ] Cost optimization uses released capacity
[ ] Female passenger safety remains a hard constraint
[ ] Driver work/rest safety remains a hard constraint
[ ] Multiple Transport Admins supported
[ ] Multiple internal operational roles supported
[ ] Admin delegation implemented
[ ] Delegation cannot exceed creator scope
[ ] Access Preview implemented
[ ] Employee transport onboard implemented
[ ] Employee transport offboard implemented
[ ] Offboarding impact preview implemented
[ ] All changes audited
[ ] WebSocket live updates implemented
[ ] Tenant isolation tested
[ ] Scope isolation tested
[ ] Mobile offline behavior tested
[ ] Dispatch failure fallback tested
[ ] E2E tests pass
```

---

# 44.77 Final Operating Model

The complete operational flow is:

```text
EMPLOYEE DEMAND
      +
DRIVER AVAILABILITY
      +
VEHICLE AVAILABILITY
      +
LIVE GPS
      +
SCHEDULE
      +
SITE/PROCESS SCOPE
      +
CLUBBING RULES
      +
SAFETY RULES
      +
COMPLIANCE
      +
SLA
      +
DRIVER PREFERENCES
      ↓
INTELLIGENT DISPATCH
      ↓
DRIVER ASSIGNMENT
      ↓
VEHICLE ASSIGNMENT
      ↓
DRIVER GPS + MOBILE INTERNET
      ↓
GEOFENCE ARRIVAL
      ↓
EMPLOYEE OTP / BOARDING
      ↓
TRIP START
      ↓
LIVE ROUTE
      ↓
PASSENGER NO-SHOW / CANCELLATION / TRANSFER
      ↓
CAPACITY RELEASE
      ↓
REAL-TIME RE-OPTIMIZATION
      ↓
CLUB / CHAIN / REASSIGN
      ↓
TRIP COMPLETION
      ↓
BILLING + REPORTING + AUDIT
```

---

# 44.78 Absolute Rules Added by Phase 44

1. Driver GPS comes from the authorized mobile application.
2. Mobile internet is the normal transport channel for live GPS data.
3. Dedicated GPS hardware is optional, not mandatory for core tracking.
4. Drivers must be operationally ACTIVE/AVAILABLE before receiving new trips unless an explicit authorized exception applies.
5. A scheduled shift does not automatically mean the driver is ACTIVE.
6. Freelancers can become ACTIVE without a fixed shift when eligible.
7. Break, rest and sleepy states remove a driver from new-trip dispatch.
8. Feeling sleepy is a safety event.
9. Favourite areas are soft preferences, not hard restrictions.
10. No favourite area means no geographic preference.
11. Demand may expand a driver's assignment outside favourite areas.
12. Safety and compliance always override driver preference and cost.
13. Transport Admin can reassign an entire trip within authorized scope.
14. Transport Admin can move one or multiple passengers between cabs within authorized scope.
15. Passenger movement must preserve historical identity and auditability.
16. GPS arrival is geofence-based, not exact-coordinate based.
17. OTP validation is server-side.
18. Trip start obeys configured boarding thresholds.
19. Driver no-show requires configured evidence and waiting rules.
20. Confirmed no-show immediately releases capacity.
21. Released capacity triggers dynamic optimization.
22. Released capacity may be used for another compatible passenger only after all constraints pass.
23. No-show optimization must never violate female passenger safety.
24. No-show optimization must never violate driver work/rest or fatigue rules.
25. One company may have multiple Transport Admins.
26. One company may have multiple internal operational transport roles.
27. Only authorized users may create/delegate administrators.
28. A user cannot delegate beyond their own authorized scope or delegatable permissions.
29. Employee transport onboarding/offboarding is separate from employee deletion.
30. Offboarding must not destroy historical transport records.
31. Every availability, dispatch, reassignment, boarding, no-show, onboard, offboard and delegation action must be auditable.
32. Client-provided tenant/scope/role/permission/status values are never trusted.
33. Live dashboards must reflect current driver, vehicle and trip state within the configured real-time SLA.
34. Stale/offline GPS must be visibly distinguished from live GPS.
35. Cross-process clubbing remains subject to Phase 43 authorization and billing separation.
36. Operational optimization must never silently alter historical financial facts.
37. Feature completion requires automated and E2E tests, not merely UI implementation.




# PHASE 45+: FINAL PRODUCTION COMPLETION MASTER SPECIFICATION
## MOVE-IN-SYNC — ZERO-GAP / PRODUCTION-GO-LIVE EDITION

**Last Updated:** 2026-09-03  
**Version:** FINAL V5 — Production Completion / Zero-Gap Implementation Specification  
**Purpose:** This addendum converts the existing V4 master plan into an implementation-completion contract. V4 remains the functional source of truth; this section is the final engineering, API, UI, mobile, security, data, operations, deployment, compliance, testing and go-live gate.

> IMPORTANT: “100% production ready” means every specified capability is implemented, connected to real infrastructure/providers, authorized server-side, observable, tested, documented, deployable, recoverable and accepted through end-to-end tests. A model, DTO, controller, placeholder, mock, demo fallback or UI screen does NOT count as implemented.

---

## 45.0 ABSOLUTE COMPLETION RULE

The coding agent MUST NOT declare the platform production-ready based on code volume, number of Prisma models, number of endpoints, compilation success, or existence of frontend pages.

A capability is COMPLETE only if:

```text
Requirement
→ Data model
→ Migration
→ Repository/service
→ API contract
→ Authentication
→ Authorization
→ Scope enforcement
→ Validation
→ Business rules
→ State machine
→ Transaction/idempotency
→ Events/queues
→ Notifications where applicable
→ Frontend
→ Mobile where applicable
→ Provider integration where applicable
→ Audit trail
→ Error handling
→ Observability
→ Security tests
→ Integration tests
→ E2E test
→ Performance verification
→ Documentation
→ Production configuration
→ Rollback/recovery behavior
→ Acceptance evidence
```

No “TODO”, “FIXME”, `throw new Error("not implemented")`, fake repository, in-memory production state, hard-coded company, hard-coded role, fake GPS, fake notification, demo-only authentication, mock billing, or silent fallback may remain in a production execution path.

---

# 46. PRODUCTION READINESS CONTROL PLANE

Create a machine-readable production readiness registry.

Required entities:

- `Feature`
- `FeatureRequirement`
- `FeatureImplementation`
- `ApiContract`
- `Permission`
- `ScopeRule`
- `TestCase`
- `DeploymentRequirement`
- `IntegrationProvider`
- `ProductionGate`
- `Release`
- `ReleaseApproval`
- `MigrationRecord`
- `RollbackPlan`
- `Runbook`
- `KnownIssue`
- `Risk`
- `SecurityFinding`
- `ComplianceFinding`

Every feature must have:

```text
featureCode
name
domain
owner
severity
status
backendStatus
apiStatus
frontendStatus
mobileStatus
integrationStatus
securityStatus
testStatus
documentationStatus
observabilityStatus
productionStatus
evidenceLinks
lastVerifiedAt
verifiedBy
```

Allowed production status:

```text
NOT_STARTED
IN_PROGRESS
BLOCKED
READY_FOR_TEST
TESTED
READY_FOR_STAGING
STAGING_VERIFIED
READY_FOR_PRODUCTION
PRODUCTION_VERIFIED
ACCEPTED
```

No feature can become `ACCEPTED` without evidence.

---

# 47. FINAL REPOSITORY AUDIT

Before modifying functionality:

1. Inspect the complete monorepo.
2. Inventory every app/package/module.
3. Inventory every controller and route.
4. Inventory every service.
5. Inventory every Prisma model and enum.
6. Inventory every migration.
7. Inventory every guard/interceptor/filter/middleware.
8. Inventory every queue and scheduler.
9. Inventory every WebSocket event.
10. Inventory every frontend route.
11. Inventory every mobile screen.
12. Inventory every environment variable.
13. Inventory every external provider.
14. Inventory every test.
15. Inventory all demo/mock/fake code.
16. Inventory all TODO/FIXME/unimplemented branches.
17. Inventory all hard-coded tenant/role/company values.
18. Inventory all direct database queries bypassing authorization.
19. Inventory all background jobs.
20. Inventory all exports/downloads.
21. Inventory all secrets.
22. Inventory all Docker/Kubernetes manifests.
23. Inventory all CI/CD workflows.

Generate:

```text
docs/PRODUCTION_INVENTORY.md
docs/API_INVENTORY.md
docs/ROLE_MATRIX.md
docs/PERMISSION_MATRIX.md
docs/SCOPE_MATRIX.md
docs/DATA_CLASSIFICATION.md
docs/INTEGRATION_MATRIX.md
docs/ENVIRONMENT_MATRIX.md
docs/PRODUCTION_GAP_REGISTER.md
docs/RELEASE_READINESS.md
```

The gap register must be generated from actual repository inspection, not guessed.

---

# 48. FINAL API CONTRACT — NO MISSING OPERATIONAL API

The existing 444 endpoints must be audited. Do not blindly add duplicates.

For every business capability, ensure complete CRUD/action/query/event APIs.

## 48.1 Authentication

Required:

```text
POST   /api/v1/auth/login
POST   /api/v1/auth/logout
POST   /api/v1/auth/refresh
POST   /api/v1/auth/mfa/challenge
POST   /api/v1/auth/mfa/verify
POST   /api/v1/auth/mfa/enroll
POST   /api/v1/auth/mfa/disable
POST   /api/v1/auth/password/forgot
POST   /api/v1/auth/password/reset
POST   /api/v1/auth/password/change
GET    /api/v1/auth/me
GET    /api/v1/auth/sessions
DELETE /api/v1/auth/sessions/:id
POST   /api/v1/auth/sessions/revoke-all
GET    /api/v1/auth/devices
POST   /api/v1/auth/devices/:id/revoke
```

Required controls:

- access token expiry
- refresh token rotation
- refresh token revocation
- reuse detection
- MFA
- account lockout
- password policy
- device/session management
- suspicious-login detection
- login audit
- rate limiting
- CSRF protection where applicable
- secure cookies where applicable
- email verification
- invitation acceptance
- SSO
- SCIM
- API-key authentication

---

# 49. API KEY / DEVELOPER PLATFORM

Implement:

```text
POST   /api/v1/platform/api-keys
GET    /api/v1/platform/api-keys
GET    /api/v1/platform/api-keys/:id
PATCH  /api/v1/platform/api-keys/:id
DELETE /api/v1/platform/api-keys/:id
POST   /api/v1/platform/api-keys/:id/rotate
POST   /api/v1/platform/api-keys/:id/revoke
GET    /api/v1/platform/api-keys/:id/usage
GET    /api/v1/platform/api-keys/:id/audit
```

API keys require:

- hashed secret storage
- prefix identification
- expiration
- scopes
- tenant binding
- environment binding
- IP restrictions where configured
- rate limits
- rotation
- immediate revocation
- last-used metadata
- audit
- usage metering
- never display full secret after creation

---

# 50. ROLE / PROFILE / PORTAL ARCHITECTURE — FIX THE “ONE HOST SERVER SHOWS EVERYTHING” PROBLEM

The web application MUST NOT show one giant unrestricted dashboard.

Login flow:

```text
LOGIN
 ↓
IDENTITY
 ↓
PLATFORM OR TENANT CONTEXT
 ↓
ROLE ASSIGNMENTS
 ↓
PERMISSION RESOLUTION
 ↓
ACCESS SCOPE RESOLUTION
 ↓
PROFILE/PORTAL RESOLUTION
 ↓
AUTHORIZED NAVIGATION
 ↓
AUTHORIZED DASHBOARD
```

## 50.1 Profile types

Create actual seeded test profiles for every role:

### Internal

```text
SUPERADMIN
MOVE-IN-ADMIN
FINANCE_TEAM
PROJECT_MANAGER
PROJECT_COORDINATOR
PLATFORM_COMPLIANCE
SECURITY_ADMINISTRATOR
SUPPORT_ENGINEER
PLATFORM_AUDITOR
```

### Customer

```text
TRANSPORT_ADMIN
TRANSPORT_SUB_ADMIN
TRANSPORT_COORDINATOR
TRANSPORT_COMPLIANCE
DIRECTOR
SENIOR_MANAGER
MANAGER
ASSISTANT_MANAGER
TEAM_LEADER
EMPLOYEE
TRAINER
```

### Partner

```text
VENDOR_ADMIN
VENDOR_DISPATCHER
DRIVER
GUARD
```

## 50.2 Profile creation UI

Create:

```text
/admin/users
/admin/users/create
/admin/users/:id
/admin/users/:id/access
/admin/users/:id/access-preview
/admin/users/:id/delegations
/admin/users/:id/sessions
/admin/users/:id/audit
```

Creation wizard:

```text
Identity
→ User type
→ Company
→ Site
→ LOB
→ Process
→ Shift
→ Role
→ Permissions
→ Scope
→ Hierarchy visibility
→ Data visibility
→ Delegatable permissions
→ Approval authority
→ MFA requirement
→ Device requirement
→ Review
→ Access Preview
→ Create
```

Access Preview MUST show:

```text
Role
Permissions
Company
Sites
LOBs
Processes
Departments
Cost Centres
Projects
Shifts
Reporting hierarchy
Approval authority
Data classifications
Allowed actions
Denied actions
Effective headcount
Effective employees
Effective vehicles
Effective drivers
Effective trips
Effective financial data
```

The final Create button MUST be disabled if:

- scope exceeds creator authority
- permission exceeds creator delegation
- role is incompatible with user type
- required MFA is missing
- approval authority is invalid
- tenant context is missing
- conflicting assignments exist

---

# 51. ROLE-SPECIFIC PORTALS

Implement separate route groups and layouts.

```text
/platform/*
/transport/*
/operations/*
/finance/*
/manager/*
/employee/*
/vendor/*
/driver/*
/guard/*
/support/*
/compliance/*
/security/*
/auditor/*
```

No portal may depend solely on frontend hiding.

Server authorization is mandatory for every API.

---

# 52. SUPERADMIN PORTAL

Required pages:

```text
Overview
Companies
Company lifecycle
Subscriptions
Plans
Pricing
Currencies
Platform billing
Usage
Revenue
Internal users
Platform roles
Feature flags
Rollouts
Release management
Security
Break-glass
Compliance
Support tickets
System health
Queues
Jobs
Audit
Data governance
Storage
Providers
Integrations
API keys
Webhooks
Developer portal
Status page
Backups
DR
Incident management
Configuration
```

SUPERADMIN must still obey internal authorization boundaries. SUPERADMIN is not a reason to remove audit, confirmation, MFA, or break-glass controls.

---

# 53. CUSTOMER TRANSPORT ADMIN PORTAL

Required:

```text
Dashboard
Employees
Transport eligibility
Onboard/offboard
Bookings
Approvals
Schedules
Routes
Clubbing
Dispatch
Control Room
Trips
Drivers
Vehicles
Guards
Compliance
No-show
Breakdown
SOS
Incidents
Vendors
Expenses
Billing
Reports
Notifications
Policies
Organization
Roles
Admins
Access Preview
Audit
Settings
```

---

# 54. TRANSPORT SUB-ADMIN

Same UI shell, but only authorized scope.

Every API must resolve:

```text
creatorScope ∩ assignedScope ∩ requestedResourceScope
```

Never:

```text
requestedScope
```

alone.

---

# 55. COORDINATOR PORTAL

Operational:

```text
Bookings
Approvals where delegated
Schedule
Dispatch
Driver availability
Vehicle availability
Trips
No-show
Replacement
Reassignment
Control room
Notifications
Reports
```

Policy editing only when explicit permission exists.

---

# 56. COMPLIANCE PORTAL

Required:

```text
Driver compliance
Vehicle compliance
Document expiry
Blocked resources
Compliance rules
Exceptions
Waivers
Audits
Compliance score
Evidence
Incident links
Dispatch blocks
Override review
```

Compliance can block unsafe dispatch but cannot silently grant unrelated administrative authority.

---

# 57. MANAGEMENT PORTALS

Director / Senior Manager / Manager / Assistant Manager / Team Leader:

- hierarchy-scoped employee view
- booking on behalf
- cancellation/reschedule where permitted
- approvals
- team transport status
- trip visibility
- expense approvals where authorized
- reports
- no access to unrelated company/site/process data

Hierarchy does not equal unrestricted transport administration.

---

# 58. EMPLOYEE PORTAL

Required:

```text
Home
My profile
Transport eligibility
My bookings
Book transport
Recurring booking
Cancel
Reschedule
Trip tracking
Driver details when policy permits
Pickup/drop
OTP/QR
Notifications
No-show history
Appeal
Expenses
SOS
Emergency contacts
Preferences
Privacy/consent
Data requests
Support
```

---

# 59. DRIVER PORTAL

Required:

```text
Login
Availability
GO ACTIVE
GO OFFLINE
Break
Rest
Sleepy
Fueling
Vehicle check
Emergency
Assigned trips
Trip detail
Navigation
Pickup
Arrive
Boarding
OTP/QR
Passenger list
No-show
Breakdown
Replacement
Trip completion
GPS status
Network status
Favourite areas
Shift/work hours
Compliance
Vehicle
Documents
Incident
SOS
Notifications
Profile
```

A shift assignment MUST NOT automatically make the driver ACTIVE.

---

# 60. GUARD PORTAL

Required:

```text
Login
Assigned shift
Trip/vehicle verification
Passenger/security checks
Boarding support
Incident
Emergency
SOS
Compliance
Handover
Audit
```

---

# 61. VENDOR PORTAL

Required:

```text
Vendor dashboard
Drivers
Vehicles
Documents
Compliance
Availability
Assignments
Trip execution
Breakdown
Replacement
Invoices
SLA
Performance
Disputes
Payments
Reports
Users
Audit
```

Vendor access is restricted to vendor-owned/authorized resources.

---

# 62. EMPLOYEE TRANSPORT ONBOARDING / OFFBOARDING

Required APIs:

```text
POST /api/v1/employees/:id/transport/onboard
POST /api/v1/employees/:id/transport/offboard
GET  /api/v1/employees/:id/transport/impact-preview
POST /api/v1/employees/:id/transport/eligibility
GET  /api/v1/employees/:id/transport/status
```

Onboarding validates:

```text
employee exists
company active
site active
process active
shift valid
transport policy
eligibility
required documents
consent
manager/hierarchy
scope
```

Offboarding must show future-trip impact before confirmation.

Historical bookings, trips, invoices and audit records remain intact.

---

# 63. EMPLOYEE CSV IMPORT — PRODUCTION GRADE

Required pipeline:

```text
UPLOAD
→ FILE SCAN
→ MIME VALIDATION
→ TEMPLATE VALIDATION
→ HEADER VALIDATION
→ ROW VALIDATION
→ MASTER CODE VALIDATION
→ DUPLICATE DETECTION
→ HIERARCHY VALIDATION
→ MANAGER VALIDATION
→ TRANSPORT ELIGIBILITY VALIDATION
→ PREVIEW
→ ERROR REPORT
→ APPROVAL
→ IMPORT
→ POST-IMPORT VALIDATION
→ AUDIT
```

Never silently create a company/site/process because a CSV contains an unknown code.

Provide:

- dry run
- downloadable error file
- row-level errors
- partial failure handling
- idempotency
- import rollback strategy
- import audit
- duplicate policy
- effective dates

---

# 64. DRIVER AVAILABILITY — PRODUCTION STATE MACHINE

Allowed transitions must be explicitly defined.

```text
OFFLINE
→ AVAILABLE
→ ACTIVE
→ ON_TRIP
→ ON_BREAK
→ RESTING
→ SLEEPY
→ FUELING
→ VEHICLE_CHECK
→ UNAVAILABLE
→ EMERGENCY
→ SUSPENDED
→ COMPLIANCE_BLOCKED
```

Do not allow arbitrary client state mutation.

Every transition records:

```text
driver
previousState
newState
timestamp
location if permitted
device
reason
source
actor
trip
shift
policy
```

---

# 65. MOBILE GPS — PRODUCTION IMPLEMENTATION

Mobile GPS is core.

Required:

- permission request
- consent
- consent withdrawal
- foreground tracking
- background tracking subject to OS/platform policy
- configurable interval
- accuracy
- altitude where available
- speed
- heading
- battery
- network state
- device timestamp
- server timestamp
- sequence number
- offline queue
- replay protection
- duplicate protection
- compression/batching
- stale detection
- spoofing signals
- impossible movement detection
- device binding
- secure transport

GPS statuses:

```text
LIVE
DEGRADED
STALE
OFFLINE
REVOKED
INVALID
SUSPICIOUS
```

Never display stale location as live.

---

# 66. GPS INGESTION APIs

Ensure complete contracts:

```text
POST /api/v1/driver-locations
POST /api/v1/driver-locations/batch
GET  /api/v1/drivers/:id/location
GET  /api/v1/trips/:id/live-location
GET  /api/v1/trips/:id/location-history
POST /api/v1/gps/consent
GET  /api/v1/gps/consent
DELETE /api/v1/gps/consent
GET  /api/v1/gps/devices
POST /api/v1/gps/devices/:id/revoke
```

Server validates:

- authenticated driver/device
- assigned trip
- tenant
- timestamp
- sequence
- coordinates
- accuracy
- impossible velocity
- duplicate
- replay
- consent
- device status

---

# 67. DRIVER FAVOURITE AREAS

Required:

```text
GET    /api/v1/drivers/me/favourite-areas
POST   /api/v1/drivers/me/favourite-areas
PATCH  /api/v1/drivers/me/favourite-areas/:id
DELETE /api/v1/drivers/me/favourite-areas/:id
```

Rules:

- soft preference only
- never hard restriction
- demand can expand outside preferred area
- no favourite area means no geographic preference
- dispatch cannot reject an otherwise safe/compliant assignment merely because it is outside preference

---

# 68. DISPATCH ENGINE — FINAL AUTHORITY ORDER

Optimization priority:

```text
1 Safety
2 Female passenger safety
3 Legal requirements
4 Policy hard constraints
5 Compliance
6 Authorization
7 Vehicle capacity
8 Driver work/rest limits
9 Passenger SLA
10 Route feasibility
11 Guard requirements
12 Ride time
13 Clubbing eligibility
14 Driver preference
15 Utilization
16 Cost
```

Cost can never override safety.

Preference can never override compliance.

AI can recommend but cannot bypass hard constraints.

---

# 69. DISPATCH APIs

Ensure complete:

```text
POST /api/v1/dispatch/preview
POST /api/v1/dispatch/optimize
POST /api/v1/dispatch/assign
POST /api/v1/dispatch/assign-bulk
POST /api/v1/dispatch/reassign
POST /api/v1/dispatch/unassign
POST /api/v1/dispatch/emergency-reassign
GET  /api/v1/dispatch/candidates
GET  /api/v1/dispatch/funnel
GET  /api/v1/dispatch/availability
GET  /api/v1/dispatch/conflicts
GET  /api/v1/dispatch/audit
```

Every dispatch action must support:

- authorization
- scope
- capacity validation
- compliance validation
- driver availability
- vehicle availability
- work/rest
- route feasibility
- female safety
- guard requirement
- SLA
- idempotency
- transaction
- audit

---

# 70. REAL-TIME RE-OPTIMIZATION

Triggers:

```text
driver becomes active
driver becomes unavailable
driver becomes sleepy
vehicle breakdown
vehicle capacity change
passenger cancellation
confirmed no-show
new booking
booking modification
route disruption
GPS deviation
SLA breach
compliance block
driver reassignment
passenger movement
trip delay
emergency
```

Optimization must be event-driven and idempotent.

Never run two conflicting dispatch decisions concurrently for the same trip.

Use distributed locks / transactional ownership / optimistic concurrency as appropriate.

---

# 71. PASSENGER MOVEMENT

Required first-class entity:

```text
PassengerMovement
```

Record:

```text
originalTrip
sourceTrip
destinationTrip
employee
reason
requestedBy
approvedBy
timestamp
sourceStop
destinationStop
sourceVehicle
destinationVehicle
sourceDriver
destinationDriver
policyVersion
costImpact
safetyImpact
```

Never rewrite historical TripPassenger rows in a way that destroys history.

---

# 72. BOARDING

Required:

```text
POST /api/v1/trips/:id/arrive
POST /api/v1/trips/:id/start
POST /api/v1/trips/:id/passengers/:employeeId/verify-boarding
POST /api/v1/trips/:id/passengers/:employeeId/no-show
POST /api/v1/trips/:id/complete
```

Boarding must support:

- OTP
- QR where configured
- offline-safe verification
- replay protection
- timestamp
- driver identity
- passenger identity
- trip identity
- location where permitted
- audit

Never trust client `otpVerified=true`.

---

# 73. NO-SHOW ENGINE

Complete:

```text
arrival
→ geofence
→ waiting timer
→ contact attempts
→ call-gap rule
→ supervisor escalation
→ evidence
→ no-show decision
→ capacity release
→ reoptimization
→ appeal
→ decision
→ ban if policy requires
```

No-show evidence is immutable.

No-show policy must resolve:

```text
company
→ site
→ LOB
→ process
→ shift
```

with most-specific-wins resolution.

---

# 74. BREAKDOWN / REPLACEMENT

Required behavior:

```text
BREAKDOWN
→ preserve original trip ID
→ freeze unsafe execution
→ notify
→ find eligible replacement
→ verify vehicle
→ verify driver
→ transfer passengers
→ OTP/QR transfer if required
→ resume
→ preserve history
→ continue billing lineage
```

Trip identity must not be destroyed.

---

# 75. SOS / INCIDENT MANAGEMENT

SOS must support:

```text
employee SOS
driver SOS
guard SOS
control room SOS
vehicle emergency
medical emergency
security emergency
fire
accident
```

Cascade:

```text
SOS
→ alert control room
→ configured emergency contacts
→ configured internal escalation
→ incident creation
→ location
→ trip context
→ passenger context
→ acknowledgement
→ response
→ resolution
→ post-incident review
```

All emergency actions are audited.

---

# 76. ROUTING PROVIDER ABSTRACTION

Create provider interface:

```text
geocode
reverseGeocode
route
distanceMatrix
eta
mapMatch
traffic
navigationUrl
```

Providers:

```text
PRIMARY
SECONDARY
FALLBACK
```

Provider failures must trigger circuit breaker and fallback.

Never make the whole platform depend on one map provider.

---

# 77. REAL ML / OPTIMIZATION

Replace placeholder/Haversine-only production behavior.

ML service must expose versioned APIs:

```text
POST /api/v1/optimize/route
POST /api/v1/optimize/dispatch
POST /api/v1/forecast/demand
POST /api/v1/anomaly/cost
POST /api/v1/anomaly/gps
POST /api/v1/safety/score
POST /api/v1/carbon/calculate
GET  /api/v1/models
GET  /api/v1/models/:id
```

AI/ML output must contain:

```text
modelVersion
inputVersion
timestamp
confidence
constraintsApplied
explanation
fallbackUsed
```

ML failure must fall back safely to deterministic rules.

AI never has authority to perform unrestricted actions.

---

# 78. AI COPILOT GOVERNANCE

AI must:

- receive only authorized context
- enforce tenant scope
- enforce role permissions
- mask restricted data
- cite source records where applicable
- distinguish fact from recommendation
- never invent records
- never invent policies
- never invent financial numbers
- never expose secrets
- never expose another tenant
- never approve high-risk actions automatically
- require explicit confirmation for mutations
- audit tool calls
- log model/provider/version
- support provider fallback
- support rate limits and usage limits

Tool execution must call the same authorization layer as normal APIs.

---

# 79. BILLING — TRANSPORT

Implement complete:

```text
rate cards
rate versions
trip cost snapshots
cost allocation
company billing
site billing
process billing
vendor billing
invoice generation
invoice submission
invoice validation
reconciliation
credit/debit notes
SLA penalties
tax
GST
currency
rounding
maker-checker
approval
payment status
budget vs actual
forecast
anomaly detection
```

Historical billing must never change because current configuration changes.

---

# 80. FINANCIAL LEDGER

Financial records require immutable ledger semantics.

Never calculate historical invoice totals using today's rate card.

Store:

```text
rateCardVersion
policyVersion
currency
taxVersion
calculationVersion
allocationVersion
sourceTrip
sourceBooking
sourceVendor
```

Every financial mutation requires audit.

---

# 81. EXPENSE ENGINE

Complete:

```text
draft
submitted
manager_review
director_review
approved
rejected
disputed
reimbursement_pending
reimbursed
cancelled
```

Required:

- receipt
- OCR
- duplicate detection
- policy validation
- approval limits
- delegation
- audit
- reimbursement export
- finance integration
- dispute
- analytics

---

# 82. SAAS PLATFORM BILLING

Implement:

```text
plans
features
entitlements
subscription
trial
upgrade
downgrade
pause
resume
cancel
renewal
usage
overage
invoice
credit note
tax
currency
payment
webhook
retry
dunning
grace period
suspension
reactivation
```

Plan enforcement must happen server-side.

Do not rely on UI to hide unavailable features.

---

# 83. MULTI-CURRENCY / TAX

Support:

- company base currency
- invoice currency
- rate source
- effective exchange rate
- exchange-rate timestamp
- tax jurisdiction
- tax registration
- tax type
- tax rate version
- rounding policy

GST/tax configuration must be validated by qualified human/legal/accounting review before production use.

---

# 84. NOTIFICATION PROVIDER ABSTRACTION

Implement provider interfaces:

```text
EmailProvider
SmsProvider
PushProvider
WhatsAppProvider
InAppProvider
```

Required:

```text
template
localization
preference
consent
delivery
retry
dead-letter
provider status
delivery receipt
deduplication
rate limit
fallback
audit
```

Notifications must not leak PII beyond intended recipients.

---

# 85. INTEGRATION HUB

Create normalized integration layer.

Supported categories:

```text
HRMS
SSO
SCIM
Email
SMS
WhatsApp
Push
Maps
Storage
Payments
Accounting
Payroll
KYC
Tax
Webhooks
BI
```

Each connector must support:

```text
credentials
health
test connection
sync
retry
rate limits
mapping
field transformation
error queue
dead-letter
audit
version
enable/disable
```

HRMS remains optional and must not become hidden operational authority.

---

# 86. WEBHOOK PLATFORM

Implement:

```text
endpoint registration
secret
event selection
signature
delivery
retry
backoff
dead-letter
replay
delivery history
test event
disable after repeated failures
```

Webhook signatures must be verified.

No sensitive data should be sent unnecessarily.

---

# 87. STORAGE / DOCUMENT VAULT

Support pluggable storage:

```text
S3-compatible
Google Drive
Azure Blob where configured
```

Database stores metadata and authorization.

Storage object access requires short-lived authorized URLs or server-mediated access.

Never expose permanent raw storage links for sensitive documents.

Required:

- encryption
- antivirus/malware scan
- MIME validation
- file-size limits
- checksum
- versioning
- retention
- legal hold
- deletion
- audit
- access logs

---

# 88. KYC / COMPLIANCE DOCUMENTS

Required:

```text
upload
verify
reject
request replacement
expire
renew
supersede
archive
legal hold
audit
```

Document type taxonomy must be configurable by country/company/vendor/driver/vehicle.

---

# 89. DATA GOVERNANCE

Classify:

```text
PUBLIC
INTERNAL
CONFIDENTIAL
PERSONAL
SENSITIVE_PERSONAL
FINANCIAL
SECURITY_SENSITIVE
LOCATION
HIGHLY_RESTRICTED
```

Every endpoint and report must define data classification.

Mask:

- phone
- email
- government IDs
- bank information
- KYC
- GPS history
- emergency information

based on role and policy.

---

# 90. PRIVACY

Implement:

```text
consent
GPS consent
privacy notice
data access request
data correction request
deletion request
restriction request where applicable
retention
legal hold
export
audit
```

Deletion must respect:

- legal hold
- financial retention
- audit requirements
- safety evidence
- statutory retention

Do not physically delete records where law or audit policy requires retention; use controlled anonymization where appropriate.

---

# 91. TENANT ISOLATION

Production must use defense in depth:

```text
application authorization
+
scope resolver
+
database tenant context
+
PostgreSQL RLS where applicable
+
cache key tenant isolation
+
queue tenant context
+
search index isolation/filtering
+
object storage tenant isolation
+
export authorization
```

Every automated test suite must include cross-tenant attack tests.

---

# 92. AUTHORIZATION ENGINE

Authorization decision must consider:

```text
identity
authority domain
role
permission
tenant
scope
hierarchy
policy
data classification
resource ownership
resource state
delegation
time validity
```

Create a centralized:

```text
AuthorizationService
AccessScopeResolver
PermissionResolver
PolicyResolver
DataVisibilityResolver
```

No controller should independently invent authorization logic.

---

# 93. CACHE SECURITY

Every cache key must contain required tenant/security context.

Never cache:

```text
user permissions
dashboard metrics
employee data
GPS
reports
financial data
```

without authorization context.

Cache invalidation is mandatory after:

- role change
- permission change
- scope change
- delegation revoke
- user disable
- tenant disable
- policy change
- sensitive data change

---

# 94. DATABASE PRODUCTION HARDENING

Required:

- PostgreSQL production cluster
- connection pooling
- migration locking
- migration backup
- PITR
- read replica strategy where required
- indexes
- query plans
- slow-query monitoring
- vacuum/autovacuum configuration
- deadlock monitoring
- transaction boundaries
- optimistic concurrency
- uniqueness constraints
- foreign keys
- check constraints where useful
- soft-delete policy where required
- immutable audit tables
- partitioning for high-volume GPS/event data where appropriate

No destructive migration without verified backup and rollback strategy.

---

# 95. IDEMPOTENCY

All mutation APIs vulnerable to retries must support idempotency.

Examples:

```text
booking
approval
dispatch
reassignment
boarding
no-show
invoice
payment
expense
import
notification
webhook
GPS batch
```

Use:

```text
Idempotency-Key
tenant
actor
operation
request hash
response
status
expiry
```

Duplicate requests must not duplicate business effects.

---

# 96. STATE MACHINES

Explicit state machines required for:

```text
User
Invitation
Employee onboarding
Employee transport
Booking
Approval
Trip
Trip passenger
Driver
Driver availability
Vehicle
Vehicle compliance
Dispatch
Boarding
No-show
Breakdown
Incident
SOS
Expense
Invoice
Vendor invoice
Subscription
Payment
Webhook
Notification
Export job
Report
Feature flag
Release
```

Invalid transitions must return structured errors.

---

# 97. CONCURRENCY / RACE CONDITION CONTROL

Test and prevent:

```text
two admins assigning same vehicle
two admins assigning same driver
two optimizers changing same trip
two boarding attempts
duplicate no-show
duplicate breakdown
duplicate invoice
simultaneous cancellation
simultaneous offboarding
capacity race
driver status race
GPS sequence race
```

Use database transactions and locking/optimistic concurrency as appropriate.

---

# 98. CONTROL ROOM

Real-time dashboard:

```text
active trips
unassigned demand
available drivers
busy drivers
offline drivers
sleepy drivers
vehicles
capacity
GPS stale
GPS offline
no-show queue
breakdown queue
SOS
compliance blocks
SLA risks
reassignment queue
optimization events
```

Drill down:

```text
KPI
→ record
→ trip
→ passenger
→ driver
→ vehicle
→ audit
```

Every action is permission-checked.

---

# 99. REPORTING PLATFORM

Implement governed report architecture:

```text
data source
→ authorization
→ filters
→ scope
→ columns
→ calculated fields
→ grouping
→ sorting
→ aggregation
→ masking
→ output
```

Formats:

```text
XLSX
CSV
PDF
JSON
PRINT
```

Large exports are asynchronous.

Every export has:

```text
requester
scope
filters
data cutoff
report version
policy version
rate version where relevant
query hash
file checksum
expiration
download audit
```

---

# 100. DASHBOARD WIDGET SECURITY

Never use:

```text
frontend role === "ADMIN"
```

as security.

Every widget must call authorized APIs.

Dashboard configuration must be permission-aware.

Scheduled report authorization must be re-evaluated at execution time, not only at creation time.

---

# 101. SEARCH

Global search must support authorization-aware:

```text
employees
bookings
trips
drivers
vehicles
vendors
invoices
incidents
documents
reports
audit
```

Search results must never reveal existence of unauthorized resources.

---

# 102. NOTIFICATION CENTER

Each user needs:

```text
unread count
read/unread
archive
preferences
channel preferences
quiet hours
critical override
delivery history
```

---

# 103. MOBILE APPLICATION PRODUCTION

Build actual mobile applications, not browser simulations.

Minimum:

```text
Employee app
Driver app
Guard app where required
```

Production requirements:

- secure authentication
- biometric unlock where supported
- device binding
- encrypted local storage
- encrypted offline queue
- certificate/public-key pinning where appropriate
- secure logging
- no secrets in app bundle
- token rotation
- remote logout
- app version enforcement
- minimum supported OS
- crash reporting
- analytics without unnecessary PII
- deep links
- push notifications
- background GPS handling
- offline sync
- conflict resolution

---

# 104. OFFLINE-FIRST DRIVER EXECUTION

Driver app must remain useful during temporary internet loss.

Offline actions may include only explicitly safe operations.

Queue:

```text
location
arrival
boarding verification
trip progress
no-show evidence
breakdown
inspection
availability transition
```

Each queued action has:

```text
localId
sequence
createdAt
deviceId
signature
payloadHash
syncStatus
retryCount
```

Server resolves duplicates and conflicts.

Never allow offline mode to bypass authorization.

---

# 105. ACCESSIBILITY

Web and mobile must meet a production accessibility target appropriate to the deployment, including:

- keyboard navigation
- screen reader labels
- focus management
- sufficient contrast
- form error accessibility
- semantic controls
- touch target sizing
- accessible charts
- accessible maps where possible
- reduced motion
- localization-ready layouts

---

# 106. INTERNATIONALIZATION

Support:

```text
language
timezone
date format
time format
currency
number format
address format
phone format
tax terminology
```

No hard-coded user-facing strings.

---

# 107. OBSERVABILITY

Implement:

```text
structured logs
metrics
distributed tracing
request IDs
tenant IDs
user IDs where appropriate
job IDs
trip IDs
correlation IDs
```

Never log:

```text
passwords
tokens
API secrets
full government IDs
full payment credentials
private document contents
raw OTPs
```

---

# 108. METRICS

Minimum:

```text
API latency
API errors
DB latency
DB connections
Redis latency
queue depth
queue failures
job duration
GPS ingestion rate
GPS stale rate
WebSocket connections
dispatch latency
optimization latency
booking rate
no-show rate
breakdown rate
notification delivery
invoice generation
payment failures
mobile crash rate
login failures
authorization denials
```

---

# 109. ALERTING

Create alerts for:

```text
API outage
high 5xx
latency breach
DB saturation
queue backlog
GPS ingestion failure
WebSocket instability
dispatch failure
optimizer failure
notification provider failure
payment failure
backup failure
security anomaly
cross-tenant authorization failure
certificate expiry
storage failure
```

Each alert needs:

```text
severity
owner
runbook
escalation
acknowledgement
resolution
postmortem requirement where applicable
```

---

# 110. BACKUP / RESTORE / DISASTER RECOVERY

Define and test:

```text
RPO
RTO
backup frequency
retention
offsite copy
encryption
restore procedure
database restore
object restore
queue recovery
cache recovery
search rebuild
DNS/failover
secrets recovery
```

A backup is NOT considered valid until a restore test succeeds.

Perform scheduled restore drills.

---

# 111. HIGH AVAILABILITY

Production must avoid single points of failure where required by SLA:

```text
API replicas
worker replicas
Redis strategy
PostgreSQL HA
load balancer
ingress
storage
message queues
monitoring
DNS
```

Define graceful degradation for:

```text
map provider unavailable
SMS unavailable
email unavailable
push unavailable
ML unavailable
Redis unavailable
Elasticsearch unavailable
GPS unavailable
payment provider unavailable
HRMS unavailable
```

---

# 112. SECURITY HARDENING

Implement:

- dependency scanning
- SAST
- secret scanning
- container scanning
- DAST
- API security testing
- OWASP testing
- rate limiting
- brute-force protection
- SSRF protections
- file upload protections
- SQL injection protections
- XSS protections
- CSRF protections where relevant
- secure headers
- CSP
- TLS
- HSTS
- secure cookies
- session revocation
- least privilege
- admin MFA
- break-glass
- audit logging

---

# 113. PENETRATION TEST

Before production:

```text
external penetration test
authenticated application test
API test
mobile test
tenant isolation test
authorization test
file upload test
GPS privacy test
SSO/SCIM test
webhook test
payment/billing test
```

All critical/high findings must be closed or formally accepted by authorized security leadership.

---

# 114. SECRET MANAGEMENT

No production secret in:

```text
source code
Git history
Docker image
frontend bundle
mobile bundle
logs
test fixtures
README
```

Use a production secrets manager.

Rotate:

```text
DB
JWT
encryption
API keys
provider credentials
webhook secrets
storage credentials
```

---

# 115. ENCRYPTION

At minimum:

```text
TLS in transit
database encryption at rest
object storage encryption
backup encryption
secret-manager encryption
sensitive field encryption where appropriate
```

Encryption keys require rotation strategy and recovery procedures.

---

# 116. AUDIT LOGGING

Audit every high-value action:

```text
login
logout
MFA
role change
permission change
scope change
admin creation
delegation
booking
approval
dispatch
reassignment
boarding
no-show
breakdown
SOS
incident
employee onboard/offboard
driver status
vehicle status
billing
invoice
payment
export
document access
data request
deletion
break-glass
feature flag
release
configuration
```

Audit entries are append-only and protected from ordinary modification.

---

# 117. RELEASE MANAGEMENT

Every production release requires:

```text
version
commit SHA
build artifact
migration plan
rollback plan
test results
security result
dependency result
configuration diff
feature flags
owner
approver
deployment timestamp
```

Use:

```text
DEV
→ TEST
→ STAGING
→ CANARY
→ GENERAL
```

where infrastructure supports it.

---

# 118. DATABASE MIGRATION SAFETY

Use expand/contract migration patterns for breaking schema changes.

Never:

```text
drop column
rename critical column
change enum destructively
delete financial data
```

without compatibility strategy.

Every migration must be:

- reproducible
- versioned
- tested
- idempotent where applicable
- backed up
- observable

---

# 119. FEATURE FLAGS

Flags require:

```text
global
platform
company
site
role
percentage
allowlist
denylist
environment
```

Each flag needs:

```text
owner
reason
createdAt
expiry
audit
rollback
```

No stale flags indefinitely.

---

# 120. PRODUCTION CONFIGURATION CENTER

Centralize:

```text
company settings
site settings
process settings
shift settings
transport policy
booking policy
no-show policy
clubbing policy
dispatch policy
safety policy
GPS policy
retention
notifications
billing
tax
localization
feature flags
```

All effective-dated and versioned where business history depends on them.

---

# 121. POLICY RESOLUTION ENGINE

Every policy lookup must use:

```text
company
→ site
→ LOB
→ process
→ shift
```

Most-specific-wins.

No module may hard-code its own inheritance rules.

Expose:

```text
GET /api/v1/policies/resolve
POST /api/v1/policies/simulate
```

Policy simulator must show:

```text
input
matching policies
selected policy
overrides
effective values
reason
version
```

---

# 122. SCHEDULING ENGINE

Support:

```text
1:00
1:30
2:00
2:15
2:20
2:45
3:00
custom intervals
```

No restriction to standard shift slots.

Publishing:

```text
DRAFT
→ VALIDATING
→ APPROVED
→ PUBLISHED
→ ACTIVE
→ EXPIRED
→ ARCHIVED
```

Schedule changes show impact before publication.

---

# 123. CLUBBING ENGINE

Cross-process clubbing within authorized operational boundary.

Supported:

```text
SAME_PROCESS_ONLY
SAME_SITE_ANY_PROCESS
SAME_LOB_ANY_PROCESS
CROSS_LOB_ALLOWED
CUSTOM_SCOPE
```

Eligibility:

```text
timing
direction
distance
detour
capacity
ride time
safety
gender policy
compliance
guard
authorization
SLA
vehicle
driver
route
```

Clubbing must never grant administrative access to another process.

---

# 124. VENDOR-LESS / “OTHERS” DRIVER GROUP

Support:

```text
VENDOR
OTHERS
```

For OTHERS:

- no fake vendor required
- still traceable
- billing category configured
- compliance remains mandatory
- driver identity remains auditable
- payment/billing recipient configurable
- historical attribution preserved

---

# 125. TRAINING / SANDBOX

Provide:

```text
company practice mode
SUPERADMIN release sandbox
synthetic data
isolated database/schema
fake notifications
fake payment provider
fake GPS
test users
reset environment
```

Sandbox must NEVER share production secrets or real sensitive customer data.

---

# 126. SUPPORT / INCIDENT MANAGEMENT

Create platform support ticketing:

```text
ticket
priority
severity
tenant
requester
assignment
SLA
comments
attachments
status
escalation
resolution
audit
```

Support access:

- time-limited
- least privilege
- explicit approval where required
- fully audited
- automatically expires

---

# 127. STATUS / HEALTH

Provide:

```text
/health/live
/health/ready
/health/dependencies
/health/version
/status
```

Health checks must distinguish:

```text
application alive
application ready
database ready
queue ready
cache ready
storage ready
provider status
```

---

# 128. API DOCUMENTATION

Generate versioned OpenAPI documentation.

Every endpoint must document:

```text
purpose
authentication
permission
scope
request
response
errors
idempotency
rate limit
audit
events
examples
```

Never expose internal secrets or unrestricted schemas.

---

# 129. API VERSIONING

Use:

```text
/api/v1
```

with compatibility policy.

Breaking changes require:

```text
new version
migration period
deprecation date
documentation
consumer notification
```

---

# 130. ERROR CONTRACT

Standardize:

```json
{
  "code": "RESOURCE_NOT_AUTHORIZED",
  "message": "You are not authorized to access this resource.",
  "requestId": "...",
  "details": {},
  "timestamp": "..."
}
```

Never return sensitive authorization details that reveal protected resources.

---

# 131. RATE LIMITS

Define by:

```text
anonymous
user
role
tenant
API key
endpoint
mutation
GPS
export
AI
webhook
```

Do not rate-limit safety-critical operations so aggressively that emergency behavior becomes unsafe.

---

# 132. FILE UPLOAD SECURITY

Every upload:

```text
authentication
authorization
tenant check
size check
MIME check
extension check
content scan
checksum
storage isolation
metadata
audit
retention
```

Never trust client MIME type.

---

# 133. PERFORMANCE TARGETS

At minimum verify:

```text
p95 standard API < 200ms where feasible
GPS ingestion < 100ms target
dispatch operation < 500ms target
10,000 WebSocket connections stable
500 GPS events/sec sustained
100 dispatch operations/minute
large export asynchronous
no unbounded memory growth
```

Run tests at realistic production concurrency, not just local development.

---

# 134. FRONTEND PERFORMANCE

Implement:

- pagination
- virtualized large lists
- debounced search
- query caching
- code splitting
- lazy loading
- image optimization
- error boundaries
- retry UI
- offline indication where appropriate
- skeleton states
- empty states
- permission-denied states
- session-expired handling

---

# 135. MOBILE PERFORMANCE

Measure:

```text
startup
memory
battery impact
GPS battery cost
offline queue growth
network retries
crash-free sessions
background behavior
large passenger lists
map performance
```

---

# 136. TEST PYRAMID

Target:

```text
Unit: ≥80% meaningful business logic
Integration: all critical workflows
Contract: all public APIs
Security: all authorization boundaries
E2E: all critical journeys
Load: stated production targets
Chaos/failure: critical provider failures
Migration: every production migration
Mobile: device/network matrix
Accessibility: critical flows
```

Do not game coverage with meaningless tests.

---

# 137. REQUIRED E2E JOURNEYS

### Journey A — New company

```text
SUPERADMIN
→ create company
→ plan
→ site
→ organization
→ policies
→ admin
→ access preview
→ MFA
→ login
→ dashboard
```

### Journey B — Employee

```text
import
→ validate
→ onboard
→ consent
→ saved pickup
→ booking
→ approval
→ dispatch
→ notification
→ trip
→ boarding
→ completion
```

### Journey C — Driver

```text
login
→ device bind
→ compliance
→ GO ACTIVE
→ receive assignment
→ navigation
→ arrive
→ board
→ GPS
→ complete
→ GO OFFLINE
```

### Journey D — No-show

```text
arrival
→ wait
→ calls
→ evidence
→ no-show
→ capacity release
→ reoptimization
→ audit
→ appeal
→ decision
```

### Journey E — Breakdown

```text
breakdown
→ SOS/incident
→ replacement
→ vehicle validation
→ passenger transfer
→ resume
→ completion
→ billing lineage
```

### Journey F — Multi-process clubbing

```text
site A
→ process 1 passenger
→ process 2 passenger
→ eligibility
→ club
→ allocate cost
→ separate reporting
```

### Journey G — Admin delegation

```text
Transport Admin
→ create Sub-Admin
→ choose scope
→ access preview
→ create
→ sub-admin login
→ authorized action
→ unauthorized action denied
→ revoke
→ cached access invalidated
```

### Journey H — Offboarding

```text
employee
→ future trips
→ impact preview
→ offboard
→ reoptimization
→ history preserved
→ audit
```

### Journey I — Billing

```text
trip
→ immutable rate snapshot
→ cost allocation
→ vendor invoice
→ reconciliation
→ approval
→ payment
→ report
```

### Journey J — SaaS

```text
company
→ subscription
→ usage
→ invoice
→ tax
→ payment
→ renewal
→ failure
→ grace period
→ suspension
→ reactivation
```

---

# 138. SECURITY ACCEPTANCE MATRIX

Must pass:

```text
Company A → Company B employee: DENY
Company A → Company B trip: DENY
Company A → Company B invoice: DENY
Sub-admin → outside site: DENY
Manager → unrelated department: DENY
Employee → another employee expense: DENY
Driver → another driver's trip: DENY
Vendor → another vendor: DENY
Auditor → mutation: DENY
Support → expired access: DENY
Revoked token → API: DENY
Revoked API key → API: DENY
Expired delegation → API: DENY
Forged companyId → API: DENY
Forged role → API: DENY
Forged permission → API: DENY
Forged OTP success → API: DENY
Forged GPS validity → API: DENY
Unauthorized export → DENY
Unauthorized scheduled report → DENY
AI unauthorized tool call → DENY
Background job outside scope → DENY
Cached unauthorized record → DENY
```

---

# 139. DATA INTEGRITY ACCEPTANCE

Verify:

```text
capacity never negative
capacity never exceeded
trip cannot complete before start
boarding cannot occur without valid trip
no-show cannot occur before arrival/wait rules
driver cannot receive trip while suspended
driver cannot exceed work/rest limits
vehicle cannot be dispatched while blocked
invoice cannot use mutable historical rate
deleted user does not destroy financial history
offboarded employee does not erase historical trips
breakdown preserves trip identity
reassignment preserves passenger history
```

---

# 140. OBSERVABILITY ACCEPTANCE

Every critical transaction must be traceable:

```text
request
→ authorization
→ database transaction
→ event
→ queue
→ worker
→ provider
→ notification
→ audit
```

Use correlation IDs.

---

# 141. FAILURE INJECTION TESTS

Simulate:

```text
DB unavailable
Redis unavailable
queue unavailable
ML unavailable
map provider unavailable
SMS unavailable
email unavailable
push unavailable
payment unavailable
storage unavailable
network loss
mobile app crash
GPS permission revoked
GPS spoofing
duplicate request
out-of-order GPS
worker crash
deployment during active trip
database migration failure
```

Expected behavior must be documented and tested.

---

# 142. DEPLOYMENT

Production deployment must include:

```text
container image
image signing where available
registry
Kubernetes manifests/Helm
secrets
config
TLS
ingress
autoscaling
health checks
resource limits
pod disruption budget
rolling deployment
canary
rollback
database migration job
worker deployment
scheduler deployment
monitoring
logging
backup
```

Never run development mode in production.

---

# 143. ENVIRONMENTS

Separate:

```text
local
development
test
staging
production
sandbox
```

Each environment has independent:

```text
database
Redis
storage
provider credentials
JWT/secrets
webhooks
queues
monitoring
```

No production database in local/dev.

---

# 144. PRODUCTION ENVIRONMENT CHECKLIST

Before go-live:

```text
[ ] production database
[ ] migrations
[ ] seed only approved reference data
[ ] no demo data
[ ] no mock services
[ ] real notifications
[ ] real storage
[ ] real map provider
[ ] real payment provider
[ ] secrets manager
[ ] TLS
[ ] backups
[ ] restore tested
[ ] monitoring
[ ] alerting
[ ] CI/CD
[ ] rollback
[ ] WAF/rate limiting where applicable
[ ] security scan
[ ] penetration test
[ ] load test
[ ] E2E
[ ] mobile release
[ ] app signing
[ ] support process
[ ] incident process
[ ] privacy review
[ ] tax/legal review
```

---

# 145. NO DEMO MODE IN PRODUCTION

Production startup MUST fail fast if:

```text
DEMO_MODE=true
NODE_ENV=development
missing DB
missing secrets
missing required provider
unsafe CORS
weak JWT secret
missing encryption key
```

Do not silently switch to memory storage.

---

# 146. REAL PROFILE DEMO / TEST USERS

Provide a non-production seed command:

```text
npm run seed:profiles
```

Creates one isolated test company and test identities:

```text
superadmin@test
transport.admin@test
transport.subadmin@test
coordinator@test
compliance@test
director@test
manager@test
employee@test
vendor.admin@test
driver@test
guard@test
finance@test
support@test
auditor@test
```

Credentials must be generated/reset securely and MUST NOT be hard-coded into production.

The UI must allow selecting authorized tenant/profile only in development/sandbox.

Production must derive identity from actual login/session.

---

# 147. PROFILE SWITCHER RULE

A development-only profile switcher may exist.

It must:

```text
only run in non-production
display "DEVELOPMENT ONLY"
never alter real authorization
never impersonate without audit
never bypass authentication
```

Production impersonation, if required for support, must use audited break-glass/impersonation with:

```text
approval
reason
target
scope
expiration
banner
audit
automatic termination
```

---

# 148. CI/CD GATES

Pipeline must fail on:

```text
TypeScript errors
lint errors
unit test failure
integration test failure
contract test failure
security scan failure
secret scan
dependency critical vulnerability
container critical vulnerability
migration validation failure
OpenAPI validation failure
build failure
E2E failure
```

Production deployment cannot be manually forced around mandatory gates without an explicit emergency procedure and audit.

---

# 149. QUALITY GATES

Required:

```text
0 TypeScript errors
0 build errors
0 lint errors
0 critical security findings
0 known cross-tenant failures
0 failing critical E2E tests
0 unresolved P0/P1 production defects
0 missing production secrets
0 missing required providers
0 untested backup
0 untested rollback
0 untested migration
```

P2/P3 issues require documented risk acceptance before launch.

---

# 150. DOCUMENTATION

Create and maintain:

```text
README.md
ARCHITECTURE.md
SECURITY.md
PRIVACY.md
API.md
OPENAPI.md
DEPLOYMENT.md
OPERATIONS.md
RUNBOOKS.md
DR.md
BACKUP_RESTORE.md
INCIDENT_RESPONSE.md
ROLE_MATRIX.md
PERMISSION_MATRIX.md
SCOPE_MATRIX.md
DATA_DICTIONARY.md
REPORT_CATALOG.md
EVENT_CATALOG.md
QUEUE_CATALOG.md
INTEGRATION_CATALOG.md
MOBILE_RELEASE.md
ADMIN_GUIDE.md
EMPLOYEE_GUIDE.md
DRIVER_GUIDE.md
VENDOR_GUIDE.md
```

---

# 151. CUSTOMER ONBOARDING

Production company creation wizard:

```text
Company
→ legal details
→ tax
→ currency
→ timezone
→ sites
→ hierarchy
→ policies
→ roles
→ admins
→ vendors
→ drivers
→ vehicles
→ notification providers
→ storage
→ integrations
→ billing
→ compliance
→ test
→ activate
```

Activation must run readiness checks.

---

# 152. TENANT LIFECYCLE

Support:

```text
PROVISIONING
ACTIVE
SUSPENDED
GRACE
DEACTIVATING
DEACTIVATED
ARCHIVED
```

Suspension must have explicit rules for:

- login
- booking
- dispatch
- trip execution
- billing
- exports
- APIs
- historical access

Never unexpectedly stop an active safety-critical trip.

---

# 153. GLOBAL CONFIGURATION SAFETY

Any global change must show:

```text
current
new
affected tenants
affected sites
affected processes
affected trips
affected financial records
effective date
rollback
```

---

# 154. ADMIN CHANGE CONTROL

High-risk changes require:

```text
reason
impact preview
maker
checker
approval
effective time
audit
rollback
```

Applicable to:

```text
billing
tax
safety
dispatch
capacity
no-show
retention
roles
permissions
scope
data deletion
platform pricing
feature rollout
```

---

# 155. FINANCIAL SEPARATION

Platform billing and customer transport billing remain separate domains.

No Finance Team access to operational data unless explicitly assigned.

No Transport Admin access to platform revenue unless explicitly assigned.

---

# 156. SECURITY / SUPPORT SEPARATION

Security Administrator:

```text
security
MFA
sessions
break-glass
audit
vulnerability
```

Support Engineer:

```text
tickets
diagnostics
approved temporary access
```

Neither role automatically receives unrestricted transport or financial authority.

---

# 157. AUDITOR

Auditor:

```text
read-only
audit
reports
compliance evidence
release evidence
```

Auditor cannot mutate business data.

---

# 158. GLOBAL SEARCH AUTHORIZATION

Search query execution must happen against an authorized query scope.

Never:

```text
SELECT * FROM employees
```

then filter in frontend.

Always apply authorization at query/data-access layer.

---

# 159. EXPORT AUTHORIZATION

Export worker receives a signed job context:

```text
tenant
actor
permission
scope
query
filters
data classification
expiration
```

Worker revalidates authorization before data generation.

---

# 160. BACKGROUND JOB AUTHORIZATION

Every queue message includes:

```text
tenant
actor/source
authorization context where applicable
resource
operation
version
correlationId
```

Workers must revalidate resource ownership/scope before mutation.

---

# 161. SCHEDULER AUTHORIZATION

Scheduled jobs must not assume the authorization of the user who created them forever.

At execution:

```text
current policy
current scope
current recipient
current tenant
current report permissions
current data classification
```

must be checked.

---

# 162. EVENT BUS

Define canonical domain events:

```text
EmployeeOnboarded
EmployeeOffboarded
BookingCreated
BookingApproved
BookingCancelled
TripDispatched
TripReassigned
DriverAvailabilityChanged
DriverLocationReceived
PassengerBoarded
PassengerNoShow
CapacityReleased
OptimizationRequested
OptimizationCompleted
BreakdownReported
ReplacementAssigned
SOSCreated
IncidentCreated
InvoiceCreated
InvoiceReconciled
PaymentReceived
SubscriptionChanged
NotificationDelivered
DocumentExpired
ComplianceBlocked
```

Events must be versioned.

---

# 163. OUTBOX PATTERN

Use transactional outbox for critical domain events.

Database transaction:

```text
business mutation
+
outbox event
```

Worker publishes event.

Consumers are idempotent.

---

# 164. QUEUE DESIGN

Queues at minimum:

```text
notifications
gps
dispatch
optimization
reports
exports
imports
billing
reconciliation
compliance
webhooks
storage
ai
scheduled-jobs
```

Every queue:

```text
retry
backoff
dead-letter
visibility
metrics
alerting
idempotency
```

---

# 165. WEB SOCKET SECURITY

Every connection validates:

```text
JWT/session
tenant
role
scope
subscription target
```

A client must not subscribe to another company's trip channel.

Server filters events.

---

# 166. WEB SOCKET RESILIENCE

Support:

```text
reconnect
backoff
heartbeat
stale connection detection
event sequence
missed-event recovery
authorization recheck
tenant isolation
```

---

# 167. GPS PRIVACY

GPS retention must be policy-controlled.

Support:

```text
live tracking
historical tracking
retention period
masking
access audit
deletion/anonymization
legal hold
consent withdrawal
```

---

# 168. LEGAL / COMPLIANCE GATE

Production cannot be declared complete without appropriate human review for:

```text
privacy
GPS tracking
employment law
transport regulation
tax/GST
payments
document retention
KYC/AML where applicable
data residency where applicable
SMS/WhatsApp consent
terms
customer contracts
```

Engineering cannot self-certify legal compliance.

---

# 169. PRODUCTION SLOs

Define per deployment:

```text
availability
API latency
GPS freshness
dispatch latency
notification delivery
RPO
RTO
mobile crash-free rate
```

Each SLO has:

```text
measurement
threshold
owner
alert
report
```

---

# 170. INCIDENT RESPONSE

Severity:

```text
SEV0 catastrophic
SEV1 critical
SEV2 major
SEV3 minor
```

Each has:

```text
detection
acknowledgement
communication
mitigation
recovery
postmortem
corrective action
```

---

# 171. ROLLBACK

Every release must have a tested rollback.

Rollback must cover:

```text
application
configuration
feature flags
database compatibility
mobile API compatibility
queues
workers
```

Never blindly roll database backwards if data migration is irreversible.

Use forward-fix where appropriate.

---

# 172. MOBILE VERSION COMPATIBILITY

Backend must support controlled API compatibility during app rollout.

Track:

```text
appVersion
OS
device
supported API version
minimum version
blocked version
```

Allow remote enforcement of minimum app version.

---

# 173. DATA MIGRATION FROM CURRENT IMPLEMENTATION

Before production:

```text
inventory existing records
map old → canonical fields
validate orphan records
validate duplicate records
validate broken foreign keys
validate tenant ownership
validate historical financial records
backup
dry run
migration
verification
reconciliation
```

No silent data loss.

---

# 174. PRODUCTION DATA SEEDING

Only seed:

```text
reference data
system permissions
system roles
required configuration
```

Never seed fake customers, fake invoices, fake trips or fake production employees.

---

# 175. API REGRESSION MATRIX

Generate a matrix containing every endpoint:

```text
method
path
controller
service
auth required
role
permission
tenant scope
resource scope
request DTO
response DTO
error codes
idempotency
rate limit
audit
events
tests
frontend consumer
mobile consumer
status
```

Any endpoint with missing fields is not production-ready.

---

# 176. MODEL REGRESSION MATRIX

Every Prisma model must document:

```text
purpose
owner
tenant boundary
PII classification
retention
relations
indexes
unique constraints
soft delete
audit
migration history
API exposure
```

Delete unused speculative models or clearly mark them as non-production internal models.

---

# 177. API / DATABASE CONSISTENCY

No endpoint may expose fields that are:

- not persisted when persistence is expected
- persisted but unauthorized
- writable but not validated
- readable but not documented
- documented but not implemented

---

# 178. FRONTEND/API CONTRACT TESTING

Generate typed API clients from OpenAPI where practical.

Build fails when frontend expects an incompatible response.

---

# 179. NO SILENT FALLBACKS

Forbidden production behavior:

```text
DB fails → memory
ML fails → fake score
GPS fails → last point shown as live
notification fails → pretend sent
payment fails → mark paid
authorization fails → show data
provider fails → fabricated response
```

Allowed fallback must be explicitly labeled:

```text
DEGRADED
FALLBACK
STALE
PENDING
UNKNOWN
```

---

# 180. DATA QUALITY

Add automated checks:

```text
orphan employee
orphan booking
orphan trip
orphan passenger
orphan invoice
negative capacity
duplicate active driver
duplicate active vehicle
invalid organization hierarchy
expired compliance assigned
historical billing mutation
scope inconsistency
```

---

# 181. SECURITY REGRESSION SUITE

Every future release must run authorization regression tests.

Create reusable test helpers:

```text
assertTenantIsolation
assertScopeIsolation
assertPermission
assertRoleBoundary
assertDataMasking
assertExportAuthorization
assertBackgroundAuthorization
assertAIAuthorization
```

---

# 182. CUSTOMER UAT

Create UAT scripts for:

```text
Transport Admin
Coordinator
Compliance
Director
Manager
Employee
Vendor
Driver
Guard
Finance
Platform Admin
Security
Support
Auditor
```

Each role must test both allowed and denied actions.

---

# 183. PRODUCTION GO-LIVE CHECKLIST

### Engineering

```text
[ ] build green
[ ] tests green
[ ] migrations verified
[ ] API contracts verified
[ ] frontend routes verified
[ ] mobile builds verified
[ ] no demo path
[ ] no mock production provider
```

### Security

```text
[ ] secrets scan
[ ] dependency scan
[ ] container scan
[ ] SAST
[ ] DAST
[ ] penetration test
[ ] authorization tests
[ ] tenant isolation tests
[ ] MFA
[ ] audit
```

### Operations

```text
[ ] monitoring
[ ] alerts
[ ] dashboards
[ ] backups
[ ] restore test
[ ] DR test
[ ] runbooks
[ ] on-call
```

### Business

```text
[ ] UAT
[ ] policy approval
[ ] tax review
[ ] privacy review
[ ] customer sign-off
[ ] support readiness
```

---

# 184. FINAL “100%” DEFINITION

MOVE-IN-SYNC may be called production-ready ONLY when all of the following are true:

```text
✓ Real database
✓ Real migrations
✓ Real persistence
✓ Real authentication
✓ MFA
✓ SSO
✓ SCIM
✓ API keys
✓ Refresh-token rotation
✓ Account protection
✓ Complete RBAC
✓ Complete permission engine
✓ Complete scope engine
✓ Platform role isolation
✓ Tenant isolation
✓ PostgreSQL RLS where applicable
✓ Employee management
✓ Employee onboarding/offboarding
✓ Master-data governance
✓ CSV import
✓ Driver management
✓ Driver availability
✓ Vehicle management
✓ Guard management
✓ Compliance
✓ Documents
✓ Routing
✓ Navigation
✓ Scheduling
✓ Booking
✓ Approvals
✓ Dispatch
✓ Optimization
✓ GPS
✓ Geofencing
✓ Mobile internet
✓ Offline mobile
✓ Boarding
✓ OTP/QR
✓ No-show
✓ Evidence
✓ Appeals
✓ Ban
✓ Breakdown
✓ Replacement
✓ Passenger transfer
✓ SOS
✓ Incidents
✓ Clubbing
✓ Cross-process clubbing
✓ Driver preference
✓ Dynamic reoptimization
✓ Notifications
✓ Email
✓ SMS
✓ WhatsApp
✓ Push
✓ In-app
✓ Transport billing
✓ SaaS billing
✓ Tax/GST review
✓ Multi-currency
✓ Vendor billing
✓ Reconciliation
✓ Expenses
✓ Analytics
✓ Dashboards
✓ Reports
✓ Report builder
✓ Export
✓ Scheduled reports
✓ Data masking
✓ AI
✓ RAG
✓ AI authorization
✓ ML routing
✓ Integration hub
✓ Webhooks
✓ Storage abstraction
✓ Google Drive option
✓ KYC/compliance document flow
✓ HRMS optional integration
✓ SSO/SCIM
✓ Privacy
✓ Consent
✓ GPS consent
✓ Retention
✓ Legal hold
✓ Data access
✓ Data correction
✓ Data deletion/anonymization
✓ Audit
✓ Break-glass
✓ Security testing
✓ Vulnerability management
✓ Observability
✓ Metrics
✓ Tracing
✓ Logs
✓ Alerting
✓ Backup
✓ Restore test
✓ PITR
✓ DR
✓ Load test
✓ Chaos/failure test
✓ CI/CD
✓ Kubernetes
✓ Autoscaling
✓ TLS
✓ Secrets manager
✓ Feature flags
✓ Canary
✓ Rollback
✓ Support
✓ Incident management
✓ Accessibility
✓ Internationalization
✓ Documentation
✓ Developer portal
✓ API versioning
✓ Role-specific portals
✓ Real profile creation
✓ Development test profiles
✓ Production identity enforcement
✓ Mobile app releases
✓ E2E tests
✓ UAT
✓ Customer acceptance
✓ Operational runbooks
✓ Legal/compliance human sign-off
```

---

# 185. FINAL IMPLEMENTATION COMMAND FOR THE CODING AGENT

Use the following as the final instruction to the coding agent:

```text
You are completing MOVE-IN-SYNC for real production deployment.

DO NOT rebuild working functionality.
DO NOT create duplicate modules when an existing module can be extended.
DO NOT assume a model/controller/service is implemented merely because it exists.
DO NOT use demo-mode behavior in production.
DO NOT use mock data in production.
DO NOT trust client-supplied companyId, siteCode, processCode, role, permission, scope, trip state, GPS validity or OTP success.
DO NOT bypass centralized authorization.
DO NOT bypass tenant isolation.
DO NOT bypass policy resolution.
DO NOT bypass audit.
DO NOT bypass financial immutability.
DO NOT allow AI to bypass authorization.
DO NOT mark anything COMPLETE without tests.

FIRST:
1. Inspect the entire repository.
2. Build the actual inventory.
3. Compare it with the entire V4 master plan and this V5 production-completion specification.
4. Generate the gap register.
5. Resolve critical security/data-integrity blockers first.
6. Then complete backend APIs.
7. Then complete authorization and scope.
8. Then complete database persistence/migrations.
9. Then complete operational workflows.
10. Then complete mobile applications.
11. Then complete provider integrations.
12. Then complete role-specific web portals.
13. Then complete reporting.
14. Then complete billing.
15. Then complete observability.
16. Then complete security.
17. Then complete CI/CD and production infrastructure.
18. Then run all automated tests.
19. Then run load/failure/security tests.
20. Then run full E2E journeys.
21. Then generate release evidence.
22. Only then declare READY_FOR_PRODUCTION.

For every gap use:

INSPECT
→ DESIGN
→ IMPLEMENT
→ MIGRATE
→ AUTHORIZE
→ VALIDATE
→ TEST
→ INTEGRATE
→ OBSERVE
→ DOCUMENT
→ VERIFY
→ EVIDENCE
→ MARK STATUS

For every API verify:
AUTH + TENANT + SCOPE + PERMISSION + VALIDATION + IDEMPOTENCY + TRANSACTION + AUDIT + EVENTS + TEST.

For every UI page verify:
REAL API + REAL DATA + PERMISSION + SCOPE + EMPTY STATE + ERROR STATE + LOADING STATE + AUDIT + ACCESSIBILITY.

For every mobile action verify:
AUTH + DEVICE + OFFLINE + SYNC + RETRY + IDEMPOTENCY + SAFETY + AUDIT.

For every financial action verify:
IMMUTABLE HISTORY + RATE VERSION + TAX VERSION + APPROVAL + AUDIT + RECONCILIATION.

For every background job verify:
TENANT CONTEXT + AUTHORIZATION RECHECK + IDEMPOTENCY + RETRY + DLQ + OBSERVABILITY.

For every production integration verify:
REAL CREDENTIALS + HEALTH CHECK + RETRY + CIRCUIT BREAKER + FALLBACK + AUDIT.

At the end produce:
1. PRODUCTION_INVENTORY.md
2. PRODUCTION_GAP_REGISTER.md
3. API_INVENTORY.md
4. ROLE_MATRIX.md
5. PERMISSION_MATRIX.md
6. SCOPE_MATRIX.md
7. INTEGRATION_MATRIX.md
8. TEST_COVERAGE_REPORT.md
9. SECURITY_TEST_REPORT.md
10. LOAD_TEST_REPORT.md
11. DR_TEST_REPORT.md
12. RELEASE_READINESS.md
13. RUNBOOKS.md
14. FINAL_E2E_REPORT.md

Do not say “production ready” because the application compiles.
Say “production ready” only after the complete release gates pass.
If a requirement cannot be verified, mark it BLOCKED rather than pretending it is complete.
```

---

# 186. FINAL ACCEPTANCE GATE

The platform is accepted only when:

```text
ALL P0 = PASS
ALL P1 = PASS
ALL SECURITY CRITICAL/HIGH = CLOSED OR FORMALLY ACCEPTED
ALL TENANT ISOLATION = PASS
ALL CRITICAL E2E = PASS
ALL PRODUCTION DEPENDENCIES = HEALTHY
BACKUP RESTORE = PASS
DR TEST = PASS
LOAD TEST = PASS
MOBILE RELEASE = PASS
UAT = PASS
LEGAL/TAX/PRIVACY REVIEWS = SIGNED OFF
ROLLBACK = TESTED
MONITORING = LIVE
ALERTING = LIVE
SUPPORT = READY
RUNBOOKS = READY
```

Only then:

```text
RELEASE_STATUS = PRODUCTION_APPROVED
```

Otherwise:

```text
RELEASE_STATUS = NOT_READY
```

---

# 187. IMPORTANT LIMITATION OF THE TERM “100%”

No software specification can honestly guarantee that a system is permanently “100% production ready” without verification against the actual deployed infrastructure, real provider accounts, real customer data, real traffic, security testing, legal requirements and operational acceptance.

Therefore this document defines **100% specification and acceptance coverage**, while actual production readiness is an evidence-based release gate.

The coding agent MUST never fabricate that evidence.

---

# 188. FINAL PRINCIPLE

The finished MOVE-IN-SYNC system must behave as one governed platform:

```text
IDENTITY
+
ROLE
+
PERMISSION
+
TENANT
+
SCOPE
+
POLICY
+
DATA CLASSIFICATION
+
STATE
+
SAFETY
+
COMPLIANCE
+
CAPACITY
+
REAL-TIME GPS
+
DISPATCH
+
MOBILE
+
FINANCE
+
AUDIT
+
OBSERVABILITY
+
SECURITY
+
RECOVERY
```

The same authorization truth must protect:

```text
API
WEB
MOBILE
WEBSOCKET
REPORT
EXPORT
AI
QUEUE
SCHEDULER
SEARCH
STORAGE
BILLING
```

There must be no second hidden authorization system.

There must be no “admin sees everything” shortcut.

There must be no frontend-only security.

There must be no demo-mode production fallback.

There must be no silent historical mutation.

There must be no unsafe optimization.

There must be no capacity overbooking.

There must be no unverified production claim.

**END OF FINAL V5 PRODUCTION COMPLETION SPECIFICATION**

---

# PHASE 189: ROLE-BASED DASHBOARD & PORTAL EXECUTION MASTER SPECIFICATION
## MIMO / OPENCODE IMPLEMENTATION CONTRACT — READ THIS BEFORE CHANGING THE FRONTEND

### 189.0 PURPOSE

This section removes ambiguity from the existing role-based dashboard requirements.

The implementation agent MUST NOT interpret “role-based dashboard” as:

- one dashboard with a few hidden menu items;
- one `/dashboard` page containing every company's data;
- frontend-only conditional rendering;
- a generic admin page reused by every role;
- placeholder cards with fake numbers;
- a static mockup disconnected from APIs;
- a route that changes only its title based on role.

The required result is a real multi-portal application in which:

```text
LOGIN
  ↓
AUTHENTICATION
  ↓
TENANT / PLATFORM CONTEXT
  ↓
ROLE ASSIGNMENT RESOLUTION
  ↓
PERMISSION RESOLUTION
  ↓
ACCESS SCOPE RESOLUTION
  ↓
DATA CLASSIFICATION RESOLUTION
  ↓
PORTAL RESOLUTION
  ↓
ROLE-SPECIFIC NAVIGATION
  ↓
ROLE-SPECIFIC DASHBOARD
  ↓
AUTHORIZED WIDGETS
  ↓
AUTHORIZED RECORDS
  ↓
AUTHORIZED ACTIONS
```

The server is authoritative at every step.

The frontend may improve usability, but it MUST NEVER be the security boundary.

### 189.1 IMPLEMENTATION ORDER — DO NOT DEVIATE

Implement in this order:

1. Inspect the existing repository.
2. Preserve working modules.
3. Identify existing authentication, user, role, permission, tenant, scope and Prisma models.
4. Identify existing API endpoints that can power each dashboard.
5. Add missing backend contracts only where required.
6. Implement centralized authorization resolution.
7. Implement portal resolution.
8. Implement portal layouts and navigation.
9. Implement dashboards.
10. Implement detail pages.
11. Implement create/edit workflows.
12. Implement loading/error/empty states.
13. Connect every widget to real APIs.
14. Remove fake/mock production data.
15. Add role/profile seed data for development and UAT.
16. Add authorization tests.
17. Add E2E tests for each portal.
18. Run typecheck, lint, unit tests, integration tests and production build.
19. Verify direct URL access to unauthorized pages is denied.
20. Verify API access is denied even when frontend restrictions are bypassed.
21. Only then mark the portal complete.

Do NOT rebuild the whole application if existing functionality already satisfies a requirement.

Do NOT create duplicate authentication or authorization systems.

Do NOT create a second role system.

Do NOT create a second tenant system.

Do NOT create hard-coded user-specific access rules.

---

# 190. CANONICAL PORTAL MAP

The application MUST support these canonical portal families:

| Portal | Canonical route | Primary roles |
|---|---|---|
| Platform | `/platform/*` | SUPERADMIN, MOVE-IN-ADMIN |
| Finance | `/finance/*` | FINANCE_TEAM, TRANSPORT_FINANCE |
| Operations | `/operations/*` | Control Room, Dispatch, Fleet, Safety, operational roles |
| Transport | `/transport/*` | TRANSPORT_ADMIN, TRANSPORT_SUB_ADMIN, TRANSPORT_COORDINATOR |
| Compliance | `/compliance/*` | PLATFORM_COMPLIANCE, TRANSPORT_COMPLIANCE |
| Management | `/manager/*` | DIRECTOR, SENIOR_MANAGER, MANAGER, ASSISTANT_MANAGER, TEAM_LEADER |
| Employee | `/employee/*` | EMPLOYEE, TRAINER where applicable |
| Vendor | `/vendor/*` | VENDOR_ADMIN, VENDOR_DISPATCHER |
| Driver | `/driver/*` | DRIVER |
| Guard | `/guard/*` | GUARD |
| Support | `/support/*` | SUPPORT_ENGINEER |
| Security | `/security/*` | SECURITY_ADMINISTRATOR |
| Auditor | `/auditor/*` | PLATFORM_AUDITOR |

A user may have more than one authorized role assignment.

If multiple roles are active, the application MUST either:

1. resolve a single authorized default portal and allow an explicit role/context switch; or
2. provide a portal selector containing only portals the user is authorized to enter.

The application MUST NEVER expose a portal simply because the route exists.

---

# 191. UNIVERSAL APPLICATION SHELL

Every portal MUST use a common technical shell while allowing role-specific navigation.

Required shell:

```text
┌──────────────────────────────────────────────────────────────┐
│ Logo │ Tenant/Context │ Global Search │ Notifications │ User │
├──────────────┬───────────────────────────────────────────────┤
│              │ Breadcrumb                                    │
│ Role-specific│ Page title + actions                           │
│ sidebar      │                                               │
│              │ Main content                                   │
│              │                                               │
│              │                                               │
└──────────────┴───────────────────────────────────────────────┘
```

Required shell capabilities:

- authenticated user identity;
- active company;
- active site;
- active role;
- active scope;
- role/context switch where authorized;
- notifications;
- help/support;
- profile;
- security/session controls;
- global search limited to authorized records;
- breadcrumbs;
- responsive behavior;
- accessibility;
- error boundary;
- loading states;
- empty states;
- permission-aware action buttons.

The shell MUST NOT contain links to unauthorized modules.

A hidden link is not sufficient security.

---

# 192. UNIVERSAL SCOPE BAR

Customer and operational portals MUST display the effective data scope.

Example:

```text
Company: CONC001
Sites: PUN01, MUM01
LOBs: BPO
Processes: VOICE01, CHAT01
Shifts: NIGHT, GENERAL
```

The scope bar MUST be derived from the server authorization response.

The client MUST NOT be allowed to expand scope by changing:

```text
companyId
siteId
siteCode
processId
processCode
shiftId
role
permission
```

in a request.

Every API MUST independently resolve:

```text
creator/actor authority
∩
assigned role authority
∩
assigned scope
∩
requested resource scope
∩
tenant
∩
policy
```

---

# 193. DASHBOARD REGISTRY

Create a single backend/frontend dashboard registry.

Each dashboard definition MUST contain at least:

```text
dashboardId
portal
role
authorityDomain
requiredPermissions
requiredScope
dataClassification
route
title
description
widgetIds
defaultFilters
supportedDrilldowns
exportPermissions
featureFlag
```

Example:

```text
dashboardId:
transport-admin-overview

portal:
transport

roles:
TRANSPORT_ADMIN

requiredPermissions:
VIEW_TRANSPORT_DASHBOARD

route:
/transport/dashboard
```

The registry MUST NOT itself grant access.

It is a presentation/registration layer.

Authorization remains server-side.

---

# 194. UNIVERSAL WIDGET CONTRACT

Every KPI, chart, table, map, alert or action widget MUST have:

```text
widgetId
metricId
requiredPermissions
dataClassification
scopeRequirement
query/service
loadingState
emptyState
errorState
drilldownRoute
exportCapability
```

Every widget must have a real data source.

The following are forbidden in production:

```text
Math.random()
fake KPI constants
mock arrays
hard-coded employee counts
hard-coded trip counts
placeholder GPS coordinates
fake revenue
fake compliance percentages
fake driver availability
```

Development seed data is permitted only when clearly identified as development/test data.

---

# 195. ROLE → PORTAL → DASHBOARD MATRIX

## 195.1 SUPERADMIN

Route:

```text
/platform
```

Sidebar:

```text
Overview
Companies
Company Lifecycle
Subscriptions
Plans
Pricing
Currencies
Platform Billing
Usage
Revenue
Internal Users
Platform Roles
Feature Flags
Rollouts
Release Management
Security
Break-Glass
Compliance
Support
System Health
Queues
Jobs
Audit
Data Governance
Storage
Providers
Integrations
API Keys
Webhooks
Developer Portal
Status
Backups
Disaster Recovery
Incident Management
Configuration
```

Dashboard widgets:

```text
Total Companies
Active Companies
Trial Companies
Suspended Companies
Active Users
Active Subscriptions
MRR / ARR where implemented
Platform Usage
API Usage
Storage Usage
Open Incidents
Open Support Tickets
Failed Jobs
Queue Backlog
System Health
Provider Health
Security Alerts
Compliance Alerts
Recent Audit Events
Backup Status
DR Readiness
Feature Rollout Status
```

Primary actions:

```text
Create Company
Suspend Company
Reactivate Company
Manage Subscription
Manage Plan
Manage Feature Flag
Start Rollout
Rollback Rollout
Manage Internal User
Review Security Event
Review Incident
Review Audit
Review Backup/DR
```

All high-risk actions require appropriate permission, confirmation, audit and MFA/break-glass controls.

---

## 195.2 MOVE-IN-ADMIN

Route:

```text
/platform
```

Default sidebar may be a governed subset of:

```text
Overview
Companies
Customer Operations
Users
Roles
Permissions
Tenants
Feature Flags
Integrations
Providers
Support
System Health
Audit
Reports
Configuration
```

The exact sidebar MUST be permission-derived.

MOVE-IN-ADMIN MUST NOT automatically receive every SUPERADMIN capability.

---

## 195.3 FINANCE TEAM

Route:

```text
/finance
```

Sidebar:

```text
Dashboard
Billing Accounts
Rate Cards
Invoices
Invoice Lines
Reconciliation
Vendor Billing
Cost Allocation
Expenses
Reimbursements
Budgets
Actuals
Taxes / GST
Payments
Disputes
Financial Reports
Exports
Audit
```

Dashboard:

```text
Current Billing Period
Invoice Value
Pending Invoices
Overdue Invoices
Reconciliation Exceptions
Vendor Payables
Employee Expense Pending
Approved Expenses
Budget vs Actual
Cost per Trip
Cost per Passenger
Cost per KM
Tax/GST Summary
Billing Anomalies
```

Finance users MUST NOT gain transport dispatch authority merely because they can see financial records.

---

# 196.4 PROJECT MANAGER

Route:

```text
/platform/project or governed project route
```

Sidebar:

```text
Portfolio
Projects
Companies
Implementation Status
Milestones
Risks
Issues
Dependencies
Change Requests
Implementation Reports
Usage
Support
Audit
```

Dashboard:

```text
Active Implementations
Milestone Completion
Blocked Milestones
Open Risks
Open Issues
Overdue Tasks
Customer Readiness
Integration Readiness
UAT Status
Go-Live Readiness
```

---

# 196.5 PROJECT COORDINATOR

Dashboard:

```text
Assigned Projects
Today's Tasks
Upcoming Milestones
Blocked Items
Customer Actions
Open Issues
Integration Tasks
UAT Tasks
```

Only assigned project scope may be visible.

---

# 195.6 PLATFORM COMPLIANCE

Route:

```text
/compliance
```

Sidebar:

```text
Overview
Compliance Rules
Customer Compliance
Driver Compliance
Vehicle Compliance
Document Expiry
Blocked Resources
Exceptions
Waivers
Evidence
Audits
Incidents
Dispatch Blocks
Override Reviews
Reports
```

Dashboard:

```text
Compliance Score
Expired Documents
Expiring Documents
Blocked Drivers
Blocked Vehicles
Open Exceptions
Open Waivers
Unsafe Dispatch Attempts
Compliance Incidents
Pending Reviews
```

Compliance blocking authority MUST remain separate from unrelated administrative authority.

---

# 195.7 SECURITY ADMINISTRATOR

Route:

```text
/security
```

Sidebar:

```text
Security Dashboard
Users & Sessions
MFA
Devices
API Keys
Security Events
Authentication Failures
Suspicious Activity
IP Restrictions
Secrets/Provider Status where permitted
Vulnerability Status
Incident Response
Break-Glass
Audit
Security Reports
```

Dashboard:

```text
Authentication Failures
Locked Accounts
MFA Exceptions
Suspicious Sessions
Unusual GPS Signals
API Key Alerts
Security Incidents
Open Vulnerabilities
Active Break-Glass Sessions
Recent Privilege Changes
```

Security administrators MUST NOT automatically receive financial or transport-operational privileges.

---

# 195.8 SUPPORT ENGINEER

Route:

```text
/support
```

Sidebar:

```text
Support Dashboard
Tickets
Customers
Users
Sessions
Diagnostics
Integration Health
Provider Health
Jobs
Queues
Incidents
Knowledge Base
Audit
```

Dashboard:

```text
Open Tickets
SLA Breaches
Critical Tickets
Provider Failures
Failed Jobs
Queue Backlog
Integration Failures
Active Incidents
```

Support access to customer data MUST be minimized and audited.

Break-glass access must be explicit and time limited.

---

# 195.9 PLATFORM AUDITOR

Route:

```text
/auditor
```

Sidebar:

```text
Audit Dashboard
Audit Events
Authentication Audit
Authorization Audit
Role Changes
Scope Changes
Admin Delegations
Financial Audit
Dispatch Audit
GPS Access Audit
Export Audit
Security Audit
Compliance Audit
System Changes
Reports
```

Dashboard:

```text
Events Today
Critical Events
Authorization Denials
Privilege Changes
Scope Changes
Admin Creation
Delegations
Exports
Financial Changes
Dispatch Overrides
Emergency Overrides
Break-Glass Events
```

Auditor access MUST be read-only unless an explicitly governed action exists.

---

# 195.10 TRANSPORT ADMIN

Route:

```text
/transport
```

Sidebar:

```text
Dashboard
Employees
Transport Eligibility
Onboard / Offboard
Bookings
Approvals
Schedules
Routes
Clubbing
Dispatch
Control Room
Trips
Drivers
Vehicles
Guards
Compliance
No-Show
Breakdown
SOS
Incidents
Vendors
Expenses
Billing
Reports
Notifications
Policies
Organization
Roles
Admins
Access Preview
Audit
Settings
```

Dashboard top row:

```text
Today's Demand
Booked
Pending Approval
Available Seats
Capacity Shortage
Active Trips
Drivers Available
Drivers On Trip
Vehicles Available
No-Shows
Breakdowns
SOS / Critical Incidents
Compliance Blocks
```

Dashboard operational widgets:

```text
Live Trip Map
Demand vs Capacity
Upcoming Shift Demand
Unassigned Trips
At-Risk Trips
Late Pickups
Late Drops
No-Show Queue
Breakdown Queue
Driver Availability
Vehicle Availability
Compliance Blocks
Dispatch Exceptions
Optimization Activity
Cost Summary
```

Quick actions:

```text
Onboard Employee
Offboard Employee
Create Booking
Create Schedule
Manage Route
Open Dispatch
Assign Driver
Assign Vehicle
Reassign Trip
Move Passenger
Manage No-Show
Manage Breakdown
Create Emergency Transport
Create/Delegate Admin
Access Preview
```

All actions are permission and scope controlled.

---

# 195.11 TRANSPORT SUB-ADMIN

Route:

```text
/transport
```

Same UI framework as Transport Admin, but all navigation, widgets, records and actions are filtered by delegated scope.

Example:

```text
Company:
CONC001

Sites:
PUN01

Processes:
VOICE01
CHAT01

Shifts:
NIGHT
```

The Sub-Admin MUST NOT see:

```text
MUM01
FINANCE_PROCESS
OTHER_SITE_PROCESS
UNAUTHORIZED_FINANCIAL_DATA
```

even if the API request manually supplies those identifiers.

The server MUST deny the request.

---

# 195.12 TRANSPORT COORDINATOR

Route:

```text
/operations or /transport
```

Recommended sidebar:

```text
Dashboard
Bookings
Approvals
Schedule
Dispatch
Driver Availability
Vehicle Availability
Trips
No-Show
Replacement
Reassignment
Control Room
Notifications
Reports
```

Dashboard:

```text
Today's Trips
Unassigned Trips
Active Trips
Late Trips
Available Drivers
Available Vehicles
Capacity Shortage
No-Shows
Breakdowns
Pending Dispatch Decisions
```

The Coordinator is operational by default.

Policy editing, administrator creation and other high-risk capabilities require explicit permissions.

---

# 195.13 TRANSPORT COMPLIANCE

Route:

```text
/compliance
```

Dashboard:

```text
Driver Compliance
Vehicle Compliance
Document Expiry
Blocked Drivers
Blocked Vehicles
Dispatch Blocks
Exceptions
Waivers
Pending Reviews
Compliance Score
Safety Incidents
```

Actions:

```text
Block Resource
Review Exception
Review Waiver
Review Evidence
Approve/Reject Governed Override
View Audit
```

A compliance user cannot use the dashboard as a route around authorization.

---

# 195.14 DIRECTOR

Route:

```text
/manager
```

Sidebar:

```text
Executive Dashboard
Transport Performance
Safety
Compliance
Cost
Utilization
Service Levels
Employees
Approvals
Reports
Alerts
```

Dashboard:

```text
Transport Cost
Cost Trend
Employees Using Transport
Transport Utilization
Trips
On-Time %
No-Show %
Breakdown %
Safety Incidents
Compliance Score
SLA Performance
Capacity Utilization
Site Comparison
Process Comparison
```

Director does NOT automatically receive:

```text
dispatch control
driver reassignment
administrator creation
policy editing
financial mutation
security administration
```

unless explicitly granted.

---

# 195.15 SENIOR MANAGER

Route:

```text
/manager
```

Dashboard:

```text
My Organization Transport Usage
Demand
Bookings
Approvals
Cost
Utilization
SLA
Safety
No-Shows
Incidents
Team/Process Trends
```

Hierarchy visibility is derived from authorized reporting hierarchy.

---

# 195.16 MANAGER

Route:

```text
/manager
```

Dashboard:

```text
Team Headcount
Transport Eligible
Transport Active
Today's Bookings
Pending Approvals
Upcoming Shifts
No-Shows
Team Transport Cost where authorized
Team Service Level
Alerts
```

Managers MUST NOT see employees outside their authorized hierarchy/scope.

---

# 195.17 ASSISTANT MANAGER

Route:

```text
/manager
```

Dashboard is a permission-scoped subset of Manager.

Typical:

```text
Team Headcount
Bookings
Approvals
Shift Demand
No-Shows
Alerts
Reports
```

No implicit escalation to Manager authority.

---

# 195.18 TEAM LEADER

Route:

```text
/manager
```

Dashboard:

```text
My Team
Today's Transport
Upcoming Transport
Booking Status
Approval Queue where delegated
No-Shows
Employee Alerts
Safety Alerts
```

Team Leader visibility follows reporting hierarchy and scope.

---

# 195.19 EMPLOYEE

Route:

```text
/employee
```

Sidebar:

```text
Home
My Transport
My Bookings
Book Transport
Cancel Booking
My Trips
Live Trip
Pickup Details
Notifications
Expenses
Profile
Emergency / SOS
Help
```

Dashboard:

```text
Next Trip
Pickup Time
Pickup Location
Vehicle / Driver information where policy permits
Booking Status
Live Trip status
Recent Trips
Pending Actions
Notifications
```

Employee MUST only see their own records plus explicitly permitted organizational information.

Employee MUST NOT see:

```text
other employees' trips
company-wide GPS
driver private information
vendor financial data
other employee expenses
administrative controls
```

---

# 195.20 TRAINER

Route:

```text
/employee or /manager
```

Trainer receives only explicitly assigned trainee/employee visibility.

Dashboard:

```text
Assigned Trainees
Transport Status
Upcoming Training-related Transport
Attendance/transport indicators where authorized
Alerts
```

Trainer is not automatically a manager or transport administrator.

---

# 195.21 VENDOR ADMIN

Route:

```text
/vendor
```

Sidebar:

```text
Dashboard
Assigned Contracts
Vehicles
Drivers
Availability
Trips
Dispatch
Documents
Compliance
Incidents
Invoices
Performance
Reports
Notifications
Profile
```

Dashboard:

```text
Assigned Trips
Active Trips
Available Drivers
Available Vehicles
Compliance Blocks
Expiring Documents
No-Shows
Breakdowns
SLA
Trip Completion
Invoice Status
```

Vendor users MUST only see data belonging to their authorized vendor relationship and assigned customer scope.

---

# 195.22 VENDOR DISPATCHER

Route:

```text
/vendor
```

Dashboard:

```text
Today's Assigned Trips
Unassigned Vendor Trips
Available Drivers
Available Vehicles
Active Trips
Breakdowns
Driver Alerts
Compliance Blocks
```

Actions are limited to explicit vendor-dispatch permissions.

---

# 195.23 DRIVER

Route:

```text
/driver
```

This is a mobile-first portal.

Sidebar/navigation:

```text
Home
Availability
Today's Trips
Trip Detail
Navigation
Passengers
Boarding
No-Show
Breakdown
SOS
Vehicle Check
Documents
Notifications
Profile
Settings
```

Driver dashboard:

```text
Current Availability
Current Trip
Next Trip
Pickup Progress
Passengers
Boarded
Remaining
Vehicle
Route
Navigation
Breakdown/SOS
Compliance Status
GPS Status
Network Status
```

Driver availability MUST explicitly show:

```text
OFFLINE
AVAILABLE
ACTIVE
ON_TRIP
ON_BREAK
SLEEPY
RESTING
FUELING
VEHICLE_CHECK
UNAVAILABLE
EMERGENCY
SUSPENDED
COMPLIANCE_BLOCKED
```

A shift does NOT automatically mean the driver is ACTIVE.

Driver must explicitly transition to ACTIVE after eligibility checks.

---

# 195.24 GUARD

Route:

```text
/guard
```

Dashboard:

```text
Assigned Trips
Upcoming Trips
Passenger Verification
Boarding Status
Security Alerts
Incidents
SOS
Shift Status
```

Guard sees only assigned operational information.

---

# 196. ROLE-SPECIFIC DETAIL PAGE CONTRACT

A dashboard is incomplete if its widgets cannot drill into the records they summarize.

Every dashboard KPI MUST have a defined drilldown where meaningful.

Examples:

```text
Active Trips
→ /transport/trips?status=ACTIVE

No-Shows
→ /transport/no-shows

Breakdowns
→ /transport/breakdowns

Available Drivers
→ /transport/drivers?availability=AVAILABLE

Compliance Blocks
→ /compliance/blocks

Pending Approvals
→ /transport/approvals

Billing Exceptions
→ /finance/reconciliation?status=EXCEPTION
```

The destination page MUST reapply authorization.

A user cannot gain access merely by arriving through a permitted KPI.

---

# 197. UNIVERSAL LIST PAGE CONTRACT

Every major entity list must support, where authorized:

```text
Search
Filter
Sort
Pagination
Column selection
Saved filters
Export
Bulk actions
Row actions
Detail view
Audit
```

Required filters should include applicable dimensions:

```text
Date
Company
Region
Site
LOB
Process
Department
BU
Cost Centre
Project
Shift
Status
Vendor
Driver
Vehicle
```

The available filter values MUST themselves be scope-filtered.

Do not expose an unauthorized site in a dropdown and merely return zero rows afterward.

---

# 198. UNIVERSAL CREATE / EDIT PAGE CONTRACT

All create/edit pages MUST use:

```text
Load
→ Validate permissions
→ Load authorized reference data
→ Enter data
→ Client validation
→ Server validation
→ Authorization validation
→ Policy validation
→ Conflict validation
→ Confirmation
→ Idempotent mutation
→ Audit
→ Refresh
```

Examples:

```text
Create User
Create Admin
Onboard Employee
Offboard Employee
Create Schedule
Create Route
Assign Driver
Assign Vehicle
Reassign Trip
Move Passenger
Create Policy
Create Billing Account
Create Rate Card
Create Vendor
```

No mutation is considered complete until the backend confirms success.

---

# 199. ADMIN / PROFILE CREATION — SCREEN-BY-SCREEN

Canonical route:

```text
/transport/admins
/transport/admins/create
```

or another authorized administration route resolved by the existing architecture.

## Step 1 — Identity

Fields:

```text
First Name
Last Name
Email
Phone
Employee/User Identifier
Username if applicable
```

Validation:

```text
unique identity
valid email
valid phone
tenant context
```

## Step 2 — User Type

Options must be role-compatible.

Examples:

```text
Platform Internal
Customer Employee
Transport Operations
Vendor/Partner
Driver
Guard
```

## Step 3 — Company

Only companies the creator can administer may appear.

## Step 4 — Site

Only sites within the selected company and creator authority.

## Step 5 — LOB

Only LOBs in scope.

## Step 6 — Process

Only processes valid for the selected company/site/LOB.

## Step 7 — Shift

Only authorized shifts.

## Step 8 — Role

Show only roles that:

```text
creator can assign
AND
are compatible with user type
AND
are allowed in tenant
```

## Step 9 — Permissions

Group permissions:

```text
Employee
Booking
Approval
Schedule
Route
Clubbing
Dispatch
Driver
Vehicle
Trip
No-Show
Breakdown
Safety
Compliance
Vendor
Finance
Reports
Exports
Users
Roles
Delegation
Audit
```

Each permission must show:

```text
Permission name
Description
Risk level
Delegatable?
Current creator permission
```

## Step 10 — Scope

Use hierarchical selectors:

```text
Company
→ Region
→ Site
→ LOB
→ Process
→ Department
→ BU
→ Cost Centre
→ Project
→ Shift
```

Do not build independent unrestricted multi-selects that accidentally create a Cartesian scope.

Where tuple scoping is required, use tuples.

Example:

```text
CONC001 | PUN01 | BPO | VOICE01 | NIGHT
```

## Step 11 — Hierarchy Visibility

Choose:

```text
Self
Direct reports
Hierarchy below
Specific hierarchy nodes
```

Only if authorized.

## Step 12 — Data Visibility

Configure:

```text
Operational
Employee PII
GPS
Financial
Security
Compliance
Audit
```

Each is permission and classification controlled.

## Step 13 — Delegatable Permissions

The creator may only delegate permissions explicitly marked delegatable and currently possessed.

## Step 14 — Approval Authority

Fields:

```text
Approval type
Maximum amount
Scope
Effective from
Effective to
Escalation
```

Never infer financial approval authority from job title.

## Step 15 — MFA

Configure:

```text
Required
Optional
Inherited by policy
```

High-risk roles should enforce required MFA according to platform policy.

## Step 16 — Device Requirement

Where required:

```text
Device binding
Managed device
Mobile device
No restriction
```

## Step 17 — Effective Dates

```text
Effective From
Effective To
```

Role, permission and scope assignments must be effective-dated where the model supports it.

## Step 18 — Review

Show a complete summary.

## Step 19 — Access Preview

Show the actual resulting access.

## Step 20 — Create

The Create button is enabled only when all blocking validations pass.

---

# 200. ACCESS PREVIEW — MANDATORY IMPLEMENTATION

Access Preview MUST be a real authorization calculation, not a UI summary assembled from selected form values.

Display:

```text
Identity
User Type
Role
Authority Domain
Company
Sites
Regions
LOBs
Processes
Departments
Business Units
Cost Centres
Projects
Shifts
Reporting Hierarchy
Permissions
Delegatable Permissions
Approval Authority
Data Classifications
Allowed Actions
Denied Actions
Effective Headcount
Effective Employees
Effective Vehicles
Effective Drivers
Effective Trips
Effective Financial Records
Effective Vendors
Effective GPS Visibility
```

Also display:

```text
WHY ALLOWED
WHY DENIED
```

for important permissions.

Example:

```text
ASSIGN_DRIVER
Allowed

Reason:
Role = TRANSPORT_SUB_ADMIN
Permission = ASSIGN_DRIVER
Company = CONC001
Site = PUN01
Process = VOICE01
```

Example:

```text
VIEW_FINANCIAL_DATA
Denied

Reason:
Permission not delegated by creator
```

The Access Preview endpoint MUST perform the same authorization resolution that production requests use.

---

# 201. ACCESS PREVIEW BLOCKING RULES

Creation MUST be blocked if:

```text
scope exceeds creator scope
permission exceeds creator delegation
role incompatible with user type
tenant missing
company missing
invalid site
invalid process
invalid shift
invalid reporting hierarchy
invalid approval authority
required MFA missing
required device policy missing
conflicting active assignment
duplicate identity
expired delegation
creator delegation expired
```

Never allow the frontend to override these errors.

---

# 202. ADMIN PROFILE DETAIL PAGE

Route:

```text
/transport/admins/:id
```

Tabs:

```text
Overview
Role
Permissions
Scope
Hierarchy
Data Visibility
Delegations
Approval Authority
Sessions
MFA
Devices
Effective Access
Access Preview
Audit
```

Actions:

```text
Edit
Suspend
Reactivate
Change Role
Change Scope
Change Permissions
Manage Delegation
Revoke Delegation
Terminate Session
Reset MFA where authorized
View Audit
```

High-risk changes require confirmation and audit.

Historical assignments MUST remain auditable.

---

# 203. EMPLOYEE ONBOARDING UI

Route:

```text
/transport/employees/:id/onboard
```

Flow:

```text
Employee
→ Organization Validation
→ Transport Eligibility
→ Applicable Policy
→ Current Bookings
→ Current Trips
→ Cost Impact
→ Capacity Impact
→ Scope Validation
→ Confirmation
→ Onboard
```

Preview:

```text
Employee
Company
Site
Process
Shift
Transport Policy
Current Eligibility
Future Eligibility
Upcoming Bookings
Potential Trip Impact
Capacity Impact
Cost Impact
```

Onboarding MUST NOT duplicate the employee master.

---

# 204. EMPLOYEE OFFBOARDING UI

Route:

```text
/transport/employees/:id/offboard
```

Before confirmation:

```text
Upcoming Bookings
Active Trips
Passenger Movements
Assigned Routes
Pending Approvals
Expenses
Billing Allocations
Notifications
```

The system MUST present rebooking/operational impact where applicable.

Offboarding MUST NOT delete historical:

```text
Bookings
Trips
Passenger Movements
No-Shows
OTP records retained by policy
Expenses
Billing Allocations
Audit
```

---

# 205. CONTROL ROOM SCREEN

Route:

```text
/operations/control-room
```

Layout:

```text
Top:
Demand | Capacity | Active Trips | No-Shows | Breakdowns | SOS

Left:
Exception queue

Center:
Live map

Right:
Selected trip/driver/passenger detail

Bottom:
Timeline / event stream
```

Queues:

```text
Unassigned
Late Pickup
Late Drop
Driver Unavailable
Vehicle Unavailable
No-Show
Breakdown
Compliance Block
SOS
GPS Stale
Route Deviation
Capacity Shortage
Optimization Required
```

Actions MUST be role/permission/scope controlled.

---

# 206. DISPATCH SCREEN

Required capabilities:

```text
Demand view
Available drivers
Available vehicles
Trip assignment
Trip reassignment
Passenger reassignment
Capacity view
Compliance state
Driver work/rest state
Route compatibility
Clubbing candidates
ETA
Optimization suggestions
Manual override
```

Priority order:

```text
1 Safety
2 Female passenger safety
3 Legal/mandatory policy
4 Driver/vehicle compliance
5 Authorization/scope
6 Vehicle capacity
7 Driver availability/work/rest
8 Pickup/drop SLA
9 Route compatibility
10 Guard requirements
11 Passenger ride time
12 Clubbing compatibility
13 Driver preference
14 Utilization
15 Cost
```

Cost MUST NOT override a higher-priority constraint.

---

# 207. LIVE DRIVER SCREEN

Required fields:

```text
Driver
Driver Type
Vendor
Company
Site
Process
Availability
Current Trip
Current Vehicle
GPS Last Seen
GPS Freshness
Network State
Compliance
Shift
Work Time
Rest Time
Fatigue/Sleepy State
```

Availability controls:

```text
Go Active
Go Offline
Break
Sleepy
Rest
Emergency
```

The backend validates every state transition.

---

# 208. LIVE TRIP SCREEN

Required:

```text
Trip ID
Status
Company
Site
Process
Shift
Driver
Vehicle
Passengers
Boarded
No-Shows
Pickup Sequence
Drop Sequence
ETA
GPS Freshness
Route
Deviation
Safety Status
Compliance Status
```

Actions:

```text
Reassign Trip
Move Passenger
Mark Arrival
Start Trip
Pause where supported
Complete
Handle No-Show
Breakdown
SOS
```

Every action is state-machine validated.

---

# 209. REPORTS UI

Every role gets only reports authorized for that role and scope.

Report page:

```text
Reports
→ Catalog
→ My Reports
→ Saved Filters
→ Scheduled
→ Exports
→ Snapshots
```

Report builder:

```text
Data Source
→ Columns
→ Filters
→ Grouping
→ Sorting
→ Calculated Fields
→ Charts
→ Totals
→ Visibility
→ Schedule
→ Save
```

Before generation:

```text
authorization
scope
data classification
export permission
```

must be evaluated.

Exports:

```text
XLSX
PDF
CSV
JSON where authorized
Print
```

Large exports MUST be asynchronous.

---

# 210. DASHBOARD FILTER RULES

Global filters may include:

```text
Date
Company
Region
Site
LOB
Process
Department
BU
Cost Centre
Project
Shift
Vendor
Driver
Vehicle
Trip Status
```

But each role sees only filters allowed by scope and permission.

Example:

A Site-scoped Transport Sub-Admin MUST NOT see a company-wide Site selector containing unauthorized sites.

A Manager MUST NOT see arbitrary Company selectors.

An Employee MUST NOT receive an organizational filter that exposes other employees.

---

# 211. ROLE SWITCHING

If a user has multiple roles, provide:

```text
Switch Role / Context
```

The server returns the valid contexts.

On switching:

```text
old portal state discarded
new context requested from server
permissions reloaded
scope reloaded
dashboard registry reloaded
navigation reloaded
```

Do not merely change a frontend variable.

---

# 212. API CONTRACT FOR PORTAL RESOLUTION

Implement or adapt an endpoint equivalent to:

```http
GET /me
GET /me/roles
GET /me/contexts
GET /me/effective-access
GET /me/navigation
GET /me/dashboard
GET /me/access-preview
```

The exact route may follow existing architecture.

Response should provide:

```json
{
  "user": {},
  "tenant": {},
  "contexts": [],
  "activeContext": {},
  "roles": [],
  "permissions": [],
  "scope": {},
  "dataVisibility": {},
  "portal": {},
  "navigation": [],
  "dashboard": {}
}
```

Do not expose secrets.

Do not trust client-submitted role/permission values.

---

# 213. FRONTEND ROUTE GUARD

Every protected route must have:

```text
authentication check
tenant/context check
portal check
permission check
scope-aware data loading
```

But frontend route guards are UX protection only.

The backend MUST enforce the same authorization.

Direct navigation tests MUST include:

```text
unauthorized route
authorized route outside scope
authorized role with missing permission
expired assignment
revoked delegation
suspended user
cross-tenant resource
```

Expected result:

```text
403 Forbidden
```

or appropriate `401/404` behavior according to the security design.

---

# 214. SIDEBAR GENERATION

Do NOT hard-code one universal sidebar and hide items.

Navigation should be generated from authorized capabilities.

Example:

```text
role
+
permissions
+
scope
+
portal
+
feature flags
=
navigation
```

The server should provide authorized navigation metadata or the frontend should resolve it from the canonical registry plus server authorization.

A user with:

```text
VIEW_TRIPS
```

but not:

```text
REASSIGN_TRIP
```

may see Trips but MUST NOT see Reassign.

A user with:

```text
VIEW_FINANCE
```

but not:

```text
EXPORT_FINANCE
```

may view permitted finance pages but MUST NOT see/export financial data.

---

# 215. BUTTON/ACTION AUTHORIZATION

Every mutation button must have:

```text
visible if potentially useful
enabled only if authorized
```

However, hiding/disabling the button is NOT sufficient.

Backend must revalidate:

```text
permission
scope
state
policy
tenant
resource
```

Examples:

```text
Create Admin
Assign Driver
Reassign Trip
Move Passenger
Offboard Employee
Approve Booking
Approve Expense
Export Report
Block Driver
Emergency Override
```

---

# 216. EMPTY, ERROR AND LOADING STATES

Every production page MUST have explicit states.

Loading:

```text
Skeleton
```

Empty:

```text
No records in your authorized scope.
```

Error:

```text
We could not load this data.
Retry
Request ID
```

Unauthorized:

```text
You do not have permission to access this resource.
```

Out of scope:

```text
This resource is outside your authorized scope.
```

Never show raw stack traces, database errors or secrets to users.

---

# 217. NO FAKE DATA RULE

MIMO MUST search the repository for:

```text
mock
dummy
fake
placeholder
sample
hardcoded KPI
random
Lorem
demo count
static trip
static employee
```

and classify each occurrence.

Development/test fixtures may remain where intentionally used.

Production dashboards MUST use real service/API data.

If a backend capability is missing, implement it or clearly mark the feature as unavailable; do not fabricate a value.

“No data yet” is acceptable.

A fake “97% utilization” is not.

---

# 218. RESPONSIVE / MOBILE RULES

Desktop operational portals:

```text
minimum 1280px design target
```

Mobile employee/driver/guard:

```text
mobile-first
touch targets
offline handling
GPS permission state
network state
battery-conscious updates
```

Driver and Guard workflows must remain usable during poor network connectivity.

---

# 219. REAL-TIME DASHBOARD RULES

Use existing WebSocket infrastructure where appropriate.

Real-time events include:

```text
driver.status.changed
driver.location.updated
trip.assigned
trip.reassigned
trip.started
trip.arrived
passenger.boarded
passenger.no_show
passenger.moved
trip.breakdown
sos.created
capacity.released
optimization.started
optimization.completed
compliance.blocked
```

The client MUST reconcile events with authoritative server state.

Do not treat a WebSocket event as proof of authorization.

---

# 220. DASHBOARD PERFORMANCE

Dashboard endpoints MUST avoid N+1 queries.

Use:

```text
server-side aggregation
proper indexes
pagination
caching where safe
authorization-aware cache keys
async jobs for heavy reports
```

Cache keys MUST include all relevant authorization dimensions.

Never use:

```text
dashboard:company:123
```

if the response also depends on:

```text
role
scope
data classification
permissions
```

unless the cached object is independently safe and authorization is rechecked before use.

---

# 221. DASHBOARD SECURITY TEST MATRIX

For every role, test:

```text
Own authorized company
Unauthorized company
Own site
Unauthorized site
Own process
Unauthorized process
Own shift
Unauthorized shift
Own hierarchy
Unauthorized hierarchy
Own employee
Other employee
Own trip
Other trip
Own vendor
Other vendor
Own financial data
Unauthorized financial data
Own GPS data
Unauthorized GPS data
Authorized export
Unauthorized export
```

Also test forged:

```text
companyId
siteId
processId
employeeId
tripId
driverId
vehicleId
vendorId
role
permission
scope
```

All must be server-validated.

---

# 222. E2E PORTAL ACCEPTANCE MATRIX

Minimum E2E coverage:

| Role | Login | Dashboard | Navigation | Detail | Mutation | Export | Unauthorized access |
|---|---:|---:|---:|---:|---:|---:|---:|
| SUPERADMIN | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| MOVE-IN-ADMIN | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| FINANCE_TEAM | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| PROJECT_MANAGER | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| PROJECT_COORDINATOR | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| PLATFORM_COMPLIANCE | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| SECURITY_ADMINISTRATOR | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| SUPPORT_ENGINEER | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| PLATFORM_AUDITOR | ✓ | ✓ | ✓ | ✓ | N/A/read-only | ✓ | ✓ |
| TRANSPORT_ADMIN | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| TRANSPORT_SUB_ADMIN | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| TRANSPORT_COORDINATOR | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| TRANSPORT_COMPLIANCE | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| DIRECTOR | ✓ | ✓ | ✓ | ✓ | governed | ✓ | ✓ |
| SENIOR_MANAGER | ✓ | ✓ | ✓ | ✓ | governed | ✓ | ✓ |
| MANAGER | ✓ | ✓ | ✓ | ✓ | governed | ✓ | ✓ |
| ASSISTANT_MANAGER | ✓ | ✓ | ✓ | ✓ | governed | ✓ | ✓ |
| TEAM_LEADER | ✓ | ✓ | ✓ | ✓ | governed | ✓ | ✓ |
| EMPLOYEE | ✓ | ✓ | ✓ | ✓ | ✓ | own data only | ✓ |
| TRAINER | ✓ | ✓ | ✓ | ✓ | governed | ✓ | ✓ |
| VENDOR_ADMIN | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| VENDOR_DISPATCHER | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| DRIVER | ✓ | ✓ | ✓ | ✓ | ✓ | own data only | ✓ |
| GUARD | ✓ | ✓ | ✓ | ✓ | ✓ | own data only | ✓ |

“✓” means implemented and tested, not merely mocked.

---

# 223. TEST PROFILES — MANDATORY DEVELOPMENT/UAT SEEDS

Create safe non-production test profiles for every role:

```text
test.superadmin
test.moveinadmin
test.finance
test.projectmanager
test.projectcoordinator
test.platformcompliance
test.securityadmin
test.support
test.auditor

test.transportadmin
test.transportsubadmin
test.transportcoordinator
test.transportcompliance
test.director
test.seniormanager
test.manager
test.assistantmanager
test.teamleader
test.employee
test.trainer

test.vendoradmin
test.vendordispatcher
test.driver
test.guard
```

Each test profile MUST have intentionally different scopes.

Example:

```text
TRANSPORT_ADMIN
Company: CONC001
Sites: PUN01, MUM01

TRANSPORT_SUB_ADMIN
Company: CONC001
Site: PUN01
Process: VOICE01

MANAGER
Company: CONC001
Site: PUN01
Hierarchy: Operations > Team A

EMPLOYEE
Company: CONC001
Site: PUN01
Self only
```

Use these profiles to prove that role-based separation actually works.

Never seed real production credentials.

---

# 224. DEMO MODE RULE

If the existing project currently has a DEMO MODE, it MUST be clearly separated from production.

Production startup MUST NOT silently fall back to:

```text
mock database
in-memory users
fake dashboard metrics
fake authentication
fake GPS
fake billing
```

If required infrastructure is unavailable, fail loudly with an actionable configuration error.

---

# 225. API ↔ DASHBOARD TRACEABILITY

For every dashboard widget, maintain a traceability record:

```text
Portal
Role
Dashboard
Widget
Metric
API endpoint/service
Prisma model/query
Permission
Scope rule
Data classification
Drilldown
Test
```

Example:

```text
Transport Admin
→ Dashboard
→ Available Drivers
→ GET /drivers/availability
→ DriverAvailability
→ VIEW_DRIVER_STATUS
→ company/site/process scope
→ operational
→ /transport/drivers?availability=AVAILABLE
→ E2E-TA-DRIVER-001
```

No widget may be orphaned.

---

# 226. PROFILE CREATION TRACEABILITY

For every profile creation field:

```text
UI field
→ API DTO
→ validation
→ service
→ authorization
→ Prisma model
→ audit event
→ E2E test
```

Examples:

```text
Role
→ roleId
→ RoleAssignment

Scope
→ AccessScope tuple
→ AccessScopeAssignment

Permission
→ PermissionAssignment

Delegation
→ Delegation record

Approval authority
→ ApprovalAuthority record

MFA requirement
→ security policy / assignment

Effective dates
→ effectiveFrom/effectiveTo
```

Do not invent duplicate persistence models if existing models already support the requirement.

---

# 227. AUDIT REQUIREMENTS FOR DASHBOARDS AND PROFILES

Audit at minimum:

```text
Login
Logout
Role switch
Profile creation
Profile modification
Role change
Permission change
Scope change
Delegation creation
Delegation revocation
MFA change
Device binding change
Access preview
Admin creation
Employee onboard
Employee offboard
Booking approval
Dispatch assignment
Trip reassignment
Passenger movement
No-show
Breakdown
Emergency override
Export
Financial mutation
Compliance block
Break-glass access
```

Audit record should capture, where appropriate:

```text
actor
tenant
authority domain
role
permission
scope
resource
action
before
after
reason
timestamp
request ID
IP/device metadata according to privacy policy
```

---

# 228. FINAL MIMO / OPENCODE EXECUTION DIRECTIVE

The implementation agent MUST treat this document as an implementation contract.

### DO

```text
Inspect existing code first.
Reuse working services.
Reuse existing Prisma models where correct.
Reuse existing API controllers where correct.
Add missing endpoints when required.
Add migrations for missing persistence.
Add seed data for every role.
Build real portal routes.
Build role-specific navigation.
Build real dashboard widgets.
Connect widgets to real APIs.
Implement profile creation.
Implement access preview.
Implement delegated admin creation.
Implement onboarding/offboarding.
Implement scope-aware filters.
Implement authorization tests.
Implement E2E tests.
Run production build.
Document every changed endpoint/model/permission/queue.
```

### DO NOT

```text
Do not rebuild everything.
Do not create a second auth system.
Do not create a second RBAC system.
Do not create a second tenant system.
Do not use frontend-only authorization.
Do not use fake production data.
Do not silently fall back to demo mode.
Do not expose every module to every role.
Do not assume job title equals permission.
Do not assume company membership equals authorization.
Do not assume manager relationship equals transport authority.
Do not allow client-supplied scope to override server scope.
Do not allow cached dashboards to bypass authorization.
Do not allow exports to bypass authorization.
Do not allow background jobs to bypass authorization.
Do not mark TODO pages as complete.
Do not mark a dashboard complete because the route loads.
```

---

# 229. DEFINITION OF DONE — ROLE-BASED DASHBOARD SYSTEM

The role-based dashboard system is COMPLETE only when:

### Identity

- Authentication works against the real identity system.
- Tenant/platform context resolves correctly.
- Role assignments resolve correctly.
- Multiple roles/contexts are supported where applicable.

### Authorization

- Central authorization is used.
- Permissions are server enforced.
- Scope is server enforced.
- Tenant isolation is server enforced.
- Hierarchy is server enforced where applicable.
- Data classification is enforced.
- Delegation is bounded.
- Direct URL bypass is blocked.
- Forged IDs are blocked.

### Portals

- Platform portal works.
- Finance portal works.
- Operations portal works.
- Transport portal works.
- Compliance portal works.
- Management portal works.
- Employee portal works.
- Vendor portal works.
- Driver portal works.
- Guard portal works.
- Support portal works.
- Security portal works.
- Auditor portal works.

### Dashboards

- Every required role has an appropriate dashboard.
- Widgets use real data.
- Widgets respect permissions.
- Widgets respect scope.
- Drilldowns work.
- Loading states work.
- Empty states work.
- Error states work.
- Real-time updates work where required.
- No fake production values exist.

### Navigation

- Sidebar is role/permission aware.
- Unauthorized modules are not exposed.
- Direct navigation is denied.
- Context switching is secure.

### Profile creation

- User creation works.
- Role assignment works.
- Permission assignment works.
- Scope assignment works.
- Hierarchy assignment works.
- Data visibility works.
- Delegation works.
- Approval authority works.
- MFA requirements work.
- Device requirements work.
- Effective dates work.
- Access Preview works.
- Create blocking rules work.
- Audit works.

### Transport administration

- Multiple Transport Admins work.
- Transport Sub-Admins work.
- Delegated scopes work.
- Coordinators work.
- Employee onboarding works.
- Employee offboarding works.
- Impact previews work.
- Dispatch dashboards work.
- Control Room works.
- Driver availability works.
- GPS works.
- Reassignment works.
- Passenger movement works.
- No-show workflow works.
- Breakdown workflow works.
- SOS workflow works.

### Reporting

- Reports are scope-aware.
- Exports are permission-aware.
- PII/GPS masking works.
- Financial separation works.
- Scheduled reports revalidate authorization.
- Async exports work.
- Export audit works.

### Quality

- Unit tests pass.
- Integration tests pass.
- Authorization tests pass.
- Cross-tenant tests pass.
- E2E role tests pass.
- Build passes.
- Typecheck passes.
- Lint passes.
- Production deployment succeeds.
- Observability is active.
- No critical known security issue remains.

---

# 230. FINAL ACCEPTANCE TEST — “ONE HOST SERVER SHOWS EVERYTHING” MUST BE ELIMINATED

This exact acceptance scenario is mandatory.

### Test A — Transport Admin

Login as:

```text
TRANSPORT_ADMIN
Company CONC001
Sites PUN01 + MUM01
```

Expected:

```text
Transport portal
Transport sidebar
Transport dashboard
Only CONC001 authorized data
PUN01/MUM01 data
Transport operational actions
```

### Test B — Sub-Admin

Login as:

```text
TRANSPORT_SUB_ADMIN
Company CONC001
Site PUN01
Process VOICE01
```

Expected:

```text
Same application shell
Different sidebar/action set
Only PUN01 + VOICE01 records
No MUM01
No unauthorized processes
No unrestricted admin creation
```

### Test C — Manager

Login as:

```text
MANAGER
Company CONC001
Authorized hierarchy only
```

Expected:

```text
Management portal
Management dashboard
Hierarchy-scoped employee information
No dispatch controls unless explicitly granted
No unrestricted driver reassignment
```

### Test D — Employee

Login as:

```text
EMPLOYEE
```

Expected:

```text
Employee portal
My Transport
My Bookings
My Trips
Notifications
Profile
Expenses
SOS
```

Expected NOT visible:

```text
Companies
Transport Admins
Dispatch
Control Room
All Drivers
All Vehicles
Billing
Platform Security
Other Employees
```

### Test E — Driver

Login as:

```text
DRIVER
```

Expected:

```text
Driver portal
Availability
Today's Trips
Navigation
Boarding
No-Show
Breakdown
SOS
GPS status
```

Expected NOT visible:

```text
Customer administration
All employees
Billing
Platform settings
Other vendors' data
```

### Test F — SUPERADMIN

Login as:

```text
SUPERADMIN
```

Expected:

```text
Platform portal
Platform navigation
Platform dashboards
```

But still:

```text
MFA
Audit
Confirmation
Break-glass
Authorization
```

must remain active.

### Test G — API bypass

Take a valid Sub-Admin session.

Manually change:

```text
siteId
processId
companyId
```

in an API request.

Expected:

```text
403 / authorization denial
```

### Test H — Direct URL bypass

Take Employee session and navigate directly to:

```text
/transport/control-room
/platform/companies
/finance/invoices
/security/events
```

Expected:

```text
denied
```

### Test I — Export bypass

Take Manager session and attempt to export unauthorized employee/financial data.

Expected:

```text
denied
```

### Test J — Admin creation bypass

Take Coordinator session without delegation permission and call the admin creation endpoint directly.

Expected:

```text
denied
```

This test suite is the proof that the “one host server shows everything” problem has actually been fixed.

---

# 231. FINAL INSTRUCTION TO THE IMPLEMENTATION AGENT

Do not report:

```text
“Role-based dashboard added”
```

merely because routes or components exist.

Report completion only with evidence:

```text
Portal
Route
Role
Permissions
Scope
API
Database
Widgets
Actions
Tests
Build
```

For every role.

The implementation report MUST contain a matrix similar to:

```text
ROLE
PORTAL
ROUTE
DASHBOARD
SIDEBAR
API
PERMISSIONS
SCOPE
MUTATIONS
EXPORTS
E2E TEST
STATUS
```

Use:

```text
IMPLEMENTED
TESTED
VERIFIED
```

instead of vague:

```text
DONE
```

A feature is not complete until it is implemented AND verified.

---

# 232. MASTER PROJECT FINALITY RULE

This Phase 189–232 addendum is an explicit execution layer over the existing MOVE-IN-SYNC master plan.

If another earlier section says only:

```text
“build dashboard”
```

interpret it using this section's concrete requirements.

If another section defines a role, preserve that role.

If another section defines a permission, preserve that permission.

If an existing implementation conflicts with the security rules, the security rules win.

If an existing working implementation already satisfies the requirement, reuse it rather than replacing it.

If a requirement cannot be safely implemented because a prerequisite is missing, implement the prerequisite or report the exact blocker. Never fake completion.

The final product must behave as:

```text
ONE PLATFORM
+
MULTIPLE TENANTS
+
MULTIPLE COMPANIES
+
MULTIPLE SITES
+
MULTIPLE PROCESSES
+
MULTIPLE ROLES
+
MULTIPLE ADMINISTRATORS
+
DELEGATED SCOPES
+
CENTRAL AUTHORIZATION
+
ROLE-SPECIFIC PORTALS
+
REAL DASHBOARDS
+
REAL APIs
+
REAL DATA
+
AUDITABILITY
+
PRODUCTION TESTING
```

and never as:

```text
ONE HOST
→
EVERY USER
→
EVERY MENU
→
EVERY RECORD
```

**END OF PHASE 189–232 ROLE DASHBOARD EXECUTION MASTER SPECIFICATION**

---

# FINAL V6 STATUS

**Document:** MOVE-IN-SYNC Complete Master Project Plan  
**Version:** FINAL V6 — Role Dashboard & Profile Execution Ready  
**Purpose:** Production implementation specification for MIMO/OpenCode  
**Status:** LIVE IMPLEMENTATION  
**Primary clarification added:** Screen-by-screen role portals, dashboard widgets, sidebars, profile creation, access preview, scope enforcement, API traceability, test profiles and acceptance tests.

The implementation agent MUST inspect the repository before modifying code and MUST preserve all existing working functionality.
